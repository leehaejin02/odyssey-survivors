# Odyssey Survivors — 프로젝트 규칙

NAN 2026 사전 과제. **접수 마감 2026-08-10** (착수 8/8, 가용 이틀).

> **제출물 중 하나라도 누락되면 심사 대상에서 제외된다.**
> 게임 완성도보다 제출물 완비가 절대 우선이다. 충돌하면 게임 기능을 자른다.
>
> **개인 참여** → 낼 것은 **4종**: 빌드+소스 / 플레이 영상 30~60초 / 게임 소개 PDF / **AI 활용 기술 PDF**.
> 「팀원 롤 기술서」는 2인 이상 팀만 해당하므로 제출하지 않는다.

## 이 프로젝트가 무엇인가

**탕탕특공대(Survivor.io)식 뱀서라이크 + 오디세이아.**
오디세우스가 집(이타카)으로 돌아가는 여정에서 만나는 **시련 하나 = 스테이지 하나**.

코어 루프: 이동만 조작 / 자동 공격 / 사방에서 몰려오는 적 웨이브 / 레벨업 시 3택 / 시련 클리어 or 사망.

## 세션 시작 시 반드시

1. `HANDOFF.md` — **첫 세션은 이것부터.** 지금까지 정한 것과 아직 안 정한 것
2. `WORKLOG.md`(생기면) — 지금까지 뭘 했고 다음에 뭘 할지
3. `docs/SUBMISSION.md`(생기면) — 제출물 4종 체크리스트. **남은 시간을 여기에 먼저 배분한다**
4. `docs/GDD.md`(생기면) — 특히 **스코프 라인**과 **완료 기준**
5. `docs/DECISIONS.md`(생기면) — 이미 내린 결정. 다시 논의하지 않는다

## ⚠️ playtest의 합격 구간이 비어 있다 — gd가 채워야 한다

`.claude/agents/*.md` 5종은 이전 프로젝트(`capybara-escape`)에서 가져와 **이 게임에 맞게 개정을 마쳤다**
(2026-08-08). 카피바라 전용 내용은 남아 있지 않다.

**단 하나 의도적으로 비워 둔 곳이 있다** — `playtest.md`의 **목표 지표 합격 구간**이 전부 「미확정」이다.
그 숫자는 `gd`의 소유물이고 `docs/GDD.md`가 원본이기 때문이다.

- **`gd`는 GDD에 목표 지표를 확정하는 순간 `playtest.md`의 표를 같은 값으로 채운다.**
- **그 전까지 `playtest`는 합격/불합격을 판정할 수 없다.** 측정값만 보고하고 "합격선 미확정 — 판정 불가"라고 쓴다.
- 이 상태에서 나온 "통과"는 무효다. 하네스 18(playtest 측정 없이 밸런스 확정 금지)에 걸린다.

## 에이전트 팀

| 에이전트 | 담당 | 소유 파일 |
|---|---|---|
| `director` | 인텐트 → 메타 프롬프트 → 라우팅 | (없음) |
| `gd` | 무엇을 만들까 / 규칙 · 수치 | `docs/`, `src/config/balance.ts` |
| `tech` | 어떻게 만들까 / 코드 | `src/` (balance.ts 제외) |
| `playtest` | 수치가 맞는가 (헤드리스 시뮬) | (읽기 전용) |
| `verify` | 기준을 만족하는가 | (읽기 전용) |

기본 흐름: **director → gd → 사용자 승인 → tech → playtest → verify**
단계를 건너뛰는 건 허용된다. 단 **왜 건너뛰는지 반드시 밝힌다.** 조용한 생략은 금지다.

## 하네스 — 절대 하면 안 되는 것

### 아키텍처
1. **`src/sim/` 아래에서 Phaser를 import하지 않는다.** 게임 로직은 브라우저 없이 Node에서 돌아야 한다. 이 경계가 무너지면 `playtest` 에이전트가 성립하지 않고, 그건 이 프로젝트의 핵심 차별점을 잃는 것이다.
2. **밸런스 수치는 `src/config/balance.ts`에만 존재한다.** 속도·공격력·쿨다운·스폰 주기·경험치 곡선을 코드에 직접 쓰지 않는다.
3. 렌더링 씬에서 게임 규칙을 판정하지 않는다. 씬은 sim의 상태를 그리기만 한다.

### 보안
4. **비밀값은 어떤 형태로도 저장소에 들어가지 않는다.** 키·토큰·비밀번호를 코드·문서·설정에 쓰지 않는다.
5. **비밀값이 포함된 명령을 실행하지 않는다.** 명령 전체가 허용 목록(`.claude/settings.local.json`)에 문자열로 기록돼 커밋될 수 있다.

### 스코프 (가용 이틀이라 이게 하네스다)
6. **가용 시간은 게임에 하루, 제출물에 하루다.** 8/8이 게임, 8/9가 영상·PDF 2종·배포다.
7. 스코프를 늘리려면 **동시에 무엇을 버릴지 같이 제시**한다. 순증가 제안은 거절한다.
8. 새 npm 의존성은 **승인 없이 추가하지 않는다.** Phaser·Vite·TypeScript 외에는 기본 거절.
9. **WON'T (초기값 — GDD가 확정되면 그쪽이 원본)**: 스킬 진화 트리, 상점, 런 간 영구 강화, 세이브, 캐릭터 커스터마이즈, 멀티플레이·서버, 컷신·대사·NPC, 모바일 대응.

### 대회 제출 규정 (위반 = 심사 제외 또는 선발 취소)
10. **커밋을 마지막에 몰아서 하지 않는다.** 작업 단위마다 커밋한다. "커밋 기록 유지"가 제출 요건이고, 에이전트가 단계별로 일한 흔적이 곧 「AI 활용 기술 문서」의 물증이다.
11. **외부 에셋·오픈소스를 가져오는 순간 `docs/ASSET_CREDITS.md`에 출처와 라이선스를 적는다.** 나중에 추적하지 않는다. 무단 도용은 선발 취소 사유다.
12. **유료 라이선스가 필요한 것을 쓰지 않는다.** 심사자가 추가 비용 없이 실행할 수 있어야 한다.
13. **플레이 영상은 실제 플레이 화면만 쓴다.** AI 합성·조작·타인 영상 도용은 규정 위반이다.
14. 제출 후에는 내용을 변경할 수 없다. **마감 당일에 처음 배포하지 않는다** — 배포는 8/9에 끝낸다.
15. 제출 형태는 **웹 빌드(링크 클릭만으로 브라우저 플레이)**. `.exe` 직접 제출은 규정상 불가.

### 정직성
16. **확인 못 한 것을 통과로 보고하지 않는다.** 실행 대신 정적 분석·계산으로 대체했으면 "실측 아님"을 반드시 명시한다.
17. **작업 중 이상한 걸 발견하면 무조건 말한다.** 스펙 모순, 기존 코드가 문서와 다르게 동작, "완료"로 적혀 있는데 실제로는 안 되는 것 — 조용히 우회하지 않는다.
18. 밸런스는 감으로 확정하지 않는다. **`playtest` 측정 없이 "밸런스 맞췄다"고 쓰지 않는다.**

## 이전 프로젝트 — 건드리지 않는다

`C:\Users\gjlee\projects\capybara-escape` (이세계 카피바라)는 **별개 저장소이고 폴백**이다.
8/9 시점에 둘 중 나은 것을 제출한다. **이 세션에서 카피바라 파일을 수정하지 않는다.**

## 발화 → 절차 연결

| 사용자가 이렇게 말하면 | 이렇게 한다 |
|---|---|
| "○○ 만들어줘" / "○○ 바꾸자" | `director`부터 호출 (작은 일이면 director가 스스로 불필요 판정) |
| "다 됐나?" / "완료됐어?" | `verify` 호출. 그 전엔 완료라고 말하지 않는다 |
| "밸런스 어때?" / "너무 쉬워/어려워" | `playtest` 호출 후 그 숫자로 답한다 |
| "커밋해줘" / "정리하자" | WORKLOG 갱신 → 타입체크 → 커밋. `.claude/` 산출물은 **공유 자산이라 무조건 포함** |

## 기록

- **WORKLOG.md**: 세션마다 한 일 / 결정한 것 / 다음 할 일 (날짜 역순)
- **docs/DECISIONS.md**: 되돌리기 어려운 선택과 **그 이유**. 이유 없는 결정은 다음 세션에서 다시 논의된다
- 에이전트·커맨드 호출 횟수는 제출 근거가 된다. 마감 전 한 번 집계한다

## 작업 방식

- 되돌리기 어려운 것(배포, 의존성 추가, 파일 대량 삭제, 스코프 변경)은 **승인받고** 진행한다. 설명만 하고 진행하는 것은 승인이 아니다.
- 작업 성격이 바뀌면(기획 ↔ 구현) 컨텍스트 사용률과 무관하게 **세션을 새로 연다.**
- 한 번에 한 축만 바꾼다. 여러 수치를 동시에 바꾸면 무엇이 효과였는지 알 수 없다.
