# WORKLOG

세션마다 **한 일 / 결정한 것 / 다음 할 일**. 날짜 역순.
이 파일은 제출물 4번(AI 활용 기술 문서)의 「개발 과정」 절 원본이 된다. 마지막에 몰아 쓰지 않는다.

---

## 2026-08-08 (세션 4) — 구현 · 밸런스 6회차 · 배포

가장 긴 세션. 게임을 0줄에서 배포까지 가져갔다.

### 한 일

**구현 (`tech`)**
- `src/sim/` 11파일 — 시드 고정 RNG(mulberry32), 스폰 압력 곡선, 3택 구성, 봇 정책, 엔진, 지표 집계.
  `Math.random()` 0건. `check-boundary` exit 0으로 Phaser 비의존 유지
- `src/scenes/` — Boot / Title / Game / Result + `Hud` + 텍스처 키 간접층(`sprites.ts`)
- `scripts/sim-runner.mjs` — `npm run sim -- 200`이 3종 × 200회를 완주하고 §6 8지표 출력
- 렌더 해상도를 월드 좌표계에서 분리(`SUPERSAMPLE` 3) — 백킹 1440×810 + 카메라 줌 3.
  카메라 월드 뷰는 480×270 불변이라 D14와 밸런스 근거가 보존된다
- 3택 카드에 효과·현재값→다음값·레벨 표시. 설명문은 손으로 안 쓰고 `stat`+`mode`+`value`에서 조립

**에셋 (코디네이터)**
- `scripts/prep-assets.mjs` 신규, **의존성 0개**(ffmpeg raw 파이프 + 순수 JS)
- 생성물이 알파 없이 체커보드가 픽셀로 그려져 온 것을 테두리 flood fill로 제거.
  갇힌 주머니(활 안쪽)는 "체커보드는 두 톤이 함께 나타난다"는 성질로 골라 지웠다
- 축소 시 흰 테를 막는 알파 블리드, Lanczos 사전 축소

**밸런스 (`playtest` ↔ `gd`, 6회차)**
- 1회차 → 6회차 클리어율: `0.000 / 0.055 / 0.030` → **`0.455 / 0.255 / 0.450`**
- 축 A `WEAPON_BOW.DAMAGE` 6→10 (6 < grunt.HP 10이라 "1발 킬"이 수학적으로 불가능했다)
- 축 B `WEAPON_BOW.AIM` `bossFirstThenNearest` (키클롭스가 구조적으로 클리어 불가였다)
- 축 C `XP_CURVE.GROWTH` 1.25→1.10 (회수가 아니라 공급이 병목이었다)
- 축 D `ENEMIES.boss.HP` 700→324 (사전 등록 산식, 예측 0.50 / 실측 0.490)
- 축 E 연꽃 `enemyHpMul` 1.15→1.00 (연꽃에서만 "1발 킬" 기준이 깨져 있었다)
- 축 F `spawnWeights` 서사 정합 (구혼자가 연꽃 해안에 있었다)

**검증 (`verify`)** — §8 42항목 O/X. 3·4·5회차 커밋을 worktree로 체크아웃해
`npm run sim -- 200`을 독립 실행하고 "서로소 시련 집합" 무동작을 바이트 단위로 재현 확인

**배포** — `gh-pages` 브랜치. https://leehaejin02.github.io/odyssey-survivors/

### 결정한 것 (D13~D58)

되돌리기 어려운 것만 추린다. 전문은 `docs/DECISIONS.md`.

| # | 결정 | 이유 |
|---|---|---|
| D40 | 렌더 해상도와 월드 좌표계 분리 | 백킹이 480×270 고정이라 필터로는 화질 개선이 불가능한 구조였다 |
| D44 | 밸런스 왕복 제한을 계산 누락 때문에 개정 | `boss.HP`가 연꽃에 영향 0인 걸 계획에 안 넣어 3지표 미달 시련을 한 번도 안 건드릴 뻔했다 |
| D50 | 표시 크기 상향. D15의 근거가 틀렸었다 | `MAX_ALIVE` 90은 아레나 전체 상한인데 화면 기준으로 계산하고 있었다 |
| D54 | 난이도 라벨 2단계 | n=200 표준오차 3.5%p. 4.0%p 차이는 노이즈라 이름을 붙이면 화면이 거짓말이 된다 |
| D56 | 시련별 적 구성 정합 | 비둘기집 — 시련 3종 × 잡몹 2종이라 한 종은 반드시 두 시련에 걸친다 |
| D58 | 새 미달이 생겼지만 되돌리지 않음 | 보이는 결함(한 판에 보임)을 안 보이는 결함(200판 통계)으로 바꾼 교환이다 |

### 미달인 채로 내는 것 (GDD §6-B)

**§6 목표 지표와 `TARGET_METRICS`는 여섯 회차 내내 한 글자도 안 바꿨다.**
숫자가 안 나온다고 기준을 내리면 `playtest`의 판정 근거 자체가 사라진다.

- 연꽃 클리어율 0.255 (합격 0.35~0.70)
- 3종 전부 레벨업 중앙값 5/5/6 (합격 8~14)
- 키클롭스·연꽃 초당 처치 0.896/0.690 (합격 1.0~4.0)
- 키클롭스 사망 생존시간 0.893 (합격 0.45~0.85) — 6회차에 새로 생겼다

### 다음 할 일 (8/9)

1. **게임을 한 판 클리어까지 직접 해본다.** 클리어 화면에 도달한 사람이 아직 없다.
   헤드리스로는 3종 다 나오지만 브라우저 실측이 없다. 여기 버그가 있으면 영상을 못 찍는다.
   확률은 키클롭스 0.455 / 이타카 0.450 / 연꽃 0.255
2. **플레이 영상 30~60초.** 실제 플레이 화면만. AI 합성·조작은 규정 위반
3. **게임 소개 PDF** — 제목·한 줄 소개·목표·조작·종료 조건·실행 방법·링크
4. **AI 활용 기술 PDF** — 재료는 이 세션에 다 쌓였다.
   `CLAUDE.md` 하네스 / 에이전트 5종 / 반려 왕복 / 커밋 / D1~D58 / 사전 등록 산식의 예측 적중
5. 카피바라 폴백 배포 (별도 세션 15분) — 아직 구버전이다

### 확인 못 한 것

- 클리어 화면 브라우저 도달 (§8-1)
- 클릭 없이 Enter로 시작되는지 — 자동화 브라우저에서 재현 안 됨, `tech`는 헤드리스 Edge에서 됐다고 보고
- HUD 항목 다수, 성능(fps) — 전부 브라우저 실측 필요

---

## 2026-08-08 (세션 3) — `gd` / 렌더 축: AI 생성 에셋 + HUD 규격 확정

### 한 일

- `docs/GDD.md`
  - §2 스코프 상한 표에 **「에셋」 행 추가** — 이미지 5장 + BGM 1곡 = **총 6개** 상한
  - WON'T에 **렌더 축 배제 목록 11항** 추가 (스프라이트시트 애니메이션 / 패럴럭스 / 엔딩 컷신 /
    효과음 개별 제작 / 시련별 고유 배경 / 폰트 파일 / 3택 아이콘 PNG / BGM 2곡 이상 /
    파티클 / 화살·오브 이미지 / 로딩 연출)
  - §8에 **8-A(HUD 10항목) / 8-B(에셋 6항목)** 완료 기준 추가 — 전부 브라우저에서 O/X로 찍히는 문장
  - **§9 신설** — 미술 양식·에셋 6개 규격·시련 틴트·3택 배지·HUD 배치도·오디오·이 축의 대가
- `src/config/balance.ts`
  - `TrialDef`에 **`bgTint` 필드 1개 추가** + 시련 3종 값 (시각 구분의 유일한 수단)
  - **`SCREEN` / `VISUAL` / `HUD` / `AUDIO` / `ASSETS`** 5개 섹션 신설. 전 상수에 근거 1줄
  - 구획선으로 **"이 아래는 렌더 축이며 sim이 읽지 않는다"** 를 파일 안에 명시
- `docs/ASSET_CREDITS.md` **신설** — 6열 표(생성 도구/모델명/생성일/프롬프트 요지/약관 URL/확인일),
  에셋 6행 미리 배치, 기입 규칙, 라이브러리 3종 라이선스
- `docs/SUBMISSION.md` **신설** — 제출물 4종 + **8/9 시각별 타임박스** + 항목별 완료 판정 문장
- `docs/DECISIONS.md` — **D13~D24** 추가
- `WORKLOG.md` **신설** (이 파일)

### 결정한 것 (상세는 DECISIONS.md D13~D24)

| # | 한 줄 |
|---|---|
| D13 | 적색상(red-figure) 양식. 플레이어=차가운 색 / 적=따뜻한 색 |
| D14 | 480×270 + 카메라 줌 **동결** |
| D15 | 표시 px = player 18 / grunt 14 / brute 22 / boss 40. 히트박스와 분리 |
| D16 | 3택 아이콘은 **코드 배지**. PNG 생성 안 함 |
| D17 | 시련 시각 구분 = `TrialDef.bgTint` 한 필드. 배경 이미지 1장 공용 |
| D18 | 에셋 6개 / 4000KB 상한. **대가: 밸런스 왕복 2회 제한** → **D37로 3회로 개정**(전제였던 시간 부족이 실측으로 깨짐) |
| D19 | BGM 1곡(mp3 60초 루프). 효과음 없음 |
| D20 | **에셋 0개 폴백을 정상 동작으로 인정** |
| D21 | 웨이브 타이머 → **난이도 스텝 pip 9칸**으로 번역 |
| D22 | 진행도 바 = `trialProgress()` 반환값을 그대로 |
| D23 | 상용 게임 스크린샷 레퍼런스 금지. ①player를 ②~⑥ 레퍼런스로 |
| D24 | 폰트 파일 미도입. 본문 최소 8px |

### 확인한 것

- `.claude/agents/playtest.md`의 합격 구간 8행이 **GDD §6 및 `TARGET_METRICS`와 전부 일치**한다(대조 완료).
  git 상태가 `M`인 것은 D11에서 「미확정」을 채운 변경이 아직 커밋되지 않았기 때문이다. **불일치 없음.**
- §6 목표 지표 8개와 `TARGET_METRICS`는 **이번 세션에서 한 줄도 건드리지 않았다.** 렌더 축은 sim과 분리돼 있다.

### 다음 할 일

1. **커밋** (하네스 10 — 작업 단위마다). `.claude/agents/playtest.md`의 미커밋 변경도 함께.
2. `tech` — 씬 4종 + sim 구현. **에셋 도착을 기다리지 않는다**(D20 폴백 우선 구현).
   HUD는 GDD §8-A 10항목 + §9-5 앵커 그대로.
3. `playtest` — 시련 3종 × 200회. **왕복 예산 3회**(D18 → D37 개정). 용처 고정: 3회차 GROWTH / 4회차 boss.HP / 4회차 직후 동결.
4. 사용자 — 에셋 6개 생성. 도착 순서대로 `public/`에 투입, 그때마다 `ASSET_CREDITS.md` 빈칸 기입.

### 열려 있는 리스크

- **1024px 원본을 40px 이하로 축소한다**(최대 1/64). `pixelArt: true`는 NEAREST 샘플링이라
  런타임 축소만으로는 뭉갠다. 다운스케일 방식은 `tech`의 결정 영역이지만,
  **합격 문장은 "40px에서 보스의 단안이 식별된다 / 14px에서 grunt가 원형 얼룩으로 보이지 않는다"** 다.
  실물이 도착하면 브라우저에서 눈으로 확인해야 한다(HANDOFF: "안 보고 만들면 안 된다").
- 배포 URL·저장소 이름과 `vite.config.ts`의 `base` 일치 여부가 아직 미확인이다(SUBMISSION 미확인 사항).

---

## 2026-08-08 (세션 2) — `gd` / 기획 확정

- `docs/GDD.md` 작성 — 코어 루프 / 스코프 상한 / 3택 9종 / 스폰 압력 곡선 / 시련 3종 / **§6 목표 지표 8개** / 봇 정책 / 완료 기준 9항
- `src/config/balance.ts` 작성 — 전 상수에 근거 1줄. `trialProgress()` 단일 판정 함수
- `.claude/agents/playtest.md`의 **「미확정」 합격 구간을 GDD §6과 같은 값으로 채움** (D11)
- `docs/DECISIONS.md` — D1~D12

## 2026-08-08 (세션 1) — scaffold

- Phaser 3 + TS + Vite 골격, `src/sim/` Phaser 금지 경계 스캐너(`scripts/check-boundary.mjs`)
- Windows에서 npm(.cmd)을 못 찾던 `deploy.mjs` 수정, 경계 스캐너의 side-effect·동적 import 사각지대 수정
