# DECISIONS — 되돌리기 어려운 선택과 그 이유

재논의 금지. 뒤집으려면 "무엇을 대신 버릴지"를 같이 제시할 것.

## 2026-08-08 (gd, 2단계)

| # | 결정 | 이유 |
|---|---|---|
| D1 | **시련 3종 = 키클롭스의 동굴 / 연꽃 먹는 자들의 해안 / 이타카의 홀** | 셋 다 "체력 많은 적 1기 / 적 조합 비율 / 스폰 총량 상한"이라는 **기존 파라미터로만** 표현된다. 새 메카닉 0개 |
| D2 | **세이렌 탈락** | "원거리 적"으로 해석해도 적 투사체 = 새 엔티티·충돌·회피 축이 생긴다. 이동만 조작인 게임에서 회피 축 신설은 하루 안에 튜닝 불가 |
| D3 | **클리어 판정은 `trialProgress()` 단일 함수** — `경과초*wTime + 처치*wKill + 보스처치*wBossKill >= goal` | 시간·처치수·보스HP 세 종류의 클리어 조건을 가중치 3개로 흡수. 시련별 `if` 분기가 0개가 된다. 이게 "시련 3종 = 데이터 행 3개"의 실제 보증 장치다 |
| D4 | **무기 1종(활)** | 무기 1종 추가 = 적 3종 × 시련 3종 × 3택 9종 전부와 조합. 곱으로 늘어난다. 다양성은 3택 카테고리로 낸다 |
| D5 | **적 3종(grunt/brute/boss), 보스는 고유 패턴 0개** | 보스 패턴 3개짜리는 하루에 못 튜닝한다. 보스는 HP·크기·접촉피해만 다른 적이다 |
| D6 | **3택은 화력1+생존1+유틸1 강제 구성** | "3개 전부 공격력 +%"가 나오면 게임의 유일한 결정이 사라진다. 무작위 3장 뽑기를 쓰지 않는 이유 |
| D7 | **유한 아레나 960×540 (무한 필드 아님)** | 조작이 이동뿐이라 난이도는 스폰 압력과 **공간**에서만 나온다. 무한 필드면 도주만으로 안 죽는다 |
| D8 | **적 속도 상한 1.35× (최대 46 < 플레이어 60)** | 적이 플레이어보다 빨라지는 순간 이동 조작이 무의미해진다. 압박은 "못 도망침"이 아니라 "도망칠 공간 없음"으로 낸다 |
| D9 | **한 판 목표 길이 90초** | 제출물 2번이 30~60초 영상. 90초면 편집 없이도 클리어까지 담기고, 심사자가 링크를 눌러 한 판을 끝낼 수 있다 |
| D10 | **봇 3택 = 고정 우선순위 배열 + HP<50% 넥타르 예외. 무작위 없음** | 무작위 선택과 최적 선택은 클리어율이 완전히 달라진다. 시드 고정 시 완전 재현되어야 밸런스 왕복이 성립한다 |
| D11 | **목표 지표 8개 확정, `playtest.md` 표를 동일값으로 채움** | 합격선이 비어 있으면 playtest의 "통과"가 무효(하네스 18). GDD §6이 원본, `TARGET_METRICS`가 코드 사본 |
| D12 | **별도 `SPEC_*.md`를 쓰지 않는다** | 가용 이틀. 읽는 시간이 구현 시간에서 그대로 빠진다. GDD 1장 + balance.ts 주석으로 대체 |

## 2026-08-08 (gd, 렌더 축) — AI 생성 에셋 + HUD 편입

> 사용자 승인 완료. **스코프 편입 여부는 재논의하지 않는다.**
> 이 축의 전제: **sim은 아래 어떤 값도 읽지 않는다.** 그래서 §6 목표 지표(D11)는 한 줄도 바뀌지 않았고
> 밸런스 재측정 대상이 아니다.

| # | 결정 | 이유 |
|---|---|---|
| D13 | **미술 양식 = 고대 그리스 적색상(red-figure). 흑색상 기각** | `SPAWN.MAX_ALIVE = 90`. 밝은 바탕 위 검은 실루엣 90개는 한 덩어리로 뭉친다. 어두운 바탕 + 밝은 인물이어야 개체가 갈린다. 여기에 **플레이어=차가운 색 / 적=따뜻한 색**을 겹쳐 색상환 반대편에 배치한다 — 90마리 식별의 유일한 장치다 |
| D14 | **논리 해상도 480×270 + 카메라 줌 동결** | `balance.ts` 근거 주석의 절반이 화면 대비 비율이다(사거리 140 = 폭의 29%, 보스 반경 14 = 높이의 5%, 흡수 반경 18). 바꾸면 밸런스 왕복 예산(D18)이 착수 전에 소진된다 |
| D15 ⚠️ **값 부분은 D50으로 개정됨(→ 34/20/30/56). 「표시 > 히트박스」 원칙과 「점유율로 상한을 정한다」는 방법은 유효하다** | **표시 크기와 히트박스를 분리. 표시는 항상 히트박스보다 크다** — ~~player 18 / grunt 14 / brute 22 / boss 40~~ (히트박스 지름 8/8/12/28) | 근거는 **90마리가 전부 화면에 들어온 최악의 경우 점유율**이다. grunt 16px면 화면의 17.8%를 먹어 배경(= 시련 구분 수단)이 사라진다 → 14px에서 13.6%. 표시 < 히트박스 방향은 "안 닿았는데 맞았다"가 되므로 금지. boss 40은 grunt의 2.86배 — 보스는 고유 패턴이 0개(D5)라 위압감의 표현 수단이 크기뿐이다 |
| D16 | **3택 아이콘 9장을 생성하지 않는다. 코드 도형 배지(카테고리 3색 + 스택 수)** | PNG로 가면 에셋이 6→15개가 되고 사용자 생성 시간이 3배가 된다. 게다가 그 9장은 12px에서 서로 구분되지 않는다. **3택 카드의 의미는 한글 이름 텍스트가 나른다** — 아이콘은 카테고리와 스택 수만 나르면 되고, 그건 색 3개와 숫자로 충분하다 |
| D17 | **시련 3종의 시각 구분 = `TrialDef.bgTint` 한 필드.** 배경 이미지는 3종 공통 1장 | 시련별 배경 이미지를 만들면 에셋이 3배가 되고, 무엇보다 **씬에 시련 id 분기가 생겨 GDD §8-2(분기 0개)를 깬다.** 틴트를 데이터 행의 필드로 두면 시련 3종이 여전히 "데이터 행 3개"로 유지된다(D1·D3의 연장) |
| D18 | **에셋 총량 상한 6개(이미지 5 + BGM 1) / `public/` 4000KB. 대가로 밸런스 왕복을 2회로 제한** ⚠️ **왕복 2회 부분은 D37로 개정됨(→ 3회). 에셋 상한 부분은 유효하다** | 스코프는 순증가할 수 없다(하네스 7). 렌더 축을 얻는 대신 튜닝 왕복을 판다. 3회차부터는 `playtest` 측정만 하고 수치 동결. 4000KB는 20Mbps에서 첫 로드 약 1.6초 — 웹 배포라 로딩이 심사자의 첫인상이다 |
| D19 | **BGM 1곡(mp3, 60초 루프, 음량 0.5, `M` 음소거). 효과음은 만들지 않는다** | 곡이 2개가 되는 순간 "언제 바꾸는가"라는 규칙 축이 생긴다. mp3는 Safari 포함 전 브라우저가 코덱 추가 없이 재생한다(ogg는 보장 안 됨). 효과음 3종(피격·발사·처치)은 곱셈 축이고, 발사는 초당 1.8회라 소음이 된다 |
| D20 | **에셋이 0개인 폴백 상태를 "정상 동작"으로 인정한다** | 그래야 `tech`가 사용자의 에셋 생성을 기다리지 않는다. 도착 우선순위(player > grunt > arena > boss > brute)의 뒤쪽이 끝내 안 와도 제출 가능해야 한다. 완료 기준 §8-20이 이걸 기계적으로 강제한다 |
| D21 | **"웨이브 타이머"를 만들지 않는다. 난이도 스텝 pip 9칸 + 경과/제한시간으로 번역한다** | **이 게임에 웨이브 개념이 없다.** 압력은 `SPAWN.STEP_SEC`(10초=1스텝) 곡선 하나뿐이다(GDD §4). 그대로 만들면 존재하지 않는 개념을 화면에 표시하게 된다. pip 9칸 = 목표 판 길이 90초 |
| D22 | **진행도 바는 `trialProgress()` 반환값을 그대로 그린다. HUD는 진행도를 계산하지 않는다** | HUD가 자체 계산하면 클리어 판정 로직이 2곳이 되어 GDD §8-3을 깬다. 두 곳이 어긋나면 "바는 다 찼는데 클리어가 안 된다"가 나오고, 그건 마감 전날에 가장 찾기 어려운 종류의 버그다 |
| D23 | **상용 게임 스크린샷을 생성 레퍼런스로 쓰지 않는다.** ①player.png를 ②~⑥의 레퍼런스로 재사용해 양식을 통일한다 | 하네스 11·12. 타인 저작물이 생성 과정에 개입할 여지를 원천 차단한다. 무단 도용은 선발 취소 사유이고, 그 위험을 감수해서 얻는 게 "양식 통일"뿐이라면 자체 생성물로 대체하는 편이 낫다 |
| D24 | **폰트 파일을 도입하지 않는다. 시스템 sans-serif, 본문 최소 8px** | 폰트 라이선스 이슈가 원천 소멸한다. 8px 하한은 카피바라 실측 — 6px 한글은 정수배 스케일에서도 획(1px)이 소실된다 |

## 2026-08-08 (gd, 에셋 실물 도착 후) — 배경 규격 확정

| # | 결정 | 이유 |
|---|---|---|
| D25 | **배경은 `assets/arena.jpg` 960×540 불투명 JPEG. `ARENA_IMAGE_SCALE` 0.5 → 1.0. `MAX_IMAGE_TOTAL_KB` 1500 → 800.** 스프라이트 4종은 투명 PNG 그대로 | 실측된 5안 중 tech는 A(1920 JPEG 163.3KB)를 권고했으나 **D(960 JPEG 26.7KB)를 택했다.** ⓐ `main.ts`가 `pixelArt: true`(NEAREST)이고 백킹스토어가 480×270 고정(D14)·줌 1이라 **1 월드 px = 1 캔버스 px**다. 1920 원본을 0.5배로 깔면 2×2 블록 중 1픽셀만 표본되고 **나머지 3/4는 화면에 절대 도달하지 않는다** — 용량 4배에 화질 이득 0. ⓑ 오프라인 Lanczos(면적 평균)가 런타임 NEAREST(점표본)보다 낫다. 즉 D는 A보다 **작고 동시에 더 잘 보인다.** ⓒ JPEG 예외의 근거는 "배경엔 투명 픽셀이 원리적으로 없다" + "석재 노이즈에 무손실은 27배 손해(720.8 vs 26.7KB)". 이 예외는 **배경 1장에만** 적용된다 — 스프라이트를 JPEG로 바꾸면 알파가 사라져 사각형이 된다(GDD §9-2·§8-26에 명시). ⓓ 새 상한 800KB는 최악의 폴백(960 무손실 PNG 720.8 + 스프라이트 8 = 728.6KB)이 아직 들어가는 값이다. 되돌리는 순서는 **품질↑ → 무손실 PNG**이며, **해상도를 1920으로 되돌리는 선택지는 없다** |

## 2026-08-08 (gd, 에셋 출처 확정) — 하네스 11 해소

| # | 결정 | 이유 |
|---|---|---|
| D26 | **AI 생성 에셋 6개의 출처는 `DeeVid`(deevid.ai) 단일 서비스로 확정한다.** 이미지 `gpt-image-2`(image-to-image / 1:1 / 2K), 오디오 `music` + `quality V2.0`, **라이트 플랜(유료)**, 생성일 2026-08-08. 약관 `https://deevid.ai/terms`, 확인일 2026-08-08. **하네스 12는 통과 판정.** | ⓐ **1차 잠정 기재가 틀렸었다는 사실을 기록으로 남긴다**: 표에 한때 `Nano Banana`(이미지)/`Google Lyria`(오디오)를 적었으나 서비스 화면 실측으로 **둘 다 반증**됐다. 원인은 사용자 구두 답변("전부 나노바나나")을 확인 없이 확정처럼 옮긴 것이다(하네스 16 위반). 재발 방지책은 **「모델명은 UI 표기 문자열을 그대로 옮기고, 실측 못 한 칸은 못 했다고 적는다」**는 기입 규칙이며 `ASSET_CREDITS.md`에 명문화했다. ⓑ **소유권은 1차 출처가 있다**: 약관 §6.3 *"As between DeeVid AI and the user, all rights, ownership, and interest in generated content belong to the user"* + *"DeeVid AI does not claim any independent intellectual property ownership"*. ⓒ **상업 이용은 1차 출처가 없다**: 약관 원문에 **플랜별 구분 조항이 존재하지 않는다.** 흔히 인용되는 "무료=워터마크+상업 불가 / 유료=상업 라이선스 포함"은 **제3자 리뷰 서술이지 1차 출처가 아니다.** 이 불확실성을 지우지 않고 문서에 그대로 남긴다 — 다만 **유료 플랜 + 비상업 제출**이라 어느 해석의 교집합 안에 있다. ⓓ **하네스 12 통과 판정의 근거**: 조항의 취지는 *"심사자가 추가 비용 없이 실행할 수 있어야 한다"*인데, 에셋은 생성이 끝나 **파일로 빌드에 포함돼 나가므로 심사자에게 DeeVid 구독이 필요 없다.** 런타임이 DeeVid를 호출하지 않는다. → **제작 도구의 비용은 이 조항의 대상이 아니다.** 대상은 "실행에 필요한 라이선스"다. **이 해석을 명시해 두지 않으면 다음 세션에서 재논의된다.** ⓔ 1차 `player.png`에만 「DeeVid AI」 워터마크가 있었던 건은 **워터마크를 임의 제거하지 않고 재생성본으로 교체**해 해소했다(경위는 `ASSET_CREDITS.md` §1-C). 1차에만 워터마크가 붙은 것은 무료 결과물 워터마크 정책과 앞뒤가 맞으나, **그 정책 자체는 1차 출처로 확인하지 못했다** |

## 2026-08-08 (gd, playtest 1회차 반려 대응)

> 1회차 실측: 클리어율 0.000 / 0.055 / 0.030, 레벨업 중앙값 2 / 1 / 2, 초당 처치 0.535 / 0.308 / 0.712.
> **§6 목표 지표(D11)는 한 줄도 바꾸지 않았다.** 측정값이 안 나온다고 합격선을 내리는 건 자기기만이다.

| # | 결정 | 이유 |
|---|---|---|
| D27 | **`WEAPON_BOW.DAMAGE` 6 → 10. 이번 회차에 바꾸는 유일한 밸런스 수치다** | `DAMAGE 6 < ENEMIES.grunt.HP 10`이라 기존 주석의 "grunt를 1발에 잡는다"가 **수학적으로 거짓**이었다. 실측 발사당 처치 0.183~0.409(2.4~5.5발/킬). **사거리 문제가 아니다** — 사거리 밖인 틱은 8~9%, 활성 적 0인 틱은 1.3~1.9%뿐이라 쿨다운은 거의 항상 흘렀다. 원인은 "쏘질 못해서"가 아니라 **"맞혀도 안 죽어서"**다. 판당 스폰 XP 총량 20.7~31.1 vs 8레벨업 필요 누적 99 / 12레벨업 271 — 성장 곡선을 손대도 화력이 풀리기 전엔 의미가 없다. **`grunt.HP`를 내리는 대안을 버린 이유**: 보스 700HP·이타카 108기 물량전에도 함께 유리한 쪽은 DAMAGE이고, 다른 수치 근거를 하나도 건드리지 않고 축 1개만 움직인다. 부작용은 서술만 갱신했다 — brute 6발 → **4발**, boss 117발 → **70발** |
| D28 | **키클롭스 클리어 불가(200판 0건) 해법으로 B1을 택한다 — `WEAPON_BOW.AIM`에 `'bossFirstThenNearest'`를 추가.** B2(보스 속도)·B3(goal 변경)·B4(사거리) 기각 | 이건 튜닝 문제가 아니다. `AIM='nearest'`인데 잡몹이 거의 항상 보스보다 가깝고 보스 속도 16은 플레이어 60에 계속 뒤처져, **보스 피해량 중앙값 0.0%**(최대 11.8%)다. `goal:1, wBossKill:1` 단독 조건이라 **화력을 얼마로 올려도 0%가 유지된다.** ⓐ B1은 **`EnemyRuntime.type`으로만 분기하는 전역 규칙 1개**라 §8-2(시련 id 분기 0개)를 깨지 않는다. ⓑ 연꽃·이타카는 `bossSpawnSec 999 > timeLimitSec 180`이라 보스가 원리적으로 안 나온다 → **3종 중 2종에서 증명 가능한 무동작**. ⓒ **다른 수치 근거를 하나도 건드리지 않는다.** B2는 D8(적 속도 상한 1.35×, "도주는 끝까지 가능")을 뒤집고, B3은 D1·D5의 "보스 = 체력 많은 적 1기"를 없애며, B4는 §1의 "사거리 140 = 화면 폭 29%"를 무너뜨리는 데다 **최근접 우선인 한 보스는 여전히 후순위라 문제를 안 푼다.** ⓓ 설명 가능한 규칙이다 — "활은 가장 큰 위협을 노린다"(구혼자들 사이에서 안티노오스를 먼저 쏘는 장면). 예외가 아니라 규칙이라 플레이어가 학습한다. **대가**: 보스가 사거리에 있는 동안 잡몹에 화살이 안 가므로 키클롭스의 동시 최대 적 p95(1회차 66)와 생존시간이 나빠질 수 있다 — 2회차-B에서 함께 본다 |
| D29 | **D27과 D28을 같은 측정 회차에 넣지 않는다. 순서는 A(DAMAGE) → B(AIM)이며, `AIM` 기본값은 2회차-A가 기록될 때까지 `'nearest'`로 둔다** | 화력 축은 **3시련 × 미달 지표 4개**에 걸리는 가장 넓은 축이고, 조준 규칙은 **1시련 × 1지표**에 걸리는 좁은 축이다. 넓은 축을 먼저 재야 기준선이 생긴다. 역순이면 A의 실효 DPS를 모르는 채로 보스 700HP의 타당성을 판정하게 되어 **보스 HP를 깜깜이로 튜닝**하게 된다. tech는 **두 값을 모두 구현**하므로 왕복 비용은 `AIM` 한 줄 플립 + 측정 시간뿐이다. **사전 등록**: 2회차-A에서 키클롭스가 0.000에 머무는 것은 정상이며 "DAMAGE가 부족했다"로 읽지 않는다. 2회차-B의 성공 판정 지표는 클리어율이 아니라 **보스 피해량 비율 중앙값**이다 |
| D30 | **2회차-B는 D18의 "밸런스 왕복 2회" 카운트에 넣지 않는다(결함 수정으로 분류). 다만 수치 동결 시점은 2회차-B 직후로 고정한다** | 클리어율 0.000은 "덜 맞춰졌다"가 아니라 **"그 시련을 끝낼 방법이 존재하지 않는다"**이다. 튜닝 예산으로 결함을 갚게 하면 "예산이 없으니 못 고치는 시련을 그대로 낸다"가 되고, 그건 §8-1(3종 각각 클리어 화면 도달)을 못 채워 제출물 자체가 흔들린다. 대신 **동결 시점을 앞당겨** 예산 총량은 지킨다 — 2회차-B 이후엔 측정만 하고 남은 시간은 제출물 4종에 쓴다. **예외**: B가 보스 피해량을 올렸는데도 클리어율 0.35 미만이면 `ENEMIES.boss.HP` **단 하나**를 측정된 보스 조준 유지율에서 산출해 조정하고 끝낸다(탐색 금지) |
| D31 | **`BOT.THREAT_RADIUS_RATIO`(= 1/3, 감지 반경 46.7px)를 `balance.ts`로 올리고 GDD §7-A에 명문화. 값은 그대로 둔다** | tech가 문서에 없어서 임의로 정한 값이었고 `bot.ts`에 리터럴로 박혀 있었다(하네스 2 위반). 규칙으로 말하면 **"쏘는 거리와 피하는 거리를 분리한다"** — 사거리 140을 그대로 쓰면 봇이 항상 도주 모드가 되어 오브 회수를 영구 포기하고(레벨업 중앙값 0), 그 판은 **이 게임을 측정한 게 아니다.** 값을 지금 안 바꾸는 이유: 이번 회차의 축은 DAMAGE 하나이고, 봇 정책을 동시에 흔들면 D10(재현성)이 깨져 1회차와 비교가 불가능해진다. **이건 밸런스가 아니라 측정 도구다** — 바꾸려면 별도 회차로 명시하고 전 지표를 재측정한다 |
| D32 | **`TARGET_METRICS.EARLY_DEATH_SEC = 30` 신설. 합격 구간은 하나도 바꾸지 않았다** | `metrics.ts`에 리터럴 `30`이 박혀 하네스 2를 어기고 있었다. **위치만 고치고 값은 30 그대로**다 — 이 지표는 1회차에 3종 전부 합격이라 값이 틀렸다는 근거가 없다. 30 = 3스텝(`SPAWN.STEP_SEC` 10 × 3) = 목표 판 길이 90초의 1/3 |
| D33 | **문서에 없어서 tech가 임의로 정했던 3건을 GDD로 올린다**: ⓐ `HIDE_ARMOR`는 최대HP 증가분만큼 현재HP도 회복(§3-B-1) ⓑ 3택 대기 중 게임 시간 정지(§3-B-2) ⓒ 3택 화면 규격(§9-5-A, `HUD.CHOICE_CARD` / `CHOICE_DIM_ALPHA`) | 명시가 없으면 **다음 세션에서 또 임의로 결정되고, 그때 값이 달라지면 원인 불명의 밸런스 변동이 된다.** ⓐ의 근거: 전투 중에 최대HP만 오르면 그 순간 아무 일도 안 일어나 생존 카테고리가 화력과 경쟁하지 못한다. ⓑ의 근거는 **"정지는 sim이 아는 것만 허용한다"** — 3택 정지는 sim 내부 상태(`pendingChoice`)라 헤드리스와 브라우저의 경과 시간이 같지만, 시련 시작 배너는 렌더 전용이라 멈추면 §6 측정이 무효가 된다(D21·§9-5의 연장). ⓒ는 배지 3색이 서로 다르다는 것 자체가 D6("3택은 성격이 다르다")를 화면에서 증명하는 장치이며, **입력은 클릭 1종**으로 못박아 §2 조작 축 상한과 QA 경우의 수를 지킨다 |
| D34 | **`XP_CURVE.GROWTH` 주석의 "레벨13까지 213 XP"는 off-by-one이었다. 실계산은 8레벨업 99 / 11레벨업 213 / 12레벨업 271. 서술만 고치고 값은 안 바꿨다** | 하네스 17(이상한 걸 발견하면 말한다). 213은 11레벨업 지점이다. 이 오차 때문에 "판당 12레벨" 근거가 실제보다 22% 낙관적이었다. **곡선을 지금 손대지 않는 이유**: 1회차 스폰 XP 총량이 20.7~31.1로 필요량의 1/10도 안 되므로 **화력 축이 풀리기 전에는 곡선을 어떻게 바꿔도 레벨업이 안 뜬다.** 2회차-A 이후에 판단한다 |

## 2026-08-08 (gd, 타이틀 화면 치장 — 밸런스 축 무변경)

> **§6 목표 지표·`TARGET_METRICS`·무기·적·스폰·XP·시련·봇 수치를 한 글자도 건드리지 않았다.**
> 아래는 전부 렌더 축이며 sim이 읽지 않는다 → playtest 재측정 대상이 아니다.

| # | 결정 | 이유 |
|---|---|---|
| D35 | **에셋 상한을 「생성 5장 + 파생 2장」으로 쪼갠다.** `ASSETS.MAX_IMAGE_FILES`는 **5 그대로 두고**(=외부 도구로 새로 만든 것만 센다), `ASSETS.MAX_DERIVED_IMAGE_FILES: 2`를 신설한다. 첫 파생은 `sprites/title-hero.png`(GDD §9-8) | **6으로 올리는 안을 기각한 이유**: 상한 5의 근거는 *"이 5장을 넘기면 사용자 생성 시간이 그대로 개발 시간에서 빠진다"*(D18)인데, 파생은 그 시간을 **0** 쓴다. 6으로 올리면 숫자는 맞지만 **근거와 숫자가 어긋난 채로 남고**, 다음에 파생이 하나 더 생기면 또 올리게 된다 — 그건 상한이 아니다. **그렇다고 무제한으로 두지 않는 이유**: 파생의 실제 비용은 생성 시간이 아니라 **tech의 로드 키 1개 + 폴백 분기 1개**이고, 이건 용량 상한(`MAX_IMAGE_TOTAL_KB` 800)으로 **통제되지 않는다** — 34KB짜리 파생 10장을 만들어도 340KB라 안 걸린다. 즉 "용량으로 통제한다"는 실효가 없다. 2 = 현재 1장 + 마감 전 급한 파생 1장 여유이며, 3번째가 필요해지는 순간 근거를 새로 쓰게 만든다. **용량은 출처와 무관하게 계속 합산한다** — 로딩 시간에는 바이트만 남는다. 추적은 `ASSET_CREDITS.md` **§1-D**(원본을 가리키는 행 1개, 출처는 원본 행에서 상속)로 하고, §1 표에 행을 복제하지 않는다 — 같은 출처를 두 벌 적으면 한쪽만 고쳐졌을 때 D26-ⓐ와 같은 사고가 난다 |
| D36 | **타이틀 화면 규격을 GDD §9-8로 확정하고 좌표·암막을 `HUD.TITLE`에 넣는다.** 배경 = `arena.jpg` 1:1 중앙 정렬 + 검정 암막 `DIM_ALPHA` **0.75**, 중앙에 128px 히어로. **틴트는 걸지 않는다** | ⓐ 이 화면은 심사자의 **첫 프레임**이자 제출 영상의 첫 프레임이고, 손보는 비용이 **새 에셋 0개**(기존 arena + player 파생 1장)라 지금이 가장 싸다. ⓑ **`CHOICE_DIM_ALPHA`(0.65) 재사용을 기각한 이유**: 0.65는 "3택 중에도 적 실루엣이 남아야 한다"는 **하한**으로 정한 값이라 타이틀에 그대로 쓰면 **다른 목적의 값을 우연히 같은 숫자로 공유**하게 되고, 나중에 3택 쪽을 튜닝하면 타이틀이 같이 흔들린다. 0.75는 배경 밝기를 원본의 25%로 내려 질감은 남기고 텍스트 대비를 확보하는 값이며, **1.0(완전 검정)이면 현행 화면과 구분이 사라져 작업 의미가 없다.** 결과적으로 암막이 게임 0 / 3택 0.65 / 타이틀 0.75로 **세 화면의 밝기가 서로 다르다.** ⓒ **틴트를 안 거는 이유**: `bgTint`는 시련의 필드이고 타이틀에는 시련이 없다. 타이틀 틴트를 고르는 순간 "어느 시련 색인가"라는 결정이 생겨 §8-2(시련 id 분기 0개)로 가는 길이 열린다. ⓓ **좌표를 전부 상수로 올린 이유**: 히어로가 없어도 나머지 요소가 한 픽셀도 안 움직여야 폴백(§8-20)이 "정상 동작"으로 판정된다. 에셋 유무로 배치가 바뀌면 그건 폴백이 아니라 **두 번째 레이아웃**이고 QA 경우의 수가 2배가 된다. ⓔ 대가: 타이틀에 **새 입력·연출·전용 BGM·로고 이미지를 만들지 않는다**(§9-8 「안 만들 것」) |

## 2026-08-08 (gd, playtest 2회차 반려 대응)

> 2회차-A(DAMAGE 6→10) 실측: 레벨업 중앙값 **4 / 3 / 4**(합격 8~14).
> 2회차-B(AIM 플립) 실측: 키클롭스 보스 피해 중앙값 11.8% → **29.1%**, 클리어율 **0.065**(합격 0.35~0.70).
> **§6 목표 지표(D11)·`TARGET_METRICS`는 이번에도 한 줄도 바꾸지 않았다.**

| # | 결정 | 이유 |
|---|---|---|
| D37 | **D18의 「밸런스 튜닝 왕복 2회 제한」을 3회로 개정한다.** 늘린 1회의 용처를 미리 못박는다 — **3회차 = `XP_CURVE.GROWTH`, 4회차 = `ENEMIES.boss.HP`. ~~동결은 4회차 직후, 5회차는 없다.~~** ⚠️ **「5회차 없음」 부분은 D44로 개정됨(→ 4회, 동결은 5회차 직후). 사유는 시간이 아니라 `boss.HP`가 연꽃에 영향 0이라는 적용 범위의 구멍이다. 나머지는 유효** D18의 에셋 상한(이미지 5 + BGM 1 / 4000KB)은 그대로 유효하다 | **전제가 깨졌기 때문이다.** D18의 근거는 *"에셋을 스코프에 넣는 대가로 시간이 모자랄 것"*이었고, 예산의 단위는 **시간**이었다. 그런데 렌더 축 구현에 4시간 30분을 배정했으나 `tech`가 **38분**에 끝냈다 — 근거가 시간이었으므로 시간이 남으면 예산도 남는다. 이건 "하고 싶어서 늘린다"가 아니라 **원래 근거의 입력값이 실측으로 바뀐 것**이다. ⓑ 동시에 **늘려야 하는 이유**도 있다: 레벨업 중앙값 3~5는 단순 수치 미달이 아니라 **D6이 정의한 이 게임의 유일한 플레이어 결정(3택)이 판당 3~5번밖에 안 뜬다**는 뜻이다. 측정 없이 이 상태로 동결하면 하네스 18에 걸린다. ⓒ **그럼에도 3에서 멈추는 이유**: 상한이 "필요할 때마다 늘어나는 수"가 되면 상한이 아니다. 그래서 **용처를 미리 지정**해 4회차 이후 탐색의 여지를 남기지 않았다(D30의 "탐색 금지"와 같은 장치). 하네스 7("늘리려면 뭘 버릴지 같이 제시")에 대해서는 **버릴 것이 이미 지불돼 있다** — 3·4회차에 손댈 수치를 각각 1개로 못박은 것이 그 대가다 |
| D38 | **`XP_CURVE.GROWTH` 1.25 → 1.10. 3회차에 바꾸는 유일한 수치다.** 동시에 **`BOT.THREAT_RADIUS_RATIO`와 `PLAYER.PICKUP_RADIUS`를 조사 대상에서 제외**하고 GDD §6-A·§7-A의 해당 서술을 실측으로 갱신한다 | **1회차의 진단(회수 손실 45~61%가 병목)이 2회차 실측으로 뒤집혔다.** 판당 스폰 XP 총량 중앙값이 90.5 / **52.0** / 77.5인데, **회수 효율을 100%로 가정해도** 레벨업이 7 / **5** / 7에 그쳐 3종 전부 구 곡선의 8레벨업 요구치 **99**에 못 닿는다(연꽃은 52 vs 99로 47% 부족). 즉 병목은 **회수가 아니라 공급**이고, **봇을 고쳐도 안 풀린다** → 두 반경 수치는 조사에서 빠진다. 부수 이득으로 D10(봇 정책 = 재현성)을 안 건드려 1·2회차와 계속 비교 가능하고, `PICKUP_RADIUS` 18의 근거("회수를 위해 적 쪽으로 들어가는 판단이 남을 만큼만")도 지킨다. **왜 GROWTH인가**: ⓐ `ENEMIES.*.XP`는 시련마다 적 구성비가 달라 3종에 고르게 안 퍼진다 — `grunt.XP` 1→2는 이타카(grunt 100%)엔 거의 배지만 연꽃(brute 70%)엔 +12.5%뿐이라 "한 축 바꿔 3종 다 잰다"가 깨진다. ⓑ `XP_CURVE.BASE`에는 *"레벨 2까지 5초 안에 첫 3택"*이라는 **이미 검증된 초반 페이싱 근거**가 붙어 있어 건드리면 그 근거까지 재검증해야 한다. GROWTH는 후반 요구량 증가율만 움직여 그 근거를 보존한다. **수치 근거(BASE=5 고정)**: 8레벨업 누적 **99 → 58**(41% 감소), 14레벨업 435 → 141, cum14/cum8 4.39배 → 2.43배. 단조증가라 "판이 갈수록 어려워진다"는 성격은 유지된다. **주의 2건을 주석에 남겼다**: ⓐ **양의 되먹임**(레벨업이 빨라지면 봇 1순위 `TWIN_SHOT`이 빨리 발동 → 킬레이트 ↑ → XP 공급 ↑)이 있어 실측이 정적 계산보다 높게 나올 수 있다 — **이건 추정이지 측정이 아니다.** 그래서 1.05 이하로 내리지 않았다(초반이 후반 스폰 압력을 압도하면 클리어율·동시 최대 적 p95가 흔들린다). **1.10은 보수적 시작값**이다. ⓑ **연꽃이 가장 위험하다** — 공급 52가 새 요구치 58에도 근소하게 못 미친다. 이것만으로 안 들어오면 다음 축은 `ENEMIES.brute.XP`이며 **이번엔 GROWTH만 단독 검증한다** |
| D39 | **`WEAPON_BOW.AIM` 기본값을 `'bossFirstThenNearest'`로 확정한다(잠정 해제).** `ENEMIES.boss.HP`는 **이번 회차에 바꾸지 않고** 4회차로 넘기며, 조정치는 `HP_new = round(700 × m)`(m = 3회차 측정 보스 피해량 중앙값 비율)로 산출한다 | **playtest는 "boss.HP와 묶어 다음 회차에 함께 확정"을 권했으나 그 권고를 따르지 않았다. 이유는 기준선 위생이다.** 여기를 `'nearest'`로 되돌리면 3회차(GROWTH 단독)의 기준선이 2회차-**A**가 되고, 그 다음 회차에 `AIM`과 `boss.HP`를 **동시에** 뒤집게 된다 — **2축이다.** 지금 확정하면 3회차의 기준선이 2회차-**B**가 되어 바뀐 축이 **GROWTH 하나**로 유지된다. 즉 확정하는 쪽이 "한 번에 한 축"을 지킨다. 확정은 `boss.HP` 조정을 막지 않는다 — **순서만 정한다.** ⓑ 되돌릴 근거도 없다: 보스 HP 피해 중앙값 11.8%→**29.1%**, 처치 6/200→**13/200**으로 방향·크기 모두 뚜렷하고, 연꽃·이타카는 **8개 지표가 바이트 단위로 동일**해 D28-ⓑ("3종 중 2종에서 증명 가능한 무동작")가 실측으로 성립했다 — **2종에서 확정 비용이 정확히 0이다.** ⓒ **대가는 실측됐고 되돌리지 않는다**: 키클롭스 레벨업 5→4, 초당 처치 0.846→0.772(화살이 보스에 묶여 잡몹 처치·XP 회수가 준다). §3-A가 "감시할 것"으로 미리 지목한 항목이 그대로 나타났다. 동시 최대 적 p95 49→54는 합격(≤90) 안이다. **이 값들은 이제 3회차의 기준선이다.** ⓓ **boss.HP를 3회차에 같이 안 바꾸는 이유**: GROWTH는 3종 전부에, boss.HP는 1종에만 걸려 섞으면 효과 분리가 불가능하다. ⓔ **4회차 산식이 `round(700 × m)`인 이유**: 중앙값 판이 정확히 보스를 잡는 지점 → 기대 클리어율 ≈ 0.50 = 합격 구간 [0.35, 0.70]의 **정중앙**이다. 정중앙을 노리면 양방향 오차 여유가 같아진다(레벨업 상승에 따른 화력 되먹임은 위로, 보스 처치가 빨라져 잡몹이 남는 효과는 아래로 민다). m은 감이 아니라 실측값이며 **3회차 측정 전에는 이 식에 숫자를 넣지 않는다** |

## 2026-08-08 (gd, playtest 3회차 반려 대응 — 4회차 적용 + 5회차 신설)

> 3회차(`XP_CURVE.GROWTH` 1.25→1.10) 실측 후 상태. **§6 목표 지표(D11)·`TARGET_METRICS`는
> 이번에도 한 줄도 바꾸지 않았다.** 대신 못 맞춘 것을 §6-B「확정 미달」로 **명시**했다 — 기준을 낮춘 것과
> 기준에 못 미친 채로 낸다고 적는 것은 다르다.

| # | 결정 | 이유 |
|---|---|---|
| D43 | **`ENEMIES.boss.HP` 700 → 324. 4회차에 바꾸는 유일한 수치다** | D39가 **사전 등록한** 산식 `HP_new = round(700 × m)`을 그대로 적용했다. m = 3회차 실측 키클롭스 보스 HP 피해 비율 **중앙값 0.462513** → `round(323.76)` = **324**. **탐색하지 않았다**(D30). ⓐ **산식 타당성(playtest 조건부 타당 판정)**: 보스 처치 42/200 < 100이라 **중앙값의 위치가 158개의 미처치(=1.0에 캡핑되지 않은) 값 안**에 있어 캡핑이 중앙값을 왜곡하지 않는다. 그리고 누적 피해량은 시간에 단조증가하고 목표 HP 총량과 무관하므로 "HP를 median-damage로 내리면 지금의 median 판이 정확히 처치선에 걸린다"가 **중앙값의 정의상** 성립한다 → 기대 클리어율 ≈ 0.50 = [0.35, 0.70]의 정중앙. ⓑ **입력값을 전부 남긴다**(200회·시드 고정·봇 정책 BOT): 피해 중앙값 46.2513% / 최댓값 100.0% / 처치 **42-200**(2회차-B 13/200의 3.2배 — GROWTH 인하의 되먹임) / 보스 조준 유지율 중앙값 35.7%·평균 38.7%. ⓒ **사전 등록한 단서**: 보스가 일찍 죽으면 남은 시간의 화살이 `AIM='bossFirstThenNearest'`에 따라 **잡몹으로 재분배**돼 킬레이트·레벨업이 오르고 클리어율이 0.50보다 **위로** 밀릴 수 있다. 상한까지 여유 0.20이라 구간 안에 남을 여지가 있으나, **넘어도 다시 조정하지 않는다** — 5회차 용처는 연꽃 하나로 못박혀 있다. ⓓ 연꽃·이타카에는 **영향이 0**이다(`bossSpawnSec 999 > timeLimitSec 180` → `spawnBossIfDue` 미실행) |
| D44 | **D37의 「동결은 4회차 직후, 5회차는 없다」를 개정한다. 5회차 1회를 신설하고 용처를 `TRIALS[lotophagoi].enemyHpMul` 하나로 못박는다. 동결은 5회차 직후, 6회차는 없다** | **개정 사유는 시간이 아니라 적용 범위의 구멍이다.** D37은 4회차를 `boss.HP`로 지정하면서 그 값이 **연꽃에 수학적으로 영향이 0**이라는 사실을 계산에 넣지 않았다. 그 결과 계획대로 동결하면 **클리어율 0.100 / 레벨업 3 / 초당 처치 0.605로 3지표 미달인 시련을 한 번도 안 건드린 채 제출**하게 된다. 0.100은 하한 0.35의 1/3도 안 되는 값이라 **지표 미달이 아니라 "사실상 이길 수 없는 스테이지"**이고, 심사자가 목록 2번을 고르면 10판 중 9판을 진다 — §8-1(3종 각각 클리어 화면 도달)이 실질적으로 무너진다. **하네스 7의 대가 4건을 함께 지불한다**: ⓐ **이타카의 레벨업 미달(6, 합격 8~14)을 「확정 미달」로 확정**한다(§6-B) — 5회차 축이 연꽃 로컬이라 이타카는 증명 가능한 무동작이고 **고칠 회차가 영원히 없다.** 이걸 고르는 근거는 이타카가 **클리어율·초당 처치 합격**이라 코어 루프가 정상 작동한다는 것이다(= 심사자가 켜면 끝까지 플레이되고 이길 수 있다). 반면 연꽃을 그대로 두는 비용은 그것과 비교가 안 된다. ⓑ **후보 3개를 지금 영구 기각**한다 — `PLAYER.PICKUP_RADIUS` / `ENEMIES.brute.XP` / `TRIALS[lotophagoi].wKill`. 5회차가 실패해도 도망갈 축을 미리 없앤다. ⓒ **6회차는 결과와 무관하게 없다.** 미달이면 §6-B에 연꽃 행을 추가하고 그대로 낸다 — **동결은 조건부가 아니다.** ⓓ 5회차에 **새로 판정하는 시련은 연꽃 1종뿐**이고 나머지 2종은 「동일함」 검증용이다. **왜 4에서 또 멈추는가**: 상한이 "필요할 때마다 늘어나는 수"면 상한이 아니다(D37·D41과 같은 장치). 그래서 축·값·동결 시점·기각 후보를 **측정 전에** 전부 못박았다 |
| D45 | **5회차 축 = `TRIALS[lotophagoi].enemyHpMul` 1.15 → 1.00. 단 하나다.** 동시에 **D38이 닫았던 회수 경로가 되살아났다는 사실을 GDD §7-B에 기록하되, 그 축(`PICKUP_RADIUS`)은 고르지 않는다** | **ⓐ D38의 제외 근거는 만료됐다. 그 사실을 지우지 않는다.** D38은 *"회수 100%를 가정해도 미달"*이라고 썼는데 그건 **구 곡선(8레벨업 요구 99)** 기준이었다. 새 곡선(요구 **58**)에서 연꽃은 스폰 XP **57.0** / 회수 XP **21.5** / 회수 효율 **35.7%**(3종 최저) / 런 종료 잔량 **65.8%**(3종 최고)다. 회수 21.5가 누적 L3(17)~L4(24)에 걸리고 **실측 레벨업 중앙값 3과 일치**하며, 독립 계측 2개가 같은 결론으로 수렴했다 → **회수는 다시 실재하는 병목이다.** 지우면 다음 세션이 §7-A만 읽고 같은 경로를 또 닫는다. **ⓑ 그런데도 `PICKUP_RADIUS`를 안 고르는 이유는 셋이다.** ① **단독으로 산술적으로 부족하다** — 공급 57.0 < 요구 58이라 **회수 효율 100%도 7레벨업**이다. 병목이지만 그것만 풀어선 하한을 못 넘는다. ② **전역 축이라 다 맞은 지표를 깬다** — 4회차에 키클롭스를 0.50에 맞춘 **직후** 전역 XP 회수를 올리면 키클롭스(상한까지 여유 0.20)와 이타카(클리어율 이미 합격)가 0.70 밖으로 밀릴 수 있다. **1종을 고치려 2종의 합격 지표를 거는 거래**이고, 되잡으려면 6회차가 필요해져 **동결이 불가능해진다**(GDD §6-A-1 표). ③ §1의 근거("회수를 위해 적 쪽으로 들어가는 판단이 남을 만큼만")를 훼손한다 — 이 값에는 상한이 있다(`BOT.THREAT_RADIUS_RATIO × RANGE` = 46.7 이상이면 그 판단이 완전히 소멸). **ⓒ 왜 `enemyHpMul`인가.** 연꽃의 세 미달은 **하나의 잠긴 고리**다: 킬레이트↓ → XP 공급·회수↓ → 레벨업↓ → 화력↓ → 킬레이트↓. 연꽃은 `spawnTotalCap` 무제한이라 **화력 1점이 그대로 킬·XP로 환산되는 유일한 시련**이므로 고리를 끊으면 셋이 같은 방향으로 움직인다. 이 필드는 고리의 분모(처치당 필요 발수)를 **직접** 줄이며, 회수 개선은 "적이 덜 쌓여 봇이 덜 도망친다"로 **부수적으로** 따라온다 — 즉 **증명되지 않은 인과 가설(brute 비중 ↔ 회수 효율의 상관)에 기대지 않는다.** **ⓓ 동시에 결함 수정이다(하네스 17).** `computeEnemyHpMul = enemyHpMul × (1 + 0.12×step)`이라 연꽃 스텝0의 grunt 실효 HP는 `10 × 1.15 = 11.5 > WEAPON_BOW.DAMAGE 10`이다 — **D27이 확정한 "grunt를 1발에 잡는다"는 이 게임의 기준 단위가 연꽃에서만 처음부터 깨져 있었다.** 1.00은 탐색값이 아니라 그 기준 단위를 3종에 일관되게 되돌리는 **원칙값**이며, 그래서 "1.05는? 0.95는?"이라는 탐색이 시작되지 않는다. **ⓔ 정체성을 안 잃는 근거**: 1.00에서도 연꽃의 마리당 평균 HP는 `0.3×10 + 0.7×34` = **26.8**로 키클롭스(`0.7×10 + 0.3×34` = 17.2)의 **1.56배**, 이타카(`1.0×10 × 0.9` = 9.0)의 **2.98배**다. "단단함"의 원천은 이 배율이 아니라 **`brute.HP` 34 + 구성비 70%**이고 둘 다 안 건드린다. "느림"·"다수"도 그대로다. **ⓕ 기각한 나머지**: `wKill` 0→양수는 **클리어 조건만 바꿔 클리어율 1개를 올릴 뿐** 레벨업·초당 처치를 하나도 안 건드리고, 이타카(wKill 시련)와 성격이 겹쳐 §5의 "시간/처치/보스" 3분할을 무너뜨린다 — **플레이를 안 고치고 골대를 옮기는 것**이라 §6-B에 미달로 적는 편이 정직하다. `ENEMIES.brute.XP`는 공급만 건드려(회수 35.7%를 곱하면 5레벨업) 하한에 못 닿고, brute가 키클롭스 30%에도 있어 **전역 축의 문제(ⓑ②)를 그대로 갖는다**. **ⓖ 사전 등록된 기대치(측정 후 갖다 붙이지 않기 위해 미리 적는다)**: 처치당 평균 발수 3.4~5.5 → 3.1~4.8(-10~-17%)이므로 **직접 효과만으로는 초당 처치 0.605 → 약 0.70이며 합격 하한 1.0에 못 미친다.** 하한을 넘기는 것은 되먹임의 몫이고 **그건 추정이지 측정이 아니다.** 이 예상이 빗나가는 것 자체는 정상이며, 그때 할 일은 6회차가 아니라 §6-B 기재다 |
| D46 | **4회차와 5회차를 같은 측정에 섞지 않는다. 순서는 `boss.HP`(4) → `enemyHpMul`(5)** | 두 변경은 **서로소인 시련 집합**에 걸린다 — `boss.HP`는 연꽃·이타카에서 무동작(`spawnBossIfDue` 미실행), `enemyHpMul`은 키클롭스·이타카에서 무동작(다른 데이터 행). 그래서 순서만 지키면 **효과 분리가 구조적으로 보장**되고, 이건 D28-ⓑ·D39-ⓑ에서 이미 한 번 실증된 방식이다(2회차-B에서 연꽃·이타카 8지표가 바이트 단위로 동일했다). **`boss.HP`를 먼저 두는 이유**: 산식 입력 m = 0.462513이 **3회차 상태에서 측정된 값**이고 이미 계산이 끝나 있다. 연꽃 축을 먼저 넣어도 키클롭스는 무동작이라 m은 살아남지만, **끝난 계산을 미루면 4·5회차 사이에 판단할 것이 두 개가 된다.** 순서를 정하는 목적이 정확히 그거다. **완료 기준을 함께 신설했다(§8-30-b)**: 4회차 측정의 연꽃·이타카가 3회차와, 5회차 측정의 키클롭스·이타카가 4회차와 각각 동일해야 한다. 다르면 밸런스가 아니라 **「서로소 집합」 가정이 깨진 것**이므로 수치를 만지기 전에 원인을 먼저 보고한다 |

## 2026-08-08 (gd, 4회차 실측 확인 + 5회차 적용)

> 4회차 실측: 키클롭스 클리어율 0.210 → **0.490**(사전 등록 목표 **0.50**), 클리어 시각 67.7s → **54.975s**,
> 합격 지표 5/8 → **6/8**. **연꽃·이타카는 8지표 전부 바이트 단위 불변.**
> **§6 목표 지표(D11)·`TARGET_METRICS`는 이번에도 한 줄도 바꾸지 않았다.**

| # | 결정 | 이유 |
|---|---|---|
| D47 | **D43의 산식이 실측으로 검증됐다고 기록한다(GDD §6-A-0). 클리어율은 [0.35, 0.70] 안이므로 `ENEMIES.boss.HP` 324를 재조정하지 않는다** | **이 항목의 가치는 수치가 아니라 방법론의 물증이라는 데 있다.** 산식(`round(700 × m)`)과 목표값(0.50)을 **측정 전에** D39·D43에 등록했고 측정은 그 뒤에 했다 → 실측 **0.490**, 오차 0.010. **사후 조정(fitting)이 아니라는 것이 커밋 순서로 증명된다** — 이게 제출물 4번(AI 활용 기술 문서)에서 "감으로 밸런스를 맞추지 않았다"를 뒷받침하는 유일한 종류의 증거다. ⓐ **사전 등록한 단서는 발현하지 않았다**: "보스 조기 처치 → 화살 재분배 → 클리어율이 0.50 **위로** 밀릴 수 있다"고 적었으나 실측은 **아래쪽**(0.490)이었다 → 되먹임이 예상보다 작았다. **빗나간 예측도 지우지 않고 남긴다.** ⓑ **재조정하지 않는 이유**: 구간 안이고, D44-ⓒ가 "넘어도 다시 조정하지 않는다"를 이미 못박았다. 구간 안인데 조정하면 그건 **합격선이 아니라 목표점 0.50을 쫓는 것**이고, 그 순간 튜닝이 끝나지 않는다. ⓒ **무동작 2차 실증**: 연꽃·이타카 8지표 바이트 단위 불변으로 §6-A-1의 「서로소 시련 집합」 가정과 `bossSpawnSec 999` → `spawnBossIfDue` 미실행 예고가 확인됐다(§8-30-b 합격). 2회차-B에 이은 **두 번째**다 |
| D48 | **키클롭스의 남은 미달 2개(판당 레벨업 중앙값 **5** / 초당 처치 **0.769**)를 §6-B 「확정 미달」로 확정한다. 이대로 제출한다** | **지금 판단하지 않으면 미달인 채로 문서에 안 남는다** — 5회차는 연꽃 로컬이라 키클롭스에 무동작이고 6회차는 없다(D44). ⓐ **둘 다 이 시련의 설계가 직접 만든 값이지 튜닝 부족이 아니다.** `spawnIntervalMul` **1.25**는 §5·D1이 *"보스에 화력을 쏟게 하려고 잡몹 압력을 25% 낮춘다"*고 **의도적으로** 정한 값이다 — 잡몹이 25% 적게 나오는 시련의 초당 처치와 XP 공급이 낮은 것은 결함이 아니라 그 설계의 산술적 귀결이다. ⓑ **D39가 「대가」로 미리 지목하고 실측한 항목이 정확히 이 둘이다**(`AIM` 플립으로 레벨업 5→4, 초당 처치 0.846→0.772). 즉 **예상 밖의 결과가 아니라 등록된 비용**이다. ⓒ **되돌리면 클리어율이 무너진다**: `spawnIntervalMul`을 낮추면 시련 정체성이 사라지고 방금 0.490에 맞춘 클리어율이 다시 흔들리며, `AIM`을 되돌리면 클리어율이 **0.065로 회귀**한다 — **이 2지표는 클리어율 0.490과 맞바꾼 것이고 그 거래는 이미 옳다고 판정됐다.** ⓓ **조준 규칙만의 탓이 아니라는 증거**: `AIM` 이전(2회차-A, `nearest`)에도 레벨업 5 / 초당 처치 0.846으로 **둘 다 이미 미달**이었다. ⓔ **합격선은 안 내린다.** §6 표는 1회차부터 한 글자도 안 바뀌었고 §8-30-c가 그걸 git diff로 강제한다 — §6-B는 "기준을 낮춘 표"가 아니라 **"기준에 못 미친 채로 낸다고 적은 표"**다 |
| D49 | **5회차 `TRIALS[lotophagoi].enemyHpMul` 1.15 → 1.00을 적용했다. D45에 사전 등록한 값 그대로이며 탐색하지 않았다** | 4회차 측정이 끝난 뒤에 적용했으므로 두 축이 섞이지 않는다(D46). 근거는 D45 전문이며 요지는 셋이다: ⓐ **원칙값이다** — `computeEnemyHpMul`(`src/sim/spawn.ts:22`) 때문에 연꽃 스텝0의 grunt 실효 HP가 `10 × 1.15 = 11.5 > WEAPON_BOW.DAMAGE 10`이라 **D27이 확정한 "grunt 1발 킬"이라는 기준 단위가 연꽃에서만 처음부터 깨져 있었다.** 1.00은 3종이 같은 기준 단위를 공유하게 되는 **유일한 값**이므로 탐색의 시작점이 아니라 **종점**이다. ⓑ **키클롭스·이타카에 무동작이다**(`TRIALS`의 다른 데이터 행). §8-30-b가 이걸 검증한다. ⓒ **사전 등록된 기대치**: 처치당 평균 발수 3.4~5.5 → 3.1~4.8이므로 **직접 효과만으로는 초당 처치 0.605 → 약 0.70이라 하한 1.0에 못 미친다.** 하한 돌파는 되먹임의 몫이고 **그건 추정이지 측정이 아니다.** **빗나가도 6회차는 없다** — 그때 할 일은 §6-B에 연꽃 행을 적는 것이다 |

## 2026-08-08 (gd, 렌더 해상도 분리 — 밸런스 축 무변경)

> 사용자가 **"캐릭터 화질이 너무 안 좋아 보인다"**고 지적했고 타당했다.
> **`WEAPON_BOW`·`ENEMIES`·`SPAWN`·`XP_CURVE`·`TRIALS`·`BOT`·`TARGET_METRICS`를 한 글자도 안 건드렸다.**
> `playtest` 4회차 진단이 진행 중이므로 이건 협상 대상이 아니다. 아래는 전부 렌더 축이며 sim이 읽지 않는다.

| # | 결정 | 이유 |
|---|---|---|
| D40 | **`SCREEN.SUPERSAMPLE` = 3 신설. 캔버스 백킹스토어 = 480R×270R, 카메라 줌 = R.** `SCREEN.WIDTH/HEIGHT` 480×270은 **"카메라 월드 뷰 크기"라는 뜻으로 재정의하고 값은 그대로 동결**한다. `VISUAL.SPRITE_PX`·`SHAPE_PX`·`HUD.*`는 전부 월드/논리 단위로 유지하고, 텍스처 파일 픽셀 수 = 표시 크기 × R을 빌드 스크립트가 곱한다(GDD §9-9) | **원인이 필터가 아니라 정보량이었다.** `index.html`에 `image-rendering: pixelated`가 이미 있으니 확대 방식 문제가 아니고, 백킹스토어가 480×270 고정이라 **grunt의 원본 텍스처가 14px**이다 — 2560px 생성물을 14px로 줄인 것을 3배로 늘린 것이라 **필터를 무엇으로 바꿔도 개선되지 않는다.** 진단은 **"논리 해상도와 렌더 해상도가 같은 값에 묶여 있었다"**이다. ⓐ **D14를 깨지 않는다**: D14가 동결한 근거는 화면 대비 비율(사거리 140 = 폭 29%, 보스 반경 14 = 높이 5%, 흡수 18, grunt 90마리 점유율 13.6%)인데 **그 비율이 참조하는 것은 카메라 월드 뷰지 물리 픽셀 수가 아니다.** 백킹 R배와 줌 R배가 상쇄돼 **월드 뷰는 480×270 불변**이므로 비율이 한 자리도 안 바뀐다. → **밸런스 재측정 대상 0개.** 이 논리를 GDD §9-9에 못박아 두지 않으면 다음 세션에서 "해상도를 왜 올렸냐"로 재논의된다 — **해상도(월드 뷰)는 안 올렸고 픽셀 밀도만 올렸다.** ⓑ **R=3인 이유**: R은 취향값이 아니라 **표시 정수배와 같아야 하는 값**이다. 사용자 화면(폭 약 1544px)에서 `fitCanvasToIntegerScale()`이 고른 정수배가 3이었고, 같게 두면 텍셀:물리픽셀 1:1이 된다. **2 기각** — 백킹 960×540은 같은 창에서 정수배 1이라 그림이 지금보다 **작아진다**(선명해지는 대신 작아지면 개선이 아니다). **4 기각** — 텍스처가 16배가 되는데 정수배 4는 폭 1920px 창부터라 **늘어난 용량이 화면에 도달하지 않고**, arena 원본이 2560×1440(ffprobe 실측)이라 R=4의 3840×2160은 **1.5배 업스케일**이 된다(R=3의 2880×1620은 1.125배라 사실상 원본 안이다). ⓒ **R을 런타임에 창 크기로 바꾸지 않는다**: 텍스처를 다시 로드해야 하고 §8-37(월드 뷰 불변) 판정이 불가능해진다. ⓓ **표시 크기 상수에 R을 곱해 적지 않는 이유**: 곱하는 순간 "화면의 13.6%" 같은 근거 문장이 전부 거짓이 되고, R을 바꿀 때마다 근거를 다시 써야 한다 |
| D41 | **`ASSETS.MAX_IMAGE_TOTAL_KB` 800 → 1200.** 동시에 **「배경 960×540 무손실 PNG 폴백」 조항(D25-ⓓ)을 폐기**하고, 용량 초과 시 되돌리는 순서를 **① arena JPEG 품질↓ → ② arena만 1920×1080(= 월드의 2배)으로 내리고 런타임 1.5배 허용** 둘로 확정한다. **스프라이트 4종과 title-hero는 어느 경우에도 R배를 유지한다** | R=3이면 이미지 픽셀 수가 **9배**다. R=1 실측 합계 **68.8KB**(스프라이트 ≈8 + arena 26.7 + title-hero 34.1)에 비관적으로 ×9를 적용하면 **619KB**이고, 800은 여유가 29%뿐이라 D25가 남겨 둔 **"화질이 나쁘면 JPEG 품질↑"** 경로(arena 파일이 배로 커질 수 있다)를 못 받는다. 1200 = 추정 최악치의 약 1.94배. ⓐ **상위 상한은 안 건드린다**: 1200 + 오디오 2000 = 3200 ≤ `MAX_TOTAL_KB` 4000 → "20Mbps 첫 로드 1.6초"라는 근거가 그대로 성립한다. 이게 800을 그냥 4000까지 열지 않고 1200에서 멈춘 이유다 — 상한이 "필요할 때마다 늘어나는 수"가 되면 상한이 아니다(D37과 같은 장치). ⓑ **무손실 PNG 조항을 폐기하는 이유**: 800의 원래 근거가 "960×540 무손실 PNG 720.8KB가 아직 들어간다"였는데, **2880×1620 무손실 PNG는 약 6MB라 어떤 상한에도 못 들어간다.** 근거가 성립하지 않는 조항을 남겨 두면 다음 세션에 그 경로를 시도하게 된다. ⓒ **스프라이트를 예외 없이 R배로 두는 이유**: 사용자 지적의 대상이 **캐릭터**이고, 스프라이트 4종 전체가 추정 60~80KB로 예산에서 차지하는 몫이 작다 — 용량을 아낄 자리는 arena(추정 150~250KB) 하나다. ⓓ ⚠️ **619KB는 계산이지 실측이 아니다(하네스 16).** 이 세션에 셸이 없어 파일 크기를 재지 못했다. tech가 R=3 산출물의 실제 바이트를 보고하면 그 값으로 1200을 재검토한다 — 실측이 619를 크게 밑돌면 다시 조인다 |
| D42 | **Phaser `pixelArt: true`(텍스처 NEAREST)는 유지한다.** 대신 **CSS 표시 배율이 1 미만일 때만 스무딩을 켠다**(`SCREEN.SMOOTH_ON_DOWNSCALE = true`). 그리고 **모든 텍스트는 논리 px × R로 「래스터화」**되어야 하며, 이는 tech가 브라우저 실물로 확인한다(§8-36) | ⓐ **NEAREST 유지**: R 도입 후 텍스처는 항상 **1 텍셀 = 1 물리 픽셀**로 그려지므로 **필터가 결과에 관여하지 않는다.** 유지하면 코드 도형·폴백·HUD의 픽셀 정렬이 지금과 같게 남아 변경 범위가 줄어든다. 근거 없이 끄면 바뀐 것이 두 축(밀도 + 필터)이 되어 실물에서 무엇이 효과였는지 못 가린다 — 한 번에 한 축. ⓑ **축소 시에만 스무딩**: `pixelated`의 목적은 "확대할 때 보간으로 흐려지는 것을 막는 것"이고 축소는 정반대다 — 보간이 없으면 3픽셀 중 2개를 버리는 점표본이라 한글 획(1px)과 90마리 실루엣이 불규칙하게 사라진다. **R 도입 전에는 축소가 원리적으로 없었다**(백킹 480×270은 어떤 창보다 작다). 백킹이 1440×810이 되면서 폭 1440px 미만 창에서 축소가 **처음으로 가능**해졌고, 그 경우 R배 슈퍼샘플은 오히려 **축소에 유리한 원본**이 된다. ⓒ **텍스트가 이 변경의 최대 위험**이다: Phaser가 폰트를 8px로 래스터화한 뒤 3배 확대하면 **개선이 정확히 0**(지금과 글자 하나까지 동일)이고, 24px로 래스터화하면 획이 살아난다. **둘은 구현이 다르고 코드만 봐서는 판정할 수 없다** — 그래서 완료 기준(§8-36)을 "획 끝의 곡선 유무로 구분한다"는 **눈으로 찍는 문장**으로 적었다. `HUD.FONT_MIN_PX` 8은 **논리 px**로 유지한다(값을 24로 바꾸면 §9-5 레이아웃 좌표가 전부 깨진다) |

## 2026-08-08 (gd, 표시 크기 개정 — 밸런스 축 무변경)

> 사용자가 **"인게임 캐릭터가 작다"**고 두 번 지적했다. D40(렌더 해상도 분리)은 효과가 실물로
> 확인됐다(타이틀·HUD 한글 선명). **남은 문제는 해상도가 아니라 크기였고, 그건 별개 축이다.**
> **`WEAPON_BOW`·`ENEMIES`(`RADIUS` 포함)·`SPAWN`·`XP_CURVE`·`TRIALS`·`BOT`·`TARGET_METRICS`를
> 한 글자도 안 건드렸다.** 밸런스는 5회차로 동결(D44)이며 히트박스 `RADIUS`도 밸런스다.

| # | 결정 | 이유 |
|---|---|---|
| D50 | **D15(표시 크기 결정)를 개정한다. `VISUAL.SPRITE_PX` 18/14/22/40 → **34/20/30/56**. 동시에 점유율 근거의 기준을 「`SPAWN.MAX_ALIVE` 90이 전부 화면」에서 **「화면당 실제 마릿수 54」**로 바꾸고, `SPAWN.MAX_ALIVE` 주석의 「화면당 약 22마리」도 폐기해 **두 주석이 같은 전제를 쓰게** 한다** | **무엇이 틀렸었나 — 산수가 아니라 전제다.** D15는 *"90마리 전부 화면에 들어와도 14²×90 = 13.6%. 16이면 17.8%라 배경을 덮는다"*로 grunt를 14에 묶었다. ⓐ **90은 아레나 960×540 전체의 상한**이고 카메라 뷰는 그 1/4이다. ⓑ 무엇보다 **실측 동시 최대 적 p95의 최댓값이 54**(§6-A, 키클롭스 2회차-B)라 **90은 5회차까지 한 번도 도달한 적이 없다** — 도달하지 않은 수를 "최악의 경우"로 잡으면 과대평가다. ⓒ 한편 `SPAWN.MAX_ALIVE` 주석은 같은 파일에서 *"화면당 약 22마리"*(면적비 균등분포)라고 적어 **전제가 서로 달랐다.** 22도 틀렸다 — `spawnPointOutsideView`(`src/sim/spawn.ts:46`)가 **카메라 뷰 경계 +24px**에 스폰하고 적이 플레이어로 수렴하므로 분포는 화면 쪽으로 치우친다(과소평가). **정직하게: "90마리 전부 화면 진입은 아레나 크기상 불가능"은 아니다** — 플레이어가 멈춰 있으면 원리적으로 가능하다. 90을 안 쓰는 이유는 불가능해서가 아니라 **관측된 적이 없어서**다. → 새 기준 **N = 54**(실측 p95 최댓값 **전부**가 화면 안이라는 보수 가정). **상한 규칙은 그대로 「적 사각 면적 합 ≤ 화면의 1/3」**이고 근거도 그대로 배경이다(시련 구분 수단이 `bgTint` 한 필드뿐, D17). N=54에서 키클롭스 22.9% / **연꽃 31.2%** / 이타카 16.7% — 옛 값은 같은 기준에서 8.2~16.6%로 **예산의 절반만 쓰고 있었다.** **각 값의 상한 근거**: player 34는 `PLAYER.PICKUP_RADIUS × 2 = 36`이 상한(넘으면 XP 오브가 몸 안에서 사라져 "근처면 먹는다"가 화면에서 거짓이 된다), brute 30은 **32면 연꽃 34.9%로 상한을 넘어서** 멈춘 값, grunt 20은 그 둘의 분모, boss 56은 **grunt 대비 배율 2.80(옛 2.86) 보존** — 보스는 고유 패턴이 0개(D5)라 위압감의 수단이 크기뿐이다. **D15에서 살아남는 것**: ① 표시 > 히트박스 지름(4.25/2.5/2.5/2.0배 — "안 닿았는데 맞았다"를 막는 장치), ② 크기만으로 3종 구분(1.50배 / 2.80배). **고쳐지는 것**: player 18 < brute 22였다 — 플레이어가 화면에서 가장 작은 축이었고, `DEPTH.PLAYER` 최상위는 가림만 막지 크기를 대신하지 못한다. **`HUD.TITLE.HERO_PX`(128)는 안 바꾼다** — 타이틀은 이미 선명하다는 평가였고, 이 값은 크기이자 레이아웃 앵커라 바꾸면 `HERO_Y`·`LIST_Y`가 함께 깨져 한 축이 세 축이 된다. **용량**: 텍스처 픽셀 총량 2.15배(102/60/90/168). 671KB / 상한 1200KB이고 arena·title-hero는 불변이라 안에 들어온다고 **추정**한다 — ⚠️ **계산이지 실측이 아니다(하네스 16).** 실측은 §8-39 |
