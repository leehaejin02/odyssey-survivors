# Odyssey Survivors — GDD

**한 줄**: 이동만으로 살아남는 뱀서라이크. 오디세우스가 세 개의 시련을 지나 이타카로 간다.
**수치의 원본은 `src/config/balance.ts`다.** 이 문서는 규칙과 합격선만 정의한다. 수치가 어긋나면 코드 쪽이 옳다.

## 1. 코어 루프

이동(WASD/방향키)만 조작 → 활이 사거리 내 최근접 적을 자동 사격 → 적 처치 시 경험치 오브 →
레벨업 시 **3택** → 시련 목표 달성(클리어) 또는 HP 0(사망).

- 카메라 월드 뷰 480×270, 아레나 960×540 유한. **무한 필드가 아니다** — 이동만 조작이므로 벽이 유일한 압박 장치.
  (렌더 픽셀 밀도는 이와 분리돼 있다 — `SCREEN.SUPERSAMPLE`, §9-9.)
- 카메라는 플레이어를 추적한다. 적은 화면 밖 24px에서 생성된다.

## 2. 스코프 상한 (여기를 넘으면 하루 안에 안 끝난다)

| 축 | 상한 | 이유 |
|---|---|---|
| 무기 종류 | **1종** (오디세우스의 활) | 무기 1종 추가 = 적 3종 × 시련 3종과 전부 조합. 곱으로 는다 |
| 적 종류 | **3종** (grunt / brute / boss) | boss 포함. 시련별 차이는 **"어느 종이 나오는가"**로만 낸다(§5-A. ~~조합 비율~~ → 6회차·D56에 전속 배치로 바뀌었다) |
| 시련 | **3종** | 아래 §5. 차이는 데이터 행 3개 |
| 3택 풀 | **9종** (화력4/생존3/유틸2) | 카테고리 3개면 3택이 항상 성격이 다르게 구성된다 |
| 조작 축 | **이동 1축** | 대시·스킬 버튼 없음 |
| **에셋(외부 생성)** | **이미지 5장 + BGM 1곡 = 총 6개** | 그 외 전부 코드 도형. 파일이 늘면 사용자 생성 시간이 그대로 개발 시간에서 빠진다. 목록·규격은 §9 |
| **에셋(파생)** | **이미지 2장까지** (`ASSETS.MAX_DERIVED_IMAGE_FILES`) | 파생 = 위 5장과 **같은 원본**에서 스크립트로 다시 뽑은 것. 생성 시간을 0 쓰므로 위 상한에 넣지 않는다(D35). 대신 별도 상한을 두는 이유는 파생 1장의 실제 비용이 **로드 키 1 + 폴백 분기 1**이고 용량 상한으로는 안 걸리기 때문이다. 현재 1/2: `sprites/title-hero.png` |

### WON'T (하네스 9의 원본. 이 목록이 최종)
스킬 진화 트리 / 상점 / 런 간 영구 강화 / 세이브 / 캐릭터 커스터마이즈 / 멀티플레이·서버 /
컷신·대사·NPC / 모바일 대응 / 시련 4종 이상 / 이동 외 새 조작 축 / 보스 고유 패턴 /
적 원거리 공격·투사체 / 지형·장애물 / 플레이어를 끌어당기는 힘.

**렌더 축에서 추가로 배제한 것 (2026-08-08, D13~D18과 함께 확정):**
스프라이트시트 애니메이션(걷기·공격 프레임) / 패럴럭스·스크롤 배경 / 엔딩 컷신 /
효과음 개별 제작(피격·발사·처치) / **시련별 고유 배경 이미지**(§9-3의 틴트로 대체) /
웹폰트·폰트 파일 도입(시스템 sans-serif만) / 3택 아이콘 PNG 9장(§9-4의 코드 배지로 대체) /
BGM 2곡 이상 / 파티클 시스템 / 화살·경험치 오브 이미지 / 로딩 화면 연출.
(피격 시 색 플래시 1종은 코드 구현이라 허용 — `VISUAL.HIT_FLASH_MS`)

**렌더 해상도 축에서 추가로 배제한 것 (2026-08-08, §9-9·D40~D42):**
해상도/품질 설정 화면·옵션 토글 / dpr별 다중 에셋 세트(1x·2x·3x) / 스프라이트시트·아틀라스 /
`SUPERSAMPLE`을 창 크기에 따라 **런타임에** 바꾸는 것 / 이번 축을 위한 새 에셋 생성(생성 5 + 파생 1 그대로).

## 3. 무기와 성장

무기는 활 1종. 발사 주기·데미지·투사체 수·관통·사거리는 전부 `WEAPON_BOW`.
**사거리 내 적이 없으면 발사하지 않는다** — "적을 끌어들이는" 위치 판단이 남는다.

### 3-A. 조준 규칙 `WEAPON_BOW.AIM` — 전역 규칙 1개 (시련 분기 아님)

**문제(playtest 1회차 실측, 2026-08-08)**: 키클롭스 시련이 200판 중 보스 처치 **0건**,
보스 피해량 중앙값 **0.0%**(최대 11.8%). `AIM='nearest'`인데 grunt/brute가 거의 항상 보스보다
가깝고, 보스 속도 16은 플레이어 60에서 계속 뒤처진다. 이 시련은 `goal:1, wBossKill:1` —
**보스 처치가 유일한 클리어 조건**이라 클리어율이 구조적으로 0%다. 화력을 올려도 움직이지 않는다.
동시에 이건 **GDD §5가 이미 적어 둔 의도("잡몹 압력을 낮춰 보스에 화력을 쏟게 한다")를
구현할 장치가 애초에 없었다**는 뜻이다.

**규칙 (`AIM = 'bossFirstThenNearest'`)**

```
후보 = 사거리(WEAPON_BOW.RANGE) 안의 살아 있는 적
후보가 0개        → 발사하지 않는다. 쿨다운도 흐르지 않는다.  (기존과 동일)
후보에 type='boss'가 있으면 → 그중 최근접을 조준한다.
그 외              → 후보 중 최근접을 조준한다.              (기존과 동일)
```

- 분기 기준은 **`EnemyRuntime.type`(적 종류)이지 시련 id가 아니다.** → §8-2(시련 id 분기 0개) 유지.
- 연꽃·이타카는 `bossSpawnSec = 999 > timeLimitSec 180`이라 보스가 **원리적으로 등장하지 않는다.**
  즉 이 규칙은 3종 중 **2종에서 증명 가능한 무동작(no-op)**이고, 키클롭스에만 영향을 준다.
- 발사 빈도·투사체 수·확산·관통은 하나도 바뀌지 않는다. **바뀌는 것은 "누구를 겨누는가" 하나뿐이다.**
- 한 판에 보스는 1기뿐(`bossSpawnSec`으로만 등장)이지만, 규칙은 "boss 중 최근접"으로 적어
  보스가 2기 이상이 되어도 정의가 무너지지 않게 한다.
- 서사적 근거: **활은 가장 큰 위협을 노린다.** 오디세우스가 구혼자들 사이에서 안티노오스를
  먼저 쏘는 장면과 같은 규칙이다. 예외 규칙이 아니라 설명 가능한 규칙이라 플레이어가 학습할 수 있다.

**대안을 버린 이유** — B2(보스 속도 인상)는 D8(적 속도 상한 1.35×, 최대 46 < 플레이어 60,
"도주는 끝까지 가능")을 뒤집는다. B3(키클롭스 goal 변경)은 D1·§5의 "보스 = 체력 많은 적 1기"를
없앤다. B4(사거리 인상)는 §1의 "사거리 140 = 화면 폭 29%" 근거를 무너뜨리고, **최근접 우선인
한 보스는 여전히 후순위라 문제 자체를 안 푼다.** B1만이 다른 수치 근거를 하나도 건드리지 않는다.

**확정됨 (2026-08-08, 2회차-B 실측 후. D39).** `WEAPON_BOW.AIM`의 기본값이 이제
`'bossFirstThenNearest'`이며 잠정값이 아니다.

| 지표(키클롭스) | 2회차-A (`nearest`) | 2회차-B (`bossFirstThenNearest`) | 판정 |
|---|---|---|---|
| 보스 HP 피해 중앙값 | 11.8% | **29.1%** | 규칙이 의도대로 동작 |
| 보스 처치 | 6 / 200 | **13 / 200** | 〃 |
| 레벨업 중앙값 | 5 | **4** | 대가(아래) |
| 초당 처치 | 0.846 | **0.772** | 대가(아래) |
| 동시 최대 적 p95 | 49 | **54** | 합격 ≤90 유지 |
| 연꽃·이타카 8개 지표 | — | **바이트 단위 동일** | D28-ⓑ 무동작 실증 |

- **감시하기로 했던 대가가 실제로 나타났다**: 화살이 보스에 묶여 잡몹 처치와 XP 회수가 줄었다
  (레벨업 5→4, 초당 처치 0.846→0.772). 동시 최대 적 p95는 합격 범위 안이다.
  **이 값들은 이제 3회차의 기준선에 포함된다** — 되돌리는 것이 아니라 기준선으로 삼는다.
- **그런데도 키클롭스 클리어율은 0.065(합격 0.35의 1/5)다.** 중앙값 29.1%는
  "화력이 보스에 못 닿는다"가 아니라 **"닿아도 700HP를 다 못 깎는다"**는 뜻이다.
  `ENEMIES.boss.HP` 주석의 계산(DPS 18.2 → 70발 → 38.5초, 등장 30초 + 38.5 = 68.5초)은
  **"보스에만 100% 명중"을 전제**했는데, 실측 도주 비율 63% / 사거리 밖 틱 7.6%로 그 전제가 깨졌다.
  → **남은 원인은 `ENEMIES.boss.HP` 하나**이며 조정은 4회차다(§6-A). **3회차에 같이 바꾸지 않는다.**

### 레벨업 3택 구성 규칙
- 3택은 **화력 1 + 생존 1 + 유틸 1**을 각 카테고리에서 뽑는다.
- 해당 카테고리에 만렙 아닌 후보가 없으면 남은 카테고리에서 보충한다.
- → "3개가 전부 공격력 +%"인 화면이 **구조적으로 나올 수 없다.** 이게 이 게임의 유일한 플레이어 결정을 지키는 장치다.

| 카테고리 | 항목 |
|---|---|
| 화력 | 청동 촉(데미지) / 쌍둥이 화살(투사체+1) / 빠른 손(주기-15%) / 관통 화살(관통+1) |
| 생존 | 가죽 갑옷(최대HP+20) / 아테나의 가호(무적+0.2s) / 넥타르(즉시 40% 회복, 무제한 반복) |
| 유틸 | 헤르메스의 샌들(이동+12%) / 먼눈(흡수반경+40%) |

### 3-B. 3택의 세부 규칙 (문서에 없어서 tech가 임의로 정했던 것 — 이제 여기가 원본)

1. **`HIDE_ARMOR`(가죽 갑옷)는 최대HP 증가분(+20)만큼 현재HP도 즉시 회복한다.**
   근거: 전투 중에 최대HP만 올라가면 "HP 바의 빈칸이 늘어날 뿐" 그 순간에는 아무 일도 안 일어난다.
   생존 카테고리의 선택지가 **지금 죽지 않게 해 주지 않으면** 화력 선택지와 경쟁이 성립하지 않는다.
   넥타르(즉시 40% 회복)와의 차이는 유지된다 — 갑옷은 +20 고정·상한 4회, 넥타르는 비율·무제한.
2. **3택 대기 중에는 게임 시간이 멈춘다**(`RunState.pendingChoice`가 있는 동안 `stepRun`이 시간을 진행시키지 않는다).
   **§9-5의 시련 시작 배너는 반대로 멈추지 않는다.** 두 규칙이 다른 이유는 하나다 —
   **정지는 sim이 아는 것만 허용한다.** 3택 정지는 sim 내부 상태라 헤드리스 측정과 브라우저가 같은
   경과 시간을 보지만, 배너는 렌더 전용이라 sim이 모르고, 배너로 멈추면 §6 측정이 무효가 된다.
   (측정 영향: 봇은 3택을 즉시 고르므로 sim의 지표는 이 규칙과 무관하다. 브라우저 체감에만 영향.)

## 4. 스폰 압력 = 유일한 긴장의 출처

10초를 1**스텝**으로, 스텝마다 ① 스폰 간격 ×0.92 ② 적 HP +12% ③ 적 속도 +3%(상한 1.35×).
적 속도 상한 1.35×에서도 가장 빠른 적(46)이 플레이어(60)보다 느리다 — **도주는 끝까지 가능하다.**
난이도는 "못 도망침"이 아니라 "**도망칠 공간이 없어짐**"에서 나온다.

## 5. 시련 3종 — 차이는 `TRIALS` 데이터 행 3개뿐

**클리어 판정은 3종 공통 단일 함수다. 시련별 `if` 분기가 0개여야 한다.**

```
progress = 경과초 * wTime + 잡몹처치수 * wKill + 보스처치수 * wBossKill
클리어 ⇔ progress >= goal        패배 ⇔ HP<=0 또는 경과초 > timeLimitSec(180)
```

| 시련 | goal | wTime/wKill/wBoss | 보스 | 스폰총량 | 잡몹(grunt/brute) | 간격× | HP× | 속도× |
|---|---|---|---|---|---|---|---|---|
| 키클롭스의 동굴 | 1 | 0/0/1 | 30초 | ∞ | **1.0 / 0** (6회차에 0.7/0.3에서, D56) | 1.25 | 1.00 | 1.00 |
| 연꽃 먹는 자들의 해안 | 90 | 1/0/0 | 없음 | ∞ | **0 / 1.0** (6회차에 0.3/0.7에서, D56) | 0.85 | **1.00** (5회차에 1.15→1.00, D49) | 0.85 |
| 이타카의 홀 | 108 | 0/1/0 | 없음 | **108** | 1.0 / 0 (**불변**) | 0.75 | 0.90 | 1.10 |

- **키클롭스의 동굴** — 보스는 "체력 많은 적 1기". 고유 패턴 없음. 잡몹 압력을 낮춰 보스에 화력을 쏟게 한다.
  **6회차부터 그 의도가 `spawnIntervalMul` 1.25뿐 아니라 적 구성으로도 표현된다** — 마리당 평균 HP
  17.2 → **10.0**(3종 최저). 잡몹은 하찮고 위협은 거인 하나다.
- **연꽃 먹는 자들의 해안** — 느리고 단단한 다수. 화력 3택을 안 고르면 화면이 메워진다.
  **"단단함"의 원천은 `brute.HP` 34(grunt의 3.4배)와 구성비이지 `enemyHpMul`이 아니다.**
  그래서 5회차에 1.15 → **1.00**으로 내려도 이 시련의 성격은 유지된다. 6회차에 구성이 brute 100%가
  되면서 마리당 평균 HP는 26.8 → **34.0**(키클롭스 10.0의 **3.40배**, 이타카 9.0의 **3.78배**)로
  **격차가 오히려 커졌다.** "느림"(`enemySpeedMul` 0.85)·"다수"(`spawnIntervalMul` 0.85)는 손대지 않는다.
  **1.00은 탐색값이 아니다** — `computeEnemyHpMul`(`src/sim/spawn.ts:22`) 때문에 1.15에서는 연꽃 스텝0의
  grunt 실효 HP가 `10 × 1.15 = 11.5 > DAMAGE 10`이라 D27이 확정한 **"grunt 1발 킬"이라는 기준 단위가
  이 시련에서만 처음부터 깨져 있었다.** 1.00은 3종이 같은 기준 단위를 공유하게 되는 유일한 값이므로
  **탐색의 시작점이 아니라 종점**이다. → §6-A 5회차, D45·D49.
  ⚠️ **6회차 이후 이 근거는 관측 불가능해졌다(grunt 0마리). 그래도 1.00을 유지한다 — 근거가
  「3종이 같은 기준 단위를 공유한다」는 불변식이지 특정 시련의 관측이 아니기 때문이다.** (D56-ⓓ)
- **이타카의 홀** — 고정 108기 섬멸. 유일하게 실제로 추격당하는 조합. **3행 중 6회차에 값이 안 바뀐 유일한 행이다.**

오디세이아다움은 **시련 이름 + `tagline` 한 줄까지다.** 그 이상은 WON'T.
**단, 그 이름이 화면에 나오는 적과 모순되면 안 된다** — 그게 §5-A다.

### 5-A. 적 배치 규칙 — **시련 하나에 잡몹 한 종** (6회차 추가, D56)

**사용자 지적(2회)**: *"각 스테이지에 맞는 캐릭터만 나오게 해야 하는 거 아니야?"* 타당했다.
5회차까지의 `spawnWeights`는 **3종 중 2종에서 시련 이름과 적이 모순**이었다 —
연꽃 해안에 구혼자가 30%, 키클롭스의 동굴에 로토파고이가 30% 있었다.

**이건 튜닝 부족이 아니라 선언과 데이터의 불일치다.** §5 본문이 *"오디세이아다움은 시련 이름 +
tagline"*이라고 선언했는데, `spawnWeights`는 §5가 "시련의 정체성을 표현한다"고 규정한 바로 그
**데이터 행의 필드**이면서 그 정체성과 반대 방향으로 채워져 있었다. → D56에서 닫았다.

#### 규칙 — 비둘기집이 이 배치를 강제한다

```
시련 3종 × 잡몹 2종(grunt/brute).  boss는 bossSpawnSec으로만 등장하므로 배치 대상이 아니다.
→ 3 > 2 이므로 어느 한 종은 반드시 두 시련에 걸친다. 이건 §2 「적 종류 3종」 상한의 산술적 귀결이다.
→ 겹치는 쪽을 grunt로 고른다. 그 종만 정체성을 시련이 주고, 나머지 한 종은 시련 전속이 된다.
```

| 시련 | 잡몹 | 그 잡몹이 이 시련에서 무엇인가 |
|---|---|---|
| 키클롭스의 동굴 | grunt 100% + boss 1기 | **동굴을 메운 이름 없는 것들.** tagline "이름 없는 자로 빠져나가라"와 같은 말이다 |
| 연꽃 먹는 자들의 해안 | brute 100% | **로토파고이.** 시련 이름이 곧 적 이름이다 |
| 이타카의 홀 | grunt 100% | **구혼자 108명.** `goal 108` = `spawnTotalCap 108` = 그 수 |

**왜 겹치는 쪽이 grunt인가 (3가지가 같은 답을 가리킨다)**

1. **brute는 두 번째 정체성을 가질 수 없다.** 「로토파고이」는 시련 이름 자체이므로 다른 시련에 두면
   반드시 모순이 된다. grunt는 형태(마르고 빠른 인간 무리)가 먼저이고 이름은 시련이 준다.
2. **밸런스 비용이 grunt 쪽이 싸다는 것이 사전 측정으로 확인됐다.** 키클롭스를 brute 전속으로 만들면
   마리당 평균 HP가 17.2 → 34.0(**+98%**)이 되어 4회차에 사전 등록 산식으로 맞춘 클리어율 0.490이
   확실히 무너진다. grunt 전속은 17.2 → 10.0이고 **측정에서 클리어율이 움직이지 않았다.**
3. **키클롭스의 설계 의도와 방향이 같다.** §5·D1이 *"잡몹 압력을 낮춰 보스에 화력을 쏟게 한다"*고
   적어 뒀는데, grunt 전속은 그 압력을 더 낮춘다. brute 전속은 정반대다.

#### 안 만들 것 (이 절에서 명시적으로 제외한다)

- **시련별 적 이름 표시 필드·UI.** 위 표의 "무엇인가" 칸은 **문서에만** 있다. 화면에 적 이름을 띄우면
  §2 조작·표시 축이 늘고, 480×270에 줄이 하나 더 생긴다. **서사는 시련 이름과 tagline이 나른다**(§5 본문).
- **네 번째 적 종.** 「동굴의 것들」 전용 스프라이트를 만들면 §2 적 3종 상한과 에셋 5장 상한이 동시에 깨진다.
- **`spawnWeights`를 0/1이 아닌 중간값으로 두는 안**(예: 0.1/0.9). 문제가 "비율이 나쁘다"가 아니라
  "있으면 안 되는 것이 있다"이므로 **비율을 줄이는 것은 모순을 90%만 남기는 것**이다.
- **시련 순서·이름·tagline 변경.** D54-ⓔ가 이미 못박았다(이타카는 여정의 도착지라 순서는 설계다).
- **6회차 결과에 따른 재조정.** D56-ⓒ에서 사전에 닫았다 — 어떤 수가 나와도 `spawnWeights`를 다시 만지지 않는다.

## 6. 목표 지표 (합격선) — `TARGET_METRICS` 및 `playtest.md`와 동일값

측정 조건: **시련별 200회, 시드 고정, 봇 정책 = §7.** 시련별로 따로 측정한다(합산 금지).

| 지표 | 합격 구간 |
|---|---|
| 시련별 클리어율 | **0.35 ~ 0.70** |
| 사망한 판의 생존시간 중앙값 ÷ 90초 | **0.45 ~ 0.85** |
| 30초 이전 사망 비율 | **≤ 0.15** |
| 판당 레벨업 횟수 중앙값 | **8 ~ 14** |
| 판 평균 초당 처치 수 | **1.0 ~ 4.0** |
| 동시 최대 적 수 p95 | **≤ 90** |
| 도달 난이도 스텝(10초=1) 중앙값 | **5 ~ 9** |
| 클리어한 판의 클리어 시각 중앙값 | **≤ 117초** |

**한 판 목표 길이 = 90초**(`TRIAL_TARGET_SEC`). 근거: 제출물 2번이 30~60초 영상이라 한 판이 90초면
편집 없이도 클리어 장면까지 담기고, 심사자가 링크를 눌러 한 판을 끝까지 볼 수 있다.

"30초 이전 사망"의 **30초는 `TARGET_METRICS.EARLY_DEATH_SEC`**다(= 3스텝 = 목표 판 길이의 1/3).
합격선이 아니라 지표의 정의이며, 값은 1회차 그대로다.

### 6-A. 튜닝 회차 계획 — 한 회차에 한 축만 (1회차 반려 대응, 2026-08-08)

1회차 결과: 클리어율 0.000/0.055/0.030, 레벨업 중앙값 2/1/2, 초당 처치 0.535/0.308/0.712 (전부 미달).
**합격선은 한 줄도 고치지 않는다.** 측정값이 안 나온다고 합격선을 내리는 건 자기기만이다.

| 회차 | 바꾸는 것 | 왜 이 순서인가 |
|---|---|---|
| **2회차-A** | `WEAPON_BOW.DAMAGE` 6 → 10 **하나뿐** | 화력 축은 **3종 시련 전부 × 미달 지표 4개**(클리어율·레벨업·초당 처치·클리어 시각)에 걸린다. 가장 넓은 축을 먼저 재야 다음 판단의 기준선이 생긴다 |
| **2회차-B** | `WEAPON_BOW.AIM` → `'bossFirstThenNearest'` (§3-A) | 좁은 축. **1종 시련 × 1지표**에만 걸린다(연꽃·이타카는 보스가 안 나와 증명 가능한 무동작). A의 결과로 확정된 실효 DPS가 있어야 보스 700HP가 타당한지 판정된다 — 순서를 뒤집으면 보스 HP를 깜깜이로 튜닝하게 된다 |
| **3회차** | `XP_CURVE.GROWTH` 1.25 → 1.10 **하나뿐** | 2회차 측정이 병목을 **회수 → 공급**으로 뒤집었다(아래). 성장 곡선은 **3종 시련 전부**에 같은 방향으로 걸리는 축이고, 시련별 적 구성비에 좌우되지 않는다. 기준선은 **2회차-B**다(`AIM` 확정으로 그 앞 회차가 아니다) |
| **4회차** | `ENEMIES.boss.HP` **하나뿐** (700 → **324**. 적용 완료) | 키클롭스 전용 잔여 결함. §3-A가 조준 규칙의 동작을 실증했는데도 클리어율 0.065 → 남은 원인이 HP뿐이다. **3회차와 섞지 않는다** — 하나는 3종 전부에, 하나는 1종에만 걸려 효과 분리가 불가능해진다. 조정치는 `HP_new = round(700 × m)`, m = 3회차에서 측정된 보스 피해량 중앙값 비율(탐색 금지, D30) → **m = 0.462513 → 324** |
| **5회차** | `TRIALS[lotophagoi].enemyHpMul` 1.15 → **1.00** **하나뿐** (적용·측정 완료 → §6-A-2) | **D37의 「5회차 없음」을 개정해 신설했다(D44).** 이유는 하나다 — `boss.HP`는 `bossSpawnSec 999`인 연꽃에 **수학적으로 영향이 0**인데(§6-A-1) 연꽃은 **3지표 미달**(클리어율 0.100 / 레벨업 3 / 초당 처치 0.605)이다. 4회차 직후 동결하면 **연꽃이 손도 안 댄 채로 제출된다.** 축을 **연꽃 행 단일 필드**로 고른 이유는 키클롭스·이타카가 **증명 가능한 무동작**이라 4회차의 `boss.HP` 결과가 훼손되지 않기 때문이다(D45) |
| **6회차** | `TRIALS[*].spawnWeights` 3행 (키클롭스 `[.7,.3,0]` → `[1,0,0]` / 연꽃 `[.3,.7,0]` → `[0,1,0]` / 이타카 **불변**). **적용·측정 완료 → §6-A-3** | **D44의 「6회차는 없다」를 개정해 신설했다(D56).** 개정 사유는 **시간도 사용자 요청도 아니라 §5 선언과 데이터의 불일치**다 — §5가 `spawnWeights`를 "시련의 정체성을 표현하는 데이터 행의 필드"로 규정해 놓고, 실제 값은 3종 중 2종에서 시련 이름과 모순이었다(연꽃에 구혼자 30%, 동굴에 로토파고이 30%). **이건 지표를 쫓는 튜닝이 아니다** — 8지표가 전부 합격이어도 똑같이 고쳐야 하는 결함이다. D28(클리어 불가)·D45-ⓓ(연꽃 1발 킬 붕괴)와 같은 「결함 수정」 분류다. **대가는 §6-A-3에 사전 등록했다** |

- **두 변경을 같은 측정에 섞지 않는다.** ~~tech는 A·B를 모두 구현하되 `AIM` 기본값은 `'nearest'`로 두고,
  2회차-A가 기록으로 남은 뒤 `AIM` 한 줄만 뒤집어 2회차-B를 돌린다.~~ → **완료. 2회차-B 측정 후
  `AIM` 기본값은 `'bossFirstThenNearest'`로 확정됐다(D39, §3-A).** 이제 3회차의 기준선은 2회차-B다.
- **2회차-A의 사전 등록된 예상**(측정 후 갖다 붙이는 해석을 막기 위해 미리 적는다):
  초당 처치 1.3~1.8배 개선. 레벨업은 봇 XP 회수 효율(45~61%) 때문에 8 미만에 머물 수 있다.
  **키클롭스 클리어율은 A만으로는 0.000에서 안 움직이는 것이 정상이다.** 그걸 "DAMAGE가 부족했다"로
  읽지 마라 — 그 판정은 2회차-B의 몫이다.
- **2회차-B가 성공했는지 판정하는 지표는 클리어율이 아니라 「보스 피해량 비율 중앙값」이다.**
  1회차 0.0% → B에서 유의미하게 오르면 조준 규칙은 의도대로 동작한 것이다.
  올랐는데도 클리어율이 0.35 미만이면 남은 원인은 **보스 HP뿐**이며, 그때 조정할 단 하나의 수치는
  `ENEMIES.boss.HP`다. 조정치는 측정된 보스 조준 유지율에서 산출한다(감으로 바꾸지 않는다).
- ~~레벨업이 A 이후에도 8 미만이면 다음 조사 대상은 `BOT.THREAT_RADIUS_RATIO`다~~ →
  ~~이 경로는 2회차 실측으로 닫혔다(§7-A).~~ → **회수 경로는 3회차 실측으로 다시 열렸다(§7-A, D45).**
  D38의 닫는 근거는 **구 곡선(요구 99)** 기준이었고 새 곡선(요구 58)에서는 성립하지 않는다.
  다만 **5회차에 고르는 축은 여전히 `PICKUP_RADIUS`가 아니다** — 이유는 §7-A와 D45에 있다.
  (`BOT.THREAT_RADIUS_RATIO`는 봇 정책 = 측정 도구라 D10에 걸려 애초에 후보가 아니다.)
- **D18(밸런스 왕복 제한)과의 관계**: 2회차-B는 밸런스 튜닝이 아니라 **결함 수정**으로 분류한다.
  클리어율 0.000은 "덜 맞춰졌다"가 아니라 "그 시련을 끝낼 방법이 존재하지 않는다"이기 때문이다.
  **D18의 왕복 상한은 2 → 3(D37) → 4(D44)로 개정됐다**(§9-7). → **수치 동결 시점은 5회차 직후**이며,
  그 뒤로는 `playtest` 측정만 하고 남은 시간은 제출물 4종에 쓴다. **6회차는 어떤 결과가 나와도 없다.**

#### 6-A-0. **4회차 실측 결과 — 사전 등록한 예측이 맞았다** (2026-08-08, D43)

**이 표가 이 프로젝트 방법론의 물증이다.** 산식과 목표값을 **측정 전에** D39·D43에 등록했고,
측정은 그 뒤에 했다. 따라서 아래 일치는 **사후 조정(fitting)이 아니다.**

| 키클롭스 | 3회차 | **4회차 실측** | 사전 등록한 예측 | 합격 구간 | 판정 |
|---|---|---|---|---|---|
| 클리어율 | 0.210 | **0.490** | **0.50**(= 산식의 목표) | 0.35~0.70 | **합격.** 예측과 오차 0.010 |
| 클리어 시각 중앙값 | 67.7s | **54.975s** | — | ≤117s | 합격 |
| 합격 지표 수 | 5/8 | **6/8** | — | 8/8 | — |
| 남은 미달 | — | 레벨업 **5** / 초당 처치 **0.769** | — | 8~14 / 1.0~4.0 | **확정 미달(§6-B)** |

- **바꾼 수치는 `ENEMIES.boss.HP` 700 → 324 하나뿐이다.** 산식 `round(700 × m)`, m = 3회차 실측
  보스 HP 피해 비율 중앙값 **0.462513**. **탐색하지 않았다**(D30).
- **연꽃·이타카는 8지표 전부 바이트 단위로 불변**이었다 → §6-A-1의 「서로소 시련 집합」 가정과
  `bossSpawnSec 999 > timeLimitSec 180`이면 `spawnBossIfDue`가 실행조차 안 된다는 예고가
  **실측으로 확인**됐다(§8-30-b 합격). 2회차-B에 이어 **두 번째 무동작 실증**이다.
- **사전 등록한 단서는 발현하지 않았다**: "보스 조기 처치 → 화살 재분배 → 클리어율이 0.50 **위로**
  밀릴 수 있다"고 적었으나 실측은 0.490으로 **아래쪽**이었다. 되먹임이 예상보다 작았다는 뜻이며,
  구간 [0.35, 0.70] 안이므로 **재조정하지 않는다**(D44-ⓒ).

#### 6-A-1. 4회차와 5회차의 순서 — 섞지 않는 근거

두 변경은 **서로소인 시련 집합**에 걸린다. 그래서 순서를 정하면 효과 분리가 보장된다.

| 회차 | 바꾸는 곳 | 키클롭스 | 연꽃 | 이타카 |
|---|---|---|---|---|
| 4회차 | `ENEMIES.boss.HP` | 영향 | **무동작**(`bossSpawnSec 999` → `spawnBossIfDue` 미실행) | **무동작**(동일) |
| 5회차 | `TRIALS[lotophagoi].enemyHpMul` | **무동작**(다른 행) | 영향 | **무동작**(다른 행) |

- **먼저 `boss.HP`다.** 산식의 입력 m(=0.462513)이 3회차 상태에서 측정된 값이라, 그 상태가 유지되는
  동안에만 유효하다. 연꽃 축을 먼저 넣어도 키클롭스는 무동작이라 m은 살아 있지만, **이미 계산이
  끝난 값을 미루면 4·5회차 사이에 판단할 것이 두 개가 된다.** 순서를 정하는 목적이 그거다.
- **5회차에 전역 축(`PLAYER.PICKUP_RADIUS`·`ENEMIES.*.XP`)을 쓰지 않는 결정적 이유가 이 표다.**
  전역 축은 위 「무동작」 칸을 전부 「영향」으로 바꾼다 → 4회차에 0.50으로 맞춰 놓은 키클롭스
  클리어율이 다시 흔들리고, 그걸 되잡으려면 6회차가 필요해진다. **동결이 불가능해진다.**

#### 6-A-2. **5회차 실측 결과 — 8지표 전체** (2026-08-08, D49 적용분. §8-30 충족)

> ⚠️ **이 표는 뒤늦게 기록됐다(D57).** 5회차 측정은 끝나 있었는데 §6-A에 회차 기록이 없었고,
> 클리어율 3개만 D54의 난이도 등급표에 인용돼 있었다. **반쪽 기록은 다음 세션이 원본으로 삼는다** —
> 그래서 8지표 전체를 여기로 옮긴다. 등급표(§9-8-A)는 이 표의 **인용**이지 원본이 아니다.

**바꾼 수치는 `TRIALS[lotophagoi].enemyHpMul` 1.15 → 1.00 하나뿐이다**(D45에 사전 등록한 값 그대로, 탐색 없음).
측정 조건: 시련별 **200회**, 시드 고정, 봇 정책 = §7.

| 지표 | 합격 구간 | 키클롭스 | 연꽃 | 이타카 |
|---|---|---|---|---|
| 클리어율 | 0.35 ~ 0.70 | **0.490** ✅ | **0.215** ❌ | **0.450** ✅ |
| 사망 판 생존시간 ÷ 90 | 0.45 ~ 0.85 | 0.804 ✅ | 0.633 ✅ | 0.594 ✅ |
| 30초 이전 사망 비율 | ≤ 0.15 | 0.000 ✅ | 0.000 ✅ | 0.000 ✅ |
| 판당 레벨업 중앙값 | 8 ~ 14 | **5** ❌ | **5** ❌ | **6** ❌ |
| 판 평균 초당 처치 | 1.0 ~ 4.0 | **0.769** ❌ | **0.812** ❌ | 1.314 ✅ |
| 동시 최대 적 p95 | ≤ 90 | 46 ✅ | **90** ✅(경계) | 50 ✅ |
| 도달 스텝 중앙값 | 5 ~ 9 | 6 ✅ | 6.5 ✅ | 6 ✅ |
| 클리어 판의 클리어 시각 중앙값 | ≤ 117초 | 54.975 ✅ | 90.050 ✅ | 71.400 ✅ |
| **합격 지표 수** | 8/8 | **6/8** | **5/8** | **7/8** |

- 연꽃 초당 처치는 **3택 카테고리 중복 결함 수정 후 0.811 → 0.812**로 이동한 값이다(같은 회차 내 재측정).
- **5회차의 판정**: 연꽃 클리어율 0.100 → **0.215**로 2.15배 올랐으나 **하한 0.35에 못 미친다.**
  D45-ⓖ가 사전 등록한 *"직접 효과만으로는 하한을 못 넘고 되먹임은 추정이지 측정이 아니다"*가
  **맞았다** — 되먹임은 실재했지만(0.100 → 0.215) 하한을 넘길 만큼은 아니었다.
  **예상대로 6회차를 열지 않았고**, D44-ⓒ대로 §6-B에 연꽃 행을 추가했다(아래).
- **§8-30-b(무동작 실증) 3차**: 5회차의 키클롭스·이타카가 4회차와 동일해야 한다. 키클롭스
  클리어율 0.490 / 클리어 시각 54.975는 §6-A-0의 4회차 값과 **자릿수까지 같다** → 합격.

⚠️ **함께 발견한 것 (하네스 17. 이번에 고치지 않았다)**: **연꽃의 동시 최대 적 p95가 90**이고,
이는 `SPAWN.MAX_ALIVE`(90)와 **정확히 같다** — 스폰 곡선이 상한에 잘렸다는 뜻이다.
`TARGET_METRICS.MAX_ALIVE_P95_MAX`가 **≤ 90**이라 형식상 합격이지만 **경계값이다.**
동시에 §9-10이 표시 크기 근거로 쓰는 **「화면당 실제 마릿수 N = 54(실측 p95 최댓값)」의 전제가 5회차에
이미 낡았다**(54는 키클롭스 2회차-B 값이다). **이번 작업에서 `VISUAL.SPRITE_PX`를 바꾸지 않는다** —
§9-10 자신의 규칙이 *"점유율 초과는 표시 크기로 대비하지 않고 밸런스 축에서 잡는다"*이고,
그 밸런스 축은 동결 중이며, 사용자가 "캐릭터가 작다"고 두 번 지적해 키운 값을 하루 만에 되돌리게 된다.
→ **사실만 §9-10 표에 남긴다.**

#### 6-A-3. 6회차 — `spawnWeights` 3행 (**측정 완료. 2026-08-08, D58**)

**바꾼 수치는 `TRIALS[*].spawnWeights` 뿐이다.** `goal`/`wTime`/`wKill`/`wBossKill`/`bossSpawnSec`/
`spawnTotalCap`/`spawnIntervalMul`/`enemyHpMul`/`enemySpeedMul`/`timeLimitSec`/`bgTint`/`difficultyTier`와
**배열 순서**, 그리고 `WEAPON_BOW`·`ENEMIES`·`SPAWN`·`XP_CURVE`·`BOT`·`UPGRADE_POOL`·`TARGET_METRICS`는
**한 글자도 안 바뀌었다.** 사유와 배치 규칙은 §5-A, 결정은 D56.

측정 조건: 시련별 **200회**(`npm run sim -- 200`), 시드 고정, 봇 정책 = §7.
타입체크 `npx tsc --noEmit` · `typecheck:sim` 모두 exit 0.

| 지표 | 합격 구간 | 키클롭스 | 연꽃 | 이타카 |
|---|---|---|---|---|
| 클리어율 | 0.35 ~ 0.70 | **0.455** ✅ | **0.255** ❌ | **0.450** ✅ |
| 사망 판 생존시간 ÷ 90 | 0.45 ~ 0.85 | **0.893** ❌ ← **새 미달** | 0.636 ✅ | 0.594 ✅ |
| 30초 이전 사망 비율 | ≤ 0.15 | 0.000 ✅ | 0.000 ✅ | 0.000 ✅ |
| 판당 레벨업 중앙값 | 8 ~ 14 | **5** ❌ | **5** ❌ | **6** ❌ |
| 판 평균 초당 처치 | 1.0 ~ 4.0 | **0.896** ❌ | **0.690** ❌ | 1.314 ✅ |
| 동시 최대 적 p95 | ≤ 90 | 46 ✅ | **90** ✅(포화) | 50 ✅ |
| 도달 스텝 중앙값 | 5 ~ 9 | 7 ✅ | 6 ✅ | 6 ✅ |
| 클리어 판의 클리어 시각 중앙값 | ≤ 117초 | 60.800 ✅ | 90.050 ✅ | 71.400 ✅ |
| **합격 지표 수** | 8/8 | **6/8** | **5/8** | **7/8** |

**5회차 → 6회차 이동 (미달 총계는 7개로 동일. 구성만 바뀌었다)**

| 항목 | 5회차 | 6회차 | 판정 |
|---|---|---|---|
| 연꽃 클리어율 | 0.215 | **0.255** | 개선. 여전히 하한 0.35 밖 |
| 키클롭스 초당 처치 | 0.769 | **0.896** | 개선. 하한까지 거리 0.231 → **0.104(절반 이하)** |
| 키클롭스 클리어율 | 0.490 | 0.455 | 합격 유지(차이 0.035 = 약 1 표준오차) |
| 키클롭스 클리어 시각 | 54.975 | 60.800 | 합격 유지 |
| 키클롭스 도달 스텝 | 6 | 7 | 합격 유지 |
| **키클롭스 사망 생존/목표** | **0.804 ✅** | **0.893 ❌** | **새 미달** → §6-B |
| 연꽃 초당 처치 | 0.812 | 0.690 | 악화(이미 미달이던 지표) |
| **이타카 8지표** | — | **전부 불변** | §8-51 합격 |

**사전 등록한 예상 vs 실측 — 맞은 것과 틀린 것을 같은 규칙으로 남긴다**

| # | 사전 등록한 내용 | 실측 | 판정 |
|---|---|---|---|
| 1 | 이타카 8지표 전부 5회차와 동일(증명 가능한 무동작) | 8지표 전부 불변 | **맞음.** 무동작 실증 **4차**(2회차-B·4회차·5회차에 이어) |
| 2 | 키클롭스 클리어율 거의 불변 | 0.490 → 0.455 (약 1 표준오차) | **맞음** |
| 2 | 키클롭스 초당 처치 소폭 상승 | 0.769 → 0.896 (**+16.5%**) | 방향 맞음. **"소폭"보다 컸다** |
| 3 | 연꽃 클리어율 소폭 상승 | 0.215 → 0.255 | **맞음** |
| 3 | 연꽃 초당 처치 하락 | 0.812 → 0.690 | **맞음** |
| 4 | 연꽃 p95가 90(상한)을 유지 | 90 | **맞음** |
| 5 | 결과와 무관하게 재조정 없음 | 7회차 없음(D58) | **집행됨** |

⚠️ **가장 중요한 것은 「빗나간 예측」이 아니라 「등록하지 않은 지표」다(D58-ⓔ).**
위 표의 예측은 **8지표 중 3개(클리어율·초당 처치·p95)만** 다뤘다. 나머지 5개는 *"어떻게 움직일지"*를
한 줄도 안 적었고, **새 미달은 정확히 그 등록 안 한 5개 중에서 나왔다**(사망 생존시간).
즉 이번 실패는 **예측이 틀린 것이 아니라 사전 등록의 범위가 좁았던 것**이다.
**이 실수는 두 번째다** — D37이 4회차를 `boss.HP`로 지정하면서 *"그 값이 연꽃에 영향 0"*을 계산에 넣지
않아 5회차 구멍을 만든 것과 **같은 종류**다(D44가 그 개정이었다).
→ **규칙으로 남긴다: 사전 등록은 「바뀔 것 같은 지표」가 아니라 8지표 전부에 대해 쓴다.**
(이번엔 고치지 않는다. 7회차가 없어 적용할 회차가 없다.)

**tech 사전 측정(다른 시드)의 예측력 — 도구로서 유효했다**

| | tech 사전 측정 | 공식 러너 6회차 | |
|---|---|---|---|
| 키클롭스 초당 처치 | 0.79 → **0.89** | 0.769 → **0.896** | 증분·절대값 모두 근접 |
| 키클롭스 클리어율 | 불변 | -0.035(≈1 표준오차) | 방향 일치 |
| 연꽃 클리어율 | 0.215 → 0.245 | 0.215 → **0.255** | 방향·크기 일치 |
| 연꽃 초당 처치 | 0.76 → 0.58 | 0.812 → **0.690** | 방향 일치, 낙폭 과대추정 |

→ **시드가 달라 절대값은 못 쓰지만 「방향과 대략의 크기」는 신뢰할 수 있다는 것이 실증됐다.**
그래도 §6 기록에는 **공식 러너 값만** 들어간다(하네스 16).

**아래는 측정 전에 적어 둔 원문이다. 지우지 않는다.**

1. **이타카는 8지표 전부 5회차와 동일해야 한다.** 이 행은 값이 안 바뀌었고, `spawnWeights`는
   `TRIALS`의 행 로컬 필드다 → **증명 가능한 무동작.** 다르면 밸런스가 아니라 §6-A-1의
   「서로소 시련 집합」 가정이 깨진 것이므로 **수치를 만지기 전에 원인을 먼저 보고한다**(§8-30-b).
2. **키클롭스**: 클리어율 **거의 불변**, 초당 처치 **소폭 상승**을 예상한다. 마리당 평균 HP가
   17.2 → 10.0이라 잡몹이 더 빨리 죽지만, 이 시련의 goal은 `wBossKill` 1이고 보스 HP·조준 규칙은
   안 바뀌었다. tech 사전 측정(다른 시드)의 방향과 같다.
3. **연꽃**: 클리어율 **소폭 상승**, 초당 처치 **하락**을 예상한다. goal이 `wTime` 90초 생존인데
   brute 실효 속도 17이 grunt 28.9보다 느리고, 대신 마리당 평균 HP가 26.8 → 34.0이라 덜 죽는다.
   **초당 처치는 이미 확정 미달 지표이므로 더 나빠져도 결론이 바뀌지 않는다.**
4. ⚠️ **연꽃의 동시 최대 적 p95가 90(상한)을 유지하거나 더 빨리 도달할 것으로 본다** — 적이 더
   단단해졌기 때문이다. **90을 넘을 수는 없다**(`SPAWN.MAX_ALIVE`가 하드 캡). 즉 이 지표는
   구조적으로 합격하지만 **의미는 「포화」다.** 그 사실을 §6-B에 적는다.
5. **어떤 결과가 나와도 `spawnWeights`를 다시 만지지 않는다(D56-ⓒ).** 7회차는 없다.
   합격 구간 밖인 지표는 전부 §6-B에 행으로 옮기고, `difficultyTier`는 새 클리어율에
   경계 0.35를 **다시 적용**한다(계산이지 판단이 아니다, D54-ⓒ).

### 6-B. 확정 미달 — 이대로 제출한다 (합격선은 한 줄도 안 내렸다)

**§6의 합격 구간은 절대선이다. 아래는 「기준을 낮춘 것」이 아니라 「기준에 못 미친 채로 낸다고
명시한 것」이다.** 이 구분을 흐리면 다음 세션이 §6 표를 고치려 든다.

| 시련 | 확정 미달 지표 | 왜 이것을 포기하는가 |
|---|---|---|
| **이타카의 홀** | 판당 레벨업 중앙값 **6** (합격 8~14) | 이타카는 **클리어율·초당 처치가 이미 합격**이라 코어 루프가 정상 작동한다. 즉 심사자가 이 시련을 켜면 **끝까지 플레이되고 이길 수 있다.** 이걸 고치는 유일한 길은 전역 XP 축이고, 그건 §6-A-1 표의 「무동작」을 전부 깨서 **키클롭스·연꽃 두 시련을 다시 흔든다.** 1지표를 얻으려 2시련을 거는 거래라 안 한다 |
| **키클롭스의 동굴** (4회차 실측으로 확정, **6회차 값으로 갱신**. 2026-08-08) | 판당 레벨업 중앙값 **5** (합격 8~14)<br>판 평균 초당 처치 **0.896** (합격 1.0~4.0. ~~0.769~~ → 개선했으나 여전히 밖)<br>**사망 판 생존시간 ÷ 90 = 0.893** (합격 0.45~0.85) ← **6회차의 새 미달. 근거는 아래 별도 문단** | **둘 다 이 시련의 설계가 직접 만든 값이지 튜닝 부족이 아니다.** ⓐ `spawnIntervalMul` **1.25**는 §5·D1이 *"보스에 화력을 쏟게 하려고 잡몹 압력을 25% 낮춘다"*고 **의도적으로** 정한 값이다 — 잡몹이 25% 적게 나오는 시련의 초당 처치와 XP 공급이 낮은 것은 결함이 아니라 그 설계의 산술적 귀결이다. ⓑ `AIM='bossFirstThenNearest'`(D39)는 화살을 보스에 묶는다. **D39가 「대가」로 미리 지목했고 실측된 항목이 정확히 이 둘이다**(레벨업 5→4, 초당 처치 0.846→0.772). ⓒ **되돌리면 클리어율이 무너진다**: `spawnIntervalMul`을 낮추면 시련 정체성이 사라지고 방금 0.490에 맞춘 클리어율이 다시 흔들리며, `AIM`을 되돌리면 클리어율이 **0.065로 회귀**한다. **즉 이 2지표는 클리어율 0.490과 맞바꾼 것이고, 그 거래는 이미 옳다고 판정됐다.** ⓓ 참고: `AIM` 이전(2회차-A, `nearest`)에도 레벨업 5 / 초당 처치 0.846으로 **둘 다 이미 미달**이었다 — 조준 규칙이 원인의 전부가 아니라는 증거다. ⓔ **고칠 회차가 없다**: 5회차는 연꽃 로컬이라 키클롭스에 무동작이고 6회차는 없다(D44) |

| **연꽃 먹는 자들의 해안** (5회차 실측으로 확정, **6회차 값으로 갱신**. 2026-08-08, D57·D58) | 클리어율 **0.255** (합격 0.35~0.70. ~~0.215~~ → 개선했으나 여전히 밖)<br>판당 레벨업 중앙값 **5** (합격 8~14)<br>판 평균 초당 처치 **0.690** (합격 1.0~4.0. ~~0.812~~ → **악화**. D56이 사전 등록한 대가다)<br>참고: 동시 최대 적 p95 **90** — 형식상 합격(≤90)이나 **`SPAWN.MAX_ALIVE`와 같은 값 = 포화**다 | **§6-B 본문이 예고한 조건이 그대로 발생했다** — *"5회차 이후에도 미달이면 이 표에 연꽃 행을 추가하고 그대로 제출한다"*(아래 원문 유지). ⓐ **5회차는 실패가 아니라 부분 성공이다**: 클리어율 0.100 → **0.215**로 2.15배 올랐다. D45-ⓖ가 *"직접 효과만으로는 하한을 못 넘고, 넘기는 것은 되먹임의 몫이며 그건 추정이지 측정이 아니다"*라고 **사전 등록**했고 그대로 됐다 — **되먹임은 실재했으나 부족했다.** ⓑ **그래도 6회차를 그 지표에 쓰지 않았다**: D44-ⓒ가 *"미달이면 §6-B에 적고 그대로 낸다. 동결은 조건부가 아니다"*를 측정 전에 못박았고, D44-ⓑ가 도망갈 축 3개(`PICKUP_RADIUS`/`brute.XP`/`lotophagoi.wKill`)를 **미리 영구 기각**해 뒀다. **예산이 없어서가 아니라 예산을 미리 닫아 뒀기 때문에 안 쓴다** — 그게 동결의 정의다. ⓒ **6회차(D56)는 이 지표를 겨냥한 것이 아니다.** `spawnWeights`는 서사 정합 결함 수정이며 §6-A-3이 *"초당 처치는 더 나빠질 것"*으로 사전 등록했다. **미달 지표를 쫓아 연 회차가 아니라는 것이 이 구분의 전부다.** ⓓ **그럼에도 §8-1은 지켜진다**: 0.215는 "이길 수 없다"(0.100, 10판에 9패)가 아니라 **"어렵다"**(5판에 1승)이고, 그 사실을 타이틀 목록에 `어려움` 라벨로 **화면에 그대로 표시**한다(§9-8-A, D54). 숨기지 않는 것이 이 항목의 대가 지불 방식이다 |

**~~연꽃은 (아직) 이 표에 넣지 않는다.~~** ~~클리어율 0.100은 지표 미달이 아니라 **"사실상 이길 수 없는 스테이지"**다
(하한 0.35의 1/3도 안 된다). 심사자가 목록 2번을 고르면 10판 중 9판을 진다 — 이건 수치 문제가 아니라
§8-1(3종 각각 클리어 화면에 도달 가능)이 실질적으로 무너지는 것이라 5회차를 거기에 쓴다.~~
**단, 5회차 이후에도 미달이면 그때는 이 표에 연꽃 행을 추가하고 그대로 제출한다.** 6회차는 없다.

→ **이 예고가 발동했다. 위 표에 연꽃 행을 추가했다(D57).** 원문을 지우지 않고 취소선으로 남기는 이유는
**"조건을 측정 전에 적어 두고 그대로 집행했다"는 것이 이 프로젝트 방법론의 물증**이기 때문이다.
(*"6회차는 없다"*는 D56에서 **다른 사유**로 개정됐다 — 미달 지표를 쫓아서가 아니라 §5 선언과
데이터의 불일치를 닫기 위해서다. 그 구분이 무너지면 동결은 아무것도 막지 못한다. §6-A 6회차 행 참조.)

~~⚠️ 위 3개 시련의 미달 지표는 5회차 기준이다. 6회차 실측이 도착하면 이 표를 그 값으로 갱신한다~~
→ **갱신 완료(D58).** 위 표는 이제 **6회차 = 동결 시점**의 값이다(§8-30-c 충족).
**합격 구간 표(§6)는 1회차부터 한 글자도 바뀌지 않았고 이번에도 안 바뀌었다.**

#### 6-B-1. 키클롭스의 새 미달 — 사망 판 생존시간 **0.893** (6회차. D58)

**이 지표가 경고하는 현상은 관측되지 않았다. 그런데도 되돌리지 않는다.** 둘 다 근거가 있다.

`DEATH_TIME_RATIO_MEDIAN`의 상한 0.85에 붙은 근거는 *"0.85 초과면 **마지막 순간만 어렵다**"*이다.
그 진단이 맞다면 판 앞부분이 헐거워졌다는 뜻이고, 그건 §4(긴장의 유일한 출처 = 스폰 압력)를 건드리므로
심각하다. **그래서 먼저 그 진단이 사실인지 검증했다 — 3개 지표가 전부 반대를 가리킨다.**

| 만약 "앞이 쉬워지고 끝만 어렵다"면 | 실측 |
|---|---|
| 클리어율이 **올라야** 한다(쉬워졌으므로) | 0.490 → **0.455로 내려갔다** |
| 판이 짧아지거나 그대로여야 한다 | 도달 스텝 6 → **7**, 클리어 시각 54.975 → **60.800** — **판 전체가 길어졌다** |
| 초반 사망이 줄어야 한다 | 30초 이전 사망 **0.000 → 0.000**(5·6회차 모두 바닥. 애초에 초반 사망이 없었다) |

→ **실제로 일어난 것은 "앞이 헐거워진 것"이 아니라 "판이 양쪽으로 늘어난 것"이다.**
인과는 하나로 설명된다: grunt는 `DAMAGE 10 ≥ HP 10`이라 **1발에 죽어 쌓이지 않는다** → 접촉 피해가
줄어 **생존이 길어진다.** 동시에 `grunt.XP 1 < brute.XP 3`이라 **처치당 XP가 줄어** 레벨업은 5로
그대로다 → 화력 성장이 안 빨라져 **보스 324를 여전히 못 깎는다** → 클리어율은 오히려 소폭 하락.
**한 방향의 이야기이고, "너무 쉬워졌다"가 아니다.**

**그리고 이 값은 원래 밴드 끝을 붙잡고 있었다.** 5회차 0.804는 상한 0.85의 **94.6% 지점**이다.
안전한 중앙에서 튕겨 나간 것이 아니라 **경계에 붙어 있던 값이 0.089 움직여 넘은 것**이며,
사망 판 n ≈ 109의 중앙값이라 클리어율(1 표준오차 이동)과 같은 정도의 노이즈를 포함한다.

**그럼에도 「지표가 경고하는 현상이 아니다」를 이유로 합격 처리하지 않는다.**
합격 구간은 절대선이고(§6), 0.893은 0.85 밖이다. **§6-B는 "기준을 낮춘 표"가 아니라
"기준에 못 미친 채로 낸다고 적은 표"**라는 정의가 여기서도 그대로 적용된다.

#### 6-B-2. **이 교환이 옳았는가 — 판정: 유지한다** (D58. 되돌릴 근거를 검토한 결과)

얻은 것: **서사 정합**(§5-A) + 연꽃 클리어율 **+0.040** + 키클롭스 초당 처치 **+0.127**(하한까지 거리
절반 이하). 잃은 것: 키클롭스 사망 생존시간 **1개** + 연꽃 초당 처치 악화(이미 미달이던 지표).
**미달 총계 7 → 7.** 순수 개수로는 무승부다. 그래서 **개수가 아니라 성격으로 판정한다.**

1. **두 결함의 관측 가능성이 다르다. 이게 결정적이다.**
   - 「연꽃 먹는 자들의 해안에 구혼자가 있다」 → 심사자가 **한 판을 켜면 즉시 본다.**
   - 「사망한 판의 생존시간 **중앙값**이 목표의 89.3%」 → **200판을 돌려야 보이는 분포 통계다.**
     한 판을 하는 심사자는 80.4초에 죽든 72.4초에 죽든 **차이를 느낄 수 없다.**
   → 이 교환은 **보이는 결함을 안 보이는 결함으로 바꾼 것**이다.
2. **되돌려도 서사 결함은 되돌아온다.** 사망 생존시간은 다른 축으로 고칠 여지가 원리적으로 있지만
   (지금은 동결이라 못 한다), **`spawnWeights`가 원래대로면 구혼자는 반드시 연꽃 해안에 있다.**
   한쪽만 대체 경로가 없다.
3. **되돌리는 행위 자체가 이 프로젝트의 방법론을 부순다.** D56-ⓒ가 **측정 전에** *"7회차는 결과와
   무관하게 없다"*를 못박았다. 결과가 마음에 안 든다고 되돌리면 D44·D47이 세운 「사전 등록 → 그대로
   집행」이 **한 번의 예외로 무효**가 된다. 그 일관성이 제출물 4번(AI 활용 기술 문서)에서
   *"감으로 밸런스를 맞추지 않았다"*를 뒷받침하는 **유일한 종류의 증거**다.
4. **되돌리는 비용이 8/8 밤에 감당할 수 없다.** `balance.ts` 복구 + 7회차 재측정 + D56·D57·§5-A·
   §6-A-2·§6-A-3·§6-B·§8-G 전면 재작성. 그걸 하고 얻는 것이 **집계 통계 1칸**이다.
   내일은 영상·PDF 2종이다(하네스 6).

**⚠️ 반대로, 되돌렸을 조건도 적어 둔다 — 이 판정이 무조건적이지 않다는 뜻이다.**
새 미달이 **클리어율**이었다면(예: 키클롭스가 0.35 밖으로 나갔다면) **되돌리자고 했을 것이다.**
클리어율은 §8-1(3종 각각 클리어 화면에 도달 가능)에 직결되고 심사자가 **한 판에 겪는** 지표다.
서사 정합과 맞바꿀 수 있는 것은 **집계 통계이지 플레이 가능성이 아니다.** 그 선을 여기 적어 둔다.

## 7. 봇 정책 (playtest 재현성 — 바꾸면 과거 측정값과 비교 불가)

- **이동**: 최근접 적 무리에서 이격 + 경험치 오브 회수. 반응 지연 300ms. 미래 스폰 위치는 예측하지 않는다.
- **3택**: 제시된 3개 중 `BOT.PICK_PRIORITY` 배열에서 인덱스가 가장 작은 것을 고른다.
  단 **HP < 50%면 넥타르를 1순위**, 아니면 최하위로 취급한다. **무작위 선택 없음.**
  순서: 쌍둥이 화살 → 청동 촉 → 빠른 손 → 가죽 갑옷 → 관통 화살 → 샌들 → 아테나의 가호 → 먼눈 → 넥타르.

### 7-A. 위협 감지 반경 `BOT.THREAT_RADIUS_RATIO` = 1/3 (문서에 없어서 tech가 임의로 정했던 값 — 이제 여기가 원본)

**감지 반경 = `WEAPON_BOW.RANGE × 1/3` = 140/3 ≈ 46.7px.** 이 반경 안의 적에게서만 이격한다.

- **사거리 140을 그대로 쓰지 않는 이유**: 그러면 봇이 거의 항상 도주 모드가 되어 경험치 오브 회수를
  영구 포기했다(레벨업 중앙값 0). 3택이 한 번도 안 뜨는 판을 측정해 봐야 이 게임을 측정한 게 아니다.
- **규칙으로 말하면**: **쏘는 거리와 피하는 거리를 분리한다.** 활 사거리 안이라고 다 도망치면
  "적을 끌어들여 사거리에 넣는다"는 이 게임의 유일한 위치 판단(§3)이 봇에게서 사라진다.
- **이건 밸런스 수치가 아니라 측정 도구의 일부다.** D10("봇 정책 = 재현성의 근거")의 연장이며,
  같은 구속을 받는다 — **바꾸면 과거 측정값과 비교 불가**이므로 바꿀 때는 전 시련 전 지표를 재측정하고
  변경 사실을 회차 계획(§6-A)에 회차로 명시한다. 조용히 만지는 것을 금지한다.
- 1회차 실측: 봇 이동 의사결정 중 **도주 66~72% / 오브 회수 17~19%**, XP 회수 효율 **45~61%**.
  playtest 판단은 "지금 스폰 밀도에선 도주가 합리적 선택이었고 측정 무효 사유가 아니다".

**~~2회차-A 이후에도 레벨업이 8 미만이면 이 값이 다음 조사 대상이다~~ → 이 경로는 닫혔다
(2026-08-08, 2회차 실측. D38).** 아래가 그 근거다.

| 판당(중앙값) | 키클롭스 | 연꽃 | 이타카 |
|---|---|---|---|
| 스폰 XP 총량 | 90.5 | **52.0** | 77.5 |
| 회수 XP 총량 | 43.0 | 20.0 | 36.5 |
| 회수 효율 | 50.8% | 37.1% | 49.1% |
| **회수 100%면 몇 레벨업?** | 7 | **5** | 7 |
| 실제 레벨업 | 4 | 3 | 4 |

**회수 효율을 100%로 만들어도 3종 전부 8레벨업(구 곡선 누적 99)에 못 닿는다.** 연꽃은 52 vs 99로
47% 부족이다. 즉 병목은 **회수가 아니라 공급**이고, `BOT.THREAT_RADIUS_RATIO`를 키워 도주 편향을
줄여도 상한이 그대로다 — **봇을 고쳐도 안 풀린다.** 같은 논리로 `PLAYER.PICKUP_RADIUS`도 빠진다.

부수 효과 둘: ⓐ D10(봇 정책 = 재현성의 근거)을 건드리지 않아 1·2회차와 계속 비교 가능하다.
ⓑ `PICKUP_RADIUS` 18의 근거("회수를 위해 적 쪽으로 들어가는 판단이 남을 만큼만")를 그대로 지킨다.
→ 축은 3회차의 `XP_CURVE.GROWTH`(공급 요구량)로 넘어갔다.

### 7-B. **위 D38의 제외 근거는 만료됐다 — 회수 경로가 되살아났다** (3회차 실측, 2026-08-08, D45)

**D38은 「회수 100%를 가정해도 미달」이라는 이유로 `PLAYER.PICKUP_RADIUS`를 조사 대상에서 뺐다.
그 계산은 구 곡선(8레벨업 요구 99) 기준이었고, 3회차에 곡선이 바뀌어(요구 58) 성립하지 않는다.**
이 사실을 지우지 않고 남긴다 — 지우면 다음 세션이 §7-A만 읽고 같은 경로를 다시 닫는다.

| 3회차 실측(연꽃) | 값 |
|---|---|
| 판당 스폰 XP | **57.0** ← 8레벨업 요구 58에서 **1** 모자란다 |
| 판당 회수 XP | **21.5** |
| 회수 효율 | **35.7%** (3종 최저. 키클롭스 46.9% / 이타카 49.5~51.0%) |
| 런 종료 시 미회수 잔량 | **65.8%** (3종 최고) |

회수 XP 21.5는 누적 L3(17)~L4(24) 사이에 걸리고 **실측 레벨업 중앙값 3과 일치**한다.
독립 계측 2개(킬-타입 diff / 런 종료 잔량 직접 관측)가 같은 결론으로 수렴했다.
→ **공급은 98%까지 따라왔고, 지금 연꽃의 병목은 회수다. 이건 확정 사실이다.**

**그런데도 5회차에 `PICKUP_RADIUS`를 고르지 않는다. 이유는 「안 풀린다」가 아니라 셋이다.**

1. **단독으로는 산술적으로 부족하다** — 공급 57.0 < 요구 58이라 **회수 효율 100%도 7레벨업**이다.
   회수는 병목이지만 **그 병목만 풀어서는 하한을 못 넘는다.**
2. **전역 축이라 다 맞은 지표를 깬다**(§6-A-1 표). 4회차에 키클롭스를 0.50에 맞춘 직후 전역 XP를
   올리면 키클롭스(상한까지 여유 0.20)와 이타카(클리어율 이미 합격)가 0.70 밖으로 밀릴 수 있다.
   **1종을 고치려 2종의 합격 지표를 거는 거래다.**
3. §1의 근거("회수를 위해 적 쪽으로 들어가는 판단이 남을 만큼만")를 훼손한다. 이 값에는 상한이 있다 —
   `BOT.THREAT_RADIUS_RATIO × RANGE` = **46.7** 이상이면 "도망칠 자리에서 줍는" 판단이 완전히 사라진다.

**왜 연꽃만 회수가 나쁜가는 인과가 증명되지 않았다.** playtest가 확인한 것은 상관관계까지다 —
회수 효율이 grunt 비중과 뚜렷하게 상관된다(이타카 grunt 100% → 50% / 키클롭스 70% → 47% /
연꽃 30% → 35.7%). "brute는 느리고 사거리 끝에서 죽어 봇이 도주하는 동안 오브가 버려진다"는
가설이 데이터와 맞지만 **가설이다.** 5회차 축(`enemyHpMul`)은 이 가설에 기대지 않는다 —
처치당 필요 발수라는 **직접 측정 가능한 양**을 줄인다.

## 8. 완료 기준 (verify가 O/X로 찍는다)

1. 3종 시련 각각을 브라우저에서 시작해 **클리어 화면과 사망 화면 양쪽에 도달할 수 있다.**
2. `TRIALS` 외에 시련 id로 분기하는 `if`/`switch`가 `src/` 전체에 **0개**다.
3. 클리어 판정이 `trialProgress()` **단일 함수 호출 1곳**에서만 이뤄진다.
4. 레벨업 3택 화면에서 제시된 3개의 `category`가 **서로 다르다**(후보 고갈 시 제외).
5. `src/sim/` 아래 Phaser import **0건** (`node scripts/check-boundary.mjs` exit 0).
6. 밸런스 수치 리터럴이 `src/config/balance.ts` 밖에 **0개**다.
7. `npm run sim`이 시련 3종 × 200회를 완주하고 §6의 8개 지표를 전부 출력한다.
8. §6의 8개 지표가 전부 합격 구간 안에 있다.
9. 한 판(클리어까지)의 실측 소요가 **60~120초**다.

### 8-A. HUD (브라우저에서 한 판을 켜고 O/X로 찍는다. 전부 §9-5의 앵커 위치)

| # | 항목 | O 판정 문장 |
|---|---|---|
| 10 | 경과/제한시간 | 상단 중앙에 `M:SS / 3:00` 형식이 있고, 초가 실제로 증가한다 |
| 11 | 난이도 스텝 | 스텝 pip 9칸이 있고, **경과 10초마다 정확히 1칸** 채워진다(`SPAWN.STEP_SEC`) |
| 12 | 처치 카운터 | 우상단 숫자가 적을 잡을 때마다 1씩 증가한다 |
| 13 | 시련 진행도 바 | 상단 중앙에 있고, **`trialProgress()` 반환값 ÷ `goal`을 그대로 그린다.** HUD가 진행도를 자체 계산하는 코드가 `src/` 전체에 **0곳**이다(§8-3 위반 방지) |
| 14 | 시련 시작 배너 | 판 시작 시 `TrialDef.name` + `tagline`이 화면 중앙에 뜨고 3초 안에 사라진다. **배너 표시 중에도 경과 시간이 흐른다**(일시정지 없음) |
| 15 | 획득 업그레이드 | 좌하단에 배지가 나열되고, 같은 업그레이드를 2회 고르면 배지 개수가 아니라 **스택 숫자가 2**가 된다 |
| 16 | HP 바 | 좌상단. 피격 시 즉시 줄고, 넥타르 선택 시 즉시 는다 |
| 17 | 레벨/XP 바 | HP 바 아래. 레벨업 순간 0으로 리셋되고 `Lv.` 숫자가 1 오른다 |
| 18 | fps 토글 | 기본 **비표시**. `F` 키로 켜고 끌 수 있다 |
| 19 | 음소거 토글 | `M` 키로 BGM이 꺼지고 켜지며, 토스트가 뜬다 |

### 8-B. 에셋 (§9)

| # | 항목 | O 판정 문장 |
|---|---|---|
| 20 | 폴백 | `public/sprites/`·`public/assets/`·`public/audio/`를 **전부 비운 상태**에서도 게임이 콘솔 에러 없이 **타이틀 화면 → 시련 시작 →** 클리어·사망까지 간다. 타이틀에서 `title-hero.png`·`arena.jpg`가 없어도 **제목·조작 안내·시련 3종 목록의 좌표가 한 픽셀도 움직이지 않는다**(§9-8) |
| 21 | 표시/히트박스 분리 | 표시 크기는 `VISUAL.SPRITE_PX`에서만 오고, 충돌 판정은 `PLAYER.RADIUS`/`ENEMIES.*.RADIUS`만 쓴다. 두 값을 곱하거나 대입하는 코드가 없다 |
| 22 | 가림 방지 | 적 30기 이상이 플레이어와 겹친 상태에서도 플레이어 스프라이트가 보인다(`VISUAL.DEPTH.PLAYER` > 모든 적) |
| 23 | 시련 시각 구분 | 시련 3종의 배경 색이 서로 다르고, 그 차이가 **`TrialDef.bgTint` 필드에서만** 온다. 시련 id로 배경을 고르는 분기가 0개다 |
| 24 | 용량 | `dist/` 빌드 산출물 중 에셋 총합이 `ASSETS.MAX_TOTAL_KB`(4000KB) 이하이고, 이미지만의 합이 `ASSETS.MAX_IMAGE_TOTAL_KB`(**1200KB**, D41), 오디오만의 합이 `ASSETS.MAX_AUDIO_TOTAL_KB`(2000KB) 이하다 |
| 25 | 출처 | `public/` 안의 생성 에셋 파일 각각이 `docs/ASSET_CREDITS.md` §1 표에 **행 1개로 존재하고, 그 행에 「(채울 것)」·「(미확인)」이 0개**다. **파생 파일은 §1-D 표에 행 1개로 존재하고 그 행이 원본 파일명을 가리킨다.** 반대로 표에 있으나 `public/`에 없는 파일은 §1-C에 제외 사유가 적혀 있다(행을 지우는 것은 금지) |
| 25-b | 에셋 개수 | `public/` 안의 이미지 중 **생성분이 `ASSETS.MAX_IMAGE_FILES`(5) 이하, 파생분이 `ASSETS.MAX_DERIVED_IMAGE_FILES`(2) 이하**다. 어느 쪽인지는 `ASSET_CREDITS.md` §1 / §1-D 중 어느 표에 있는지로 판정한다 |
| 26 | 포맷 | `public/sprites/`의 파일은 전부 **알파 채널이 있는 PNG**이고, `public/assets/arena.jpg`는 **알파가 없는 JPEG**다. 그 외 확장자의 생성 에셋이 `public/`에 0개다(오디오 `bgm.mp3` 제외) |

### 8-C. 1회차 반려 대응 (2026-08-08 추가)

| # | 항목 | O 판정 문장 |
|---|---|---|
| 27 | 조준 규칙 | `WEAPON_BOW.AIM`이 `'nearest'`와 `'bossFirstThenNearest'` **둘 다 동작**하고, 값을 바꾸는 것 외에 코드를 고칠 필요가 없다. `'bossFirstThenNearest'`에서 사거리 내 boss가 있는 틱의 발사는 **100% boss를 향한다** |
| 28 | 3택 화면 | 카드 3장이 §9-5-A 배치로 뜨고, 각 카드에 카테고리 배지 색이 **서로 다르게** 표시되며, 화면이 떠 있는 동안 경과 시간이 **증가하지 않는다** |
| 29 | 하네스 2 | `src/sim/` 전체에 밸런스 리터럴이 0개다. 특히 `metrics.ts`의 `30`은 `TARGET_METRICS.EARLY_DEATH_SEC`, `bot.ts`의 `/ 3`은 `BOT.THREAT_RADIUS_RATIO`를 참조한다 |
| 30 | 회차 기록 | `docs/` 어딘가에 §6-A 표의 **각 회차(2회차-A / 2회차-B / 3회차 / 4회차 / 5회차) 측정 결과가 회차마다 따로** 남아 있다. 두 회차의 변경이 한 표에 섞인 기록은 불합격 |
| 30-b | 무동작 실증 | **4회차** 측정의 연꽃·이타카 8지표가 3회차와, **5회차** 측정의 키클롭스·이타카 8지표가 4회차와 **각각 동일**하다(§6-A-1). 다르면 밸런스가 아니라 「서로소 시련 집합」 가정이 깨진 것이므로 **원인을 먼저 보고한다** |
| 30-c | 확정 미달 기록 | 동결 시점(5회차 직후)에 합격 구간 밖인 지표가 **전부 §6-B 표에 행으로 존재**한다. §6의 합격 구간 표는 **1회차부터 한 글자도 바뀌지 않았다**(git diff로 확인 가능) |

**항목 31~34(타이틀 화면)는 §9-8 끝의 표에 있다.** 번호는 이어지며 판정 방식은 같다.

### 8-D. 렌더 해상도 분리 (§9-9. 2026-08-08 — 사용자가 "캐릭터 화질이 나쁘다"고 지적)

**전부 브라우저 실물을 눈으로 보고 O/X를 찍는다.** 헤드리스로는 판정할 수 없는 항목이다.

| # | 항목 | O 판정 문장 |
|---|---|---|
| 35 | 스프라이트 선명도 | 게임 화면을 **정지시키지 않은 채** 플레이어·grunt·brute·boss를 본다. 각 스프라이트의 윤곽에 **한 변이 3물리픽셀인 정사각 계단이 보이지 않는다.** 같은 창에서 변경 전 화면과 비교했을 때 세부(활·모자·단안)가 더 보인다 |
| 36 | **HUD 한글** | 좌상단 `Lv.`·우상단 `처치`·시련 시작 배너의 **한글 획이 3픽셀 두께의 블록으로 뭉개져 있지 않다.** 8px 폰트가 24 물리픽셀로 **래스터화**된 것이지 8px을 3배 확대한 것이 아니어야 한다 — 두 경우는 획 끝의 곡선 유무로 구분된다. 3택 카드 이름(10px)·타이틀 제목(18px)도 같은 기준 |
| 37 | 월드 뷰 불변 | 변경 전후로 **화면에 보이는 월드 영역이 같다.** 판정: 플레이어를 아레나 좌상단 구석에 붙였을 때 보이는 벽의 위치가 같고, 활 사거리(140) 원이 화면 폭의 약 29%를 차지한다. **적이 더 많이/적게 보이면 불합격**(= D14가 깨진 것) |
| 38 | 창 크기 대응 | 브라우저 창을 **백킹스토어(480R×270R)보다 작게** 줄여도 캔버스가 창 밖으로 넘치지 않고, 그 상태에서 HUD 한글이 읽힌다(`SCREEN.SMOOTH_ON_DOWNSCALE`). 창을 다시 키우면 35·36이 그대로 O다 |
| 39 | 용량 실측 | R 반영 후 `dist/` 이미지 합계가 `ASSETS.MAX_IMAGE_TOTAL_KB`(1200) 이하다. **실측 바이트를 숫자로 보고한다** — 추정으로 O를 찍지 않는다(§8-24와 같은 항목이나, 이번엔 값이 추정이라 실측이 필수다) |

### 8-E. 표시 크기 개정 (§9-10. 2026-08-08 — 사용자가 "캐릭터가 작다"고 두 번 지적)

| # | 항목 | O 판정 문장 |
|---|---|---|
| 40 | 크기 서열·하한 | `VISUAL.SPRITE_PX`가 **grunt 20 < brute 30 < player 34 < boss 56**이고, 4개 전부 대응 히트박스 지름(8/8/12/28)보다 크다. `PLAYER` 값이 `PLAYER.PICKUP_RADIUS × 2`(36) 이하다 |
| 41 | 전제 일치 | `SPAWN.MAX_ALIVE` 주석과 `VISUAL.SPRITE_PX` 주석이 **같은 화면당 마릿수(54)**를 쓴다. `balance.ts` 안에 "화면당 22마리" 또는 "90마리 전부 화면"을 근거로 삼는 문장이 **0개**다 |
| 42 | 실물 크기 | 브라우저에서 한 판을 켜 **플레이어가 brute보다 크게 보이고**, 적이 20기 이상인 상황에서도 배경(`bgTint`)의 색이 시련별로 구분된다. 스프라이트 텍스처는 `표시 크기 × 3`(102/60/90/168)이라 §8-35(계단 없음)가 그대로 O다 |

### 8-F. 3택 카드 내용 (§9-5-A 개정. 2026-08-08 — 사용자가 "3택 화면이 구리다"고 지적)

**43·44는 브라우저 실물, 45는 `git diff`로 찍는다.** §6 지표에 영향을 주는 항목이 하나도 없다.

| # | 항목 | O 판정 문장 |
|---|---|---|
| 43 | 카드 내용 | 한 판을 켜서 레벨업 3택을 띄우고, 뜬 카드의 **이름 / 효과 / 현재→다음 / 레벨** 4줄이 §9-5-A ⓒ-1 정답표의 문자열과 **글자 그대로** 일치한다(초기 스탯일 때). 이름이 2행으로 접히는 카드가 0장이다 |
| 44 | 무제한·호버·암막 | ⓐ `NECTAR`가 뜬 카드의 레벨 줄이 `Lv.n (무제한)`이고 `/ 0`이 아니다. ⓑ 세 카드 각각에 마우스를 올리면 **테두리가 굵어지고 배경색이 바뀌며**, 커서를 빼면 원래대로 돌아온다. 크기가 변하는 카드는 0장이다. ⓒ 3택이 뜬 동안 뒤의 적 실루엣이 **여전히 보이되** 카드 글자가 배경보다 확실히 밝다 |
| 45 | 리터럴 0개 / 밸런스 불변 | ⓐ `Hud.ts`의 3택 코드에 좌표·폰트·색·문자열 리터럴이 0개이고 전부 `HUD.CHOICE_CARD`·`VISUAL.COLOR`·`UPGRADE_STAT_DISPLAY`에서 온다(§8-6). ⓑ `git diff`에서 `UPGRADE_POOL`의 `id`/`category`/`maxLevel`/`stat`/`mode`/`value`가 **한 글자도 안 바뀌었다**. ⓒ `WEAPON_BOW`·`ENEMIES`·`SPAWN`·`XP_CURVE`·`TRIALS`·`BOT`·`TARGET_METRICS`도 동일 |

## 9. 시각·오디오·HUD 규격 (렌더 축 — §6 지표에 영향을 주지 않는다)

> **이 절의 어떤 값도 sim이 읽지 않는다.** 그래서 여기를 고쳐도 밸런스 재측정이 필요 없다.
> 히트박스는 §3·`ENEMIES.*.RADIUS` 그대로이며, 표시 크기와 **분리**돼 있다.
> 수치 원본은 `balance.ts`의 `SCREEN` / `VISUAL` / `HUD` / `AUDIO` / `ASSETS`.

### 9-1. 미술 양식 — 고대 그리스 **적색상(red-figure) 도기화**

어두운 바탕 + 밝은 황토색 인물. 흑색상(밝은 바탕 + 검은 실루엣)을 쓰지 않는 이유는
`SPAWN.MAX_ALIVE = 90`이기 때문이다. 밝은 바탕 위의 검은 실루엣 90개는 한 덩어리로 뭉친다.

**색 규칙(이게 90마리 식별의 유일한 장치다): 플레이어·내가 낸 것 = 차가운 색(청록·백금) /
적 = 따뜻한 색(적갈·주황·흙자주).** 색상환 반대편에 배치한다.

**카메라 월드 뷰 480×270은 동결**이다(D14). 이 문서 수치 근거의 절반이 화면 대비
비율이라(사거리 140 = 폭의 29%, 보스 반경 14 = 높이의 5%) 바꾸면 밸런스를 전부 다시 재야 한다.
→ **렌더 픽셀 밀도는 이것과 분리돼 있다(`SCREEN.SUPERSAMPLE`, §9-9).** D14가 동결한 것은
"카메라가 보여 주는 월드 영역"이지 "물리 픽셀 수"가 아니다.

### 9-2. 생성 에셋 6개 (이게 전부다 — §2 상한)

**표시 크기는 월드 단위, 배포 규격은 물리 픽셀이다.** 배포 규격 = 표시 크기 × `SCREEN.SUPERSAMPLE`(R=3).
**R을 바꾸면 배포 규격 칸만 바뀌고 표시 크기는 불변**(§9-9).
「원본」 칸은 **ffprobe 실측값**이다(`ASSET_CREDITS.md` §1의 "해상도 근거"). 프롬프트에 쓴 요청 캔버스와 혼동하지 않는다.

| 우선 | 파일(배포 산출물) | 원본(실측) → 배포 규격(R=3) | 내용 | 표시 크기(월드) |
|---|---|---|---|---|
| ① | `public/sprites/player.png` | 2560×2560 투명 → **투명 PNG 102×102** | 오디세우스(활 + 원뿔 필로스 모자), 청록·백금 | **34** |
| ② | `public/sprites/grunt.png` | 2560×2560 투명 → **투명 PNG 60×60** | **달려드는 자.** 마르고 빠름. 주황·적갈. 이타카에서는 구혼자, 동굴에서는 이름 없는 것들(§5-A) | **20** |
| ③ | `public/assets/arena.jpg` | 2560×1440 불투명 → **2880×1620 JPEG(알파 없음)** | **아레나 전체 1장**(타일 아님). `ARENA` 960×540을 정확히 덮어 이음매가 없다. 어둡고 저대비 | **1.0배** |
| ④ | `public/sprites/boss.png` | 2560×2560 투명 → **투명 PNG 168×168** | 폴리페모스. 뼈색 + 단안 호박색 발광 | **56** |
| ⑤ | `public/audio/bgm.mp3` | — | 루프 BGM 1곡, 60초 목표(최대 90초) | 음량 0.5 |
| ⑥ | `public/sprites/brute.png` | 2560×2560 투명 → **투명 PNG 90×90** | 로토파고이. 넓고 둔중. 흙적·자주. **연꽃 해안 전속**(§5-A) | **30** |

**포맷 규칙 — "투명 PNG"는 스프라이트 4종에만 적용된다. 배경 1장은 명시적 예외로 불투명 JPEG다.**
(D25) 근거 둘: ⓐ 배경은 아레나 전체를 덮으므로 **투명 픽셀이 원리적으로 없다.** ⓑ 사진 계열 석재
텍스처라 무손실 PNG가 노이즈를 그대로 저장한다 — 같은 원본 실측으로 PNG 720.8KB vs JPEG 26.7KB(27배).
**이 예외는 배경 1장뿐이다.** 스프라이트를 JPEG로 바꾸면 알파가 사라져 배경 위에 사각형이 뜬다.

**배포 규격의 원칙(D25에서 이어짐):** **재표본은 오프라인에서 끝내고, 런타임은 1 텍셀 = 1 물리 픽셀일 때만 쓴다.**
렌더러가 `pixelArt: true`(NEAREST)라 런타임 확대·축소는 곧 픽셀 중복·소실이기 때문이다.
바뀐 것은 그 1:1이 성립하는 픽셀 수뿐이다 — 백킹스토어가 480R×270R, 카메라 줌이 R이 되면서
**1 월드 단위 = R 물리 픽셀**이 됐다(§9-9). 그래서 배포 규격이 전부 R배다.
`ARENA_IMAGE_SCALE` 1.0은 **"배경이 아레나 전체를 덮는다"는 월드 단위 뜻**이며 텍스처 크기와 무관하다.

**arena만은 정직하게 적어 둔다**: 원본이 **2560×1440**이라 2880×1620은 **1.125배 업스케일**이고
그 마지막 12.5%에는 새 디테일이 없다. 그래도 R배로 내보내는 이유는 그 12.5%가 아니라
**960→2560 구간의 실디테일 2.67배**이며, 동시에 **런타임 NEAREST 비정수배가 만드는 계단의 제거**다.
배경은 애초에 "어둡고 저대비"로 설계돼 디테일 손실이 의도된 쪽이다. 용량이 걸리면 §9-9의 되돌리기 순서를 따른다.

**이 표는 「새로 생성한 것」만 센다.** 같은 원본에서 다시 뽑은 **파생 파일은 §9-8·`ASSET_CREDITS.md` §1-D**에 있고
`ASSETS.MAX_DERIVED_IMAGE_FILES`(2)로 따로 통제한다(D35). 현재 파생 1장: `public/sprites/title-hero.png`.

- **뒤 순번이 끝내 도착하지 않아도 게임은 완성 상태다**(§8-20 폴백). tech는 에셋을 기다리지 않는다.
- 화살·경험치 오브·3택 아이콘은 **생성하지 않는다.** 12px 이하에서 손그림과 코드 도형이 구분되지 않는다.
- 표시 크기 근거: **화면당 실제 마릿수 54**(= 실측 동시 최대 적 p95의 최댓값)에서의 화면 점유율.
  가장 무거운 판(연꽃)이 31.2%로 상한 1/3 안에 들어온다. **자세한 계산과 옛 근거(「90마리 전부 화면」,
  grunt 13.6%)가 틀렸던 이유는 §9-10.**
- **배경 화질/용량 문제의 되돌리기 순서는 §9-9로 옮겼다.** ~~무손실 PNG 폴백~~ 조항은 **폐기됐다(D41)** —
  2880×1620 무손실 PNG는 약 6MB라 어떤 상한에도 안 들어간다. 남은 경로는 **① JPEG 품질↑ →
  ② arena만 1920×1080으로 되돌리고 런타임 1.5배 허용** 둘뿐이다.

### 9-3. 시련 3종의 시각 구분 = `TrialDef.bgTint` **한 필드**

배경 이미지는 3종 공통 1장이고, 시련마다 곱연산 틴트만 다르다.
시련별 배경 이미지를 만들면 에셋이 3배가 되고 씬에 시련 id 분기가 생겨 §8-2를 깬다.

| 시련 | bgTint | 성격 |
|---|---|---|
| 키클롭스의 동굴 | `0x8090b8` | 차가운 청회색. 가장 어둡다(≈0.62) — 보스의 호박색 단안이 유일한 발광원이 된다 |
| 연꽃 먹는 자들의 해안 | `0xc8c090` | 바랜 모래빛. 가장 밝다(≈0.76) — 흙적·자주 brute가 배경에서 떨어진다 |
| 이타카의 홀 | `0xc08870` | 횃불빛 적동색(≈0.72). 주황 grunt와 색이 가까우므로 채도를 낮춘다 |

각 채널 **≥ 0x70**을 지킨다. 그 아래는 배경이 검게 뭉쳐 적 90마리의 실루엣 경계가 사라진다.

### 9-4. 3택 아이콘 = 코드 도형 배지 (PNG 아님)

카테고리 3색 정사각 배지 + 스택 수 숫자. `VISUAL.COLOR.CAT_FIRE/LIFE/UTIL`이 `UPGRADE_CATEGORIES`와 1:1.
아이콘 9장을 그리면 에셋이 6→15개가 되고, 그 9장이 12px에서 서로 구분되지도 않는다.
**3택 카드는 이름(한글 텍스트)이 의미를 나른다.** 배지는 카테고리와 스택 수만 나른다.

### 9-5. HUD 배치 (480×270 기준. 화면 좌표 고정, 카메라를 따라가지 않는다)

```
 ┌───────────────────────────────────────────────────────┐
 │ [HP바 6,6 84x6]        01:12 / 3:00        처치 37 →│  y 0–12
 │ [Lv/XP 6,15 84x4]   [ 진행도바 140x5 43/108 ]        │  y 12–20
 │                     ▮▮▮▮▯▯▯▯▯ STEP 4                 │  y 20–26
 │                                                       │
 │                  (플레이 영역 y 30–250)               │
 │                                                       │
 │                                        BGM OFF →     │  y 246
 │ [배지][배지][배지] …                          fps 60 →│  y 252–264
 └───────────────────────────────────────────────────────┘
```

- **진행도 바는 `trialProgress()` 반환값을 그대로 그린다.** HUD가 진행도를 따로 계산하면 §8-3
  ("클리어 판정은 단일 함수 1곳") 위반이다. 표시값 = `trialProgress(...) / goal`.
- **"웨이브 타이머"는 이 게임에 없다.** 웨이브 개념 자체가 없고(§4) 압력은 10초=1스텝 곡선뿐이다.
  존재하지 않는 개념을 화면에 띄우지 않는다 → **난이도 스텝 pip + 경과/제한시간**이 그 자리를 대신한다.
- 스텝 pip는 9칸(= 목표 판 길이 90초). 9스텝을 넘으면 칸은 가득 찬 채 옆 숫자만 올라간다.
- 본문 폰트 최소 **8px(논리)**. 6px 한글은 정수배 스케일에서도 획(1px)이 소실된다(카피바라 실측).
  **폰트 파일은 도입하지 않는다** — 시스템 sans-serif만 쓴다.
  **§9-9 도입 후 조건**: 모든 폰트 수치는 논리 px이고 **실제 래스터화는 논리 px × R**로 해야 한다.
  8px로 그린 뒤 3배 확대하면 개선이 0이다(= 지금과 글자 하나까지 동일). 판정은 §8-36.
- 시련 시작 배너는 `name` + `tagline`을 2.5초 표시 후 0.5초 페이드. **게임 시간은 배너 중에도 흐른다.**
  일시정지를 넣으면 배너를 모르는 sim과 브라우저의 경과 시간이 달라져 §6 측정이 무효가 된다.

### 9-5-A. 레벨업 3택 화면 — **카드 내용 규격 (2026-08-08 개정. D51~D53)**

이 게임의 **유일한 플레이어 결정**이 뜨는 화면이다.

**개정 사유(사용자 지적)**: 카드에 `관통 화살` + `Lv.1`밖에 없어서 **그게 무엇을 얼마나 바꾸는지,
지금 내 값이 얼마인지, 몇 렙이 상한인지가 전부 없었다.** D6이 3택을 "유일한 플레이어 결정"으로
정의해 놓고 그 결정을 깜깜이로 시키고 있었다. 카드 가운데가 비어 보인 것은 디자인이 아니라
**들어갈 정보가 빠져 있었기 때문**이다.

```
 ┌───────────────────────────────────────────────────────┐
 │              (게임 화면이 어둡게 깔린다 0.80)         │
 │                    LEVEL  7                           │  y 68  12px
 │  ┌──────────┐   ┌──────────┐   ┌──────────┐          │
 │  │▪ 화력    │   │▪ 생존    │   │▪ 유틸    │          │  y 90  (카드 상단)
 │  │          │   │          │   │          │          │
 │  │ 관통 화살│   │ 가죽 갑옷│   │헤르메스의│          │  +34   10px  이름
 │  │추가 관통+1│  │최대 체력+20│ │이동 속도+12%│       │  +49    9px  효과(카테고리 색)
 │  │  0 → 1   │   │ 100 → 120│   │  60 → 67 │          │  +62    8px  현재→다음
 │  │          │   │          │   │          │          │
 │  │ Lv.1 / 3 │   │ Lv.1 / 4 │   │ Lv.1 / 4 │          │  +78    8px  레벨/상한
 │  └──────────┘   └──────────┘   └──────────┘          │  y 180 (카드 하단)
 └───────────────────────────────────────────────────────┘
```

- 카드 3장, **가로 중앙 정렬**. 좌표·폰트·색은 전부 `HUD.CHOICE_CARD`와 `VISUAL.COLOR`에서 온다.
  **씬에 좌표·폰트·색 리터럴이 0개여야 한다**(§8-6). 지금은 `Hud.ts`에 `+8`·`+4`·`+34`·`card.H - 14`·`16`이 박혀 있다.
- **입력은 마우스 클릭 1종.** 키보드 단축키는 만들지 않는다(§2 조작 축 상한, 기존 그대로).
- **이 화면이 떠 있는 동안 게임 시간은 멈춘다**(§3-B-2). 시련 시작 배너와 반대다.

#### ⓐ 효과 설명은 **쓰지 않고 조립한다** — `stat` + `mode` + `value`에서 생성

**`UpgradeDef`에 설명 문자열 필드를 추가하지 않는다.** 표시 문자열은 이미 있는 3개 필드와
**stat별 라벨/단위 표 하나**(`UPGRADE_STAT_DISPLAY`, balance.ts)에서 전부 유도한다.

> **D16의 「설명 문장을 만들지 않는다」와 충돌하지 않는다.** D16이 거절한 것은 **9종 × 손으로 쓴
> 설명문**이고, 그 근거는 *"8px에서 안 읽히고 번역 유지 비용만 는다"*였다. 조립 방식은
> ① 업그레이드당 새로 쓰는 텍스트가 **0개**라 유지 비용이 늘지 않고(라벨 표는 stat 9개뿐이며
> 업그레이드가 아니라 **스탯**에 붙는다), ② 결과가 문장이 아니라 **`라벨 + 증분` 8글자 이하**라
> 8px 가독 근거에도 걸리지 않는다. ③ 무엇보다 **수치를 바꾸면 설명이 자동으로 따라온다** —
> 손으로 쓴 설명문은 `value`를 고칠 때마다 어긋나고, 그 어긋남은 마감 전날에 아무도 못 잡는다.

**줄 구성 (이름·효과·현재→다음 3줄. 네 번째 줄인 레벨/상한은 ⓒ에서 따로 정한다)**

| 줄 | 내용 | 폰트 | 색 |
|---|---|---|---|
| 이름 | `UpgradeDef.name` 그대로. **1행 고정**(줄바꿈 금지) | 10px | `COLOR.TEXT` |
| 효과 | `{라벨} {증분}` | 9px | **카테고리 색**(`CAT_FIRE/LIFE/UTIL`) |
| 현재→다음 | `{현재값}{단위} → {다음값}{단위}` | 8px | `COLOR.TEXT_DIM` |

- **이름이 1행 고정인 것은 카드 폭의 판정 조건이다** — 9종 중 가장 긴 `헤르메스의 샌들`이 10px에서
  카드 안쪽 폭(`W - PAD×2` = 104)에 들어가야 한다. 넘치면 폭을 늘리는 게 아니라 이름을 줄인다.
- **효과 줄에 카테고리 색을 쓰는 이유**: 배지 색과 같은 색을 카드 안에서 한 번 더 반복하면
  카드가 카테고리 단위로 묶여 읽힌다. 새 색 상수가 0개라는 것도 이유다.
- 이름 > 효과 > 현재→다음의 **폰트 서열이 정보 서열이다**(D16: 의미는 이름이 나른다 — 유지).

**stat 9종의 라벨·단위·소수 자릿수** (원본은 `UPGRADE_STAT_DISPLAY`)

| stat | 라벨 | 단위 | 소수 | 낮을수록 좋음 |
|---|---|---|---|---|
| `damage` | 화살 위력 | — | 1 | 아니오 |
| `projectiles` | 동시 발사 | 발 | 0 | 아니오 |
| `cooldown` | 발사 간격 | ms | 0 | **예** |
| `pierce` | 추가 관통 | — | 0 | 아니오 |
| `maxHp` | 최대 체력 | — | 0 | 아니오 |
| `iframeMs` | 무적 시간 | ms | 0 | 아니오 |
| `healPct` | 즉시 회복 | % | 0 | 아니오 |
| `moveSpeed` | 이동 속도 | — | 0 | 아니오 |
| `pickupRadius` | 흡수 반경 | — | 0 | 아니오 |

- **단위를 최소로 쓴다.** 시간(ms)과 발수(발)만 붙인다 — 그 둘은 단위 없이는 무엇의 수인지
  모른다. 나머지(위력·체력·속도·반경)는 라벨이 이미 단위 역할을 하고, 8px 한 줄에서 `px/s` 같은
  꼬리표는 폭만 먹는다.
- **`cooldown`의 라벨이 「발사 간격」인 것은 규칙이다.** 낮을수록 좋은 stat은 **라벨 자체에 방향이
  들어 있어야 한다** — 「간격」은 짧을수록 좋다는 게 상식이라 `-15%`가 손해로 읽히지 않는다.
  ("공격 속도"로 뒤집어 `+17.6%`로 적는 안은 **기각**한다. `value`가 -0.15인데 화면에 +17.6%가
  뜨면 `balance.ts`와 화면의 숫자가 달라지고, 그건 밸런스를 바꾼 것처럼 보인다.)
- 낮을수록 좋은 stat에 색·기호를 따로 쓰지 않는다. 축을 하나 더 만들지 않는다.

#### ⓑ 표기 규칙 — `mode`별로 기계적으로 결정된다

| mode | 효과 줄 증분 | 현재→다음 줄 |
|---|---|---|
| `add` | `+{value}{단위}` | `{현재}{단위} → {현재 + value}{단위}` |
| `mulPct` | `{부호}{절댓값(value)×100}%` | `{현재}{단위} → {현재 × (1+value)}{단위}` |
| `pct` | `{value×100}%` | `HP {현재hp} → {min(maxHp, hp + maxHp×value)}` |

**소수점 규칙 (이게 `mulPct`의 까다로운 부분이다)**

1. 표시값은 위 표의 **소수 자릿수에서 반올림**한다. `damage`만 1자리, 나머지 8종은 0자리.
2. **반올림 후 소수부가 0이면 소수점을 지운다.** `12.0` → `12`.
3. **이 반올림은 표시에서만 일어난다. sim은 반올림하지 않는다.** 화면의 `12.5`는 내부값
   `12.5`이고 다음 단계는 `15.6`으로 보이지만 내부는 `15.625`다. **화면 숫자를 이어 곱해서
   검산하면 어긋난다 — 그건 버그가 아니라 표시 반올림이다.**
4. `damage`만 소수 1자리인 이유: 초기값 10에 +25%면 **12.5**라 정수로 자르면 12가 되어
   `+25%`와 화면이 어긋나 보인다. 나머지 8종은 초기값이 커서(550·600·100·60·18) 0자리로도
   증분이 1 이상 움직인다 — 자릿수를 늘려도 정보가 늘지 않는다.

**`pct`(넥타르)만 다른 이유**: 회복은 **누적되는 스탯이 없다.** `healPct`는 `PlayerRuntime`에 대응
필드가 없고(`src/sim/upgrades.ts`가 즉시효과로 분기한다) "현재 회복력 → 다음 회복력"이라는 축이
존재하지 않는다. 그래서 이 카드만 **지금 이 순간의 HP가 얼마에서 얼마가 되는지**를 보여 준다 —
넥타르를 고를지 말지의 판단 재료는 정확히 그것 하나뿐이고, 3택 중에는 시간이 멈춰 있어(§3-B-2)
그 값이 고르는 사이에 변하지 않는다. **상한 clamp(`min(maxHp, …)`)를 표시에 반영한다** — HP가
80/100일 때 `HP 80 → 100`이라고 떠야 "지금 고르면 20밖에 못 받는다"가 화면에서 읽힌다.

#### ⓒ 레벨/상한 줄 — `NECTAR`(무제한)의 표기

| 조건 | 표기 | 예 |
|---|---|---|
| `maxLevel > 0` | `Lv.{다음 레벨} / {maxLevel}` | `Lv.1 / 3` |
| `maxLevel === 0` | `Lv.{다음 레벨} ({UNLIMITED_LABEL})` | `Lv.3 (무제한)` |

- `다음 레벨` = 현재 스택 + 1. **`다음 레벨 ≤ maxLevel`은 항상 성립한다** —
  `isEligible()`(`src/sim/upgrades.ts`)이 만렙을 후보에서 제외하므로 `Lv.4 / 3`은 구조적으로 못 뜬다.
- 무제한을 `Lv.3 / ∞`로 쓰지 않는 이유: `∞`는 시스템 sans-serif에서 8px일 때 `oo`로 뭉개진다.
  한글 3글자가 8px에서 더 확실하고, 카드 폭에도 들어간다.
- **`/ {maxLevel}`을 넣는 것이 이 줄의 개정 핵심이다.** 상한을 모르면 "이걸 더 고를 수 있나"를
  판단할 수 없고, 그 판단이 없으면 3택이 후반에 무의미해진다.

#### ⓒ-1. **9종 전부의 결과 문자열** (판정용 정답표. 초기 스탯 · 스택 0 · HP 43/100 기준)

이 표가 §8-43의 O/X 판정 기준이다. tech는 이 문자열이 **글자 그대로** 나오게 만든다.

| id | 이름 | 효과 줄 | 현재→다음 줄 | 레벨 줄 |
|---|---|---|---|---|
| `SHARP_BRONZE` | 청동 촉 | `화살 위력 +25%` | `10 → 12.5` | `Lv.1 / 5` |
| `TWIN_SHOT` | 쌍둥이 화살 | `동시 발사 +1발` | `1발 → 2발` | `Lv.1 / 3` |
| `SWIFT_HAND` | 빠른 손 | `발사 간격 -15%` | `550ms → 468ms` | `Lv.1 / 4` |
| `PIERCING` | 관통 화살 | `추가 관통 +1` | `0 → 1` | `Lv.1 / 3` |
| `HIDE_ARMOR` | 가죽 갑옷 | `최대 체력 +20` | `100 → 120` | `Lv.1 / 4` |
| `ATHENA_GRACE` | 아테나의 가호 | `무적 시간 +200ms` | `600ms → 800ms` | `Lv.1 / 3` |
| `NECTAR` | 넥타르 | `즉시 회복 40%` | `HP 43 → 83` | `Lv.1 (무제한)` |
| `HERMES_SANDAL` | 헤르메스의 샌들 | `이동 속도 +12%` | `60 → 67` | `Lv.1 / 4` |
| `WIDE_EYE` | 먼눈 | `흡수 반경 +40%` | `18 → 25` | `Lv.1 / 3` |

**만렙까지의 전개(반올림 검산용. `mulPct` 4종만 적는다 — 나머지는 가산이라 자명하다)**

| id | 값 전개 |
|---|---|
| `SHARP_BRONZE` | 10 → 12.5 → 15.6 → 19.5 → 24.4 → **30.5** (5렙) |
| `SWIFT_HAND` | 550 → 468 → 397 → 338 → **287** ms (4렙) |
| `HERMES_SANDAL` | 60 → 67 → 75 → 84 → **94** (4렙) |
| `WIDE_EYE` | 18 → 25 → 35 → **49** (3렙) |

**표시값을 이어 곱하면 내부값과 어긋난다**(ⓑ-3). 실제로 갈리는 예가 `SWIFT_HAND`다 —
표시값 경로는 `468 × 0.85 = 397.8` → **398**이지만 내부 원값은 `550 × 0.85² = 397.375` → **397**이다.
그래서 tech는 **항상 내부 원값에서 한 번만 반올림한다**(누적 반올림 금지). 표의 값이 정답이다.

#### ⓓ 카드 규격 — `HUD.CHOICE_CARD` (높이 78 → 90, Y 96 → 90)

내용이 1줄에서 4줄로 늘었으므로 높이를 다시 잡았다. **폭 120과 간격 12는 안 바꾼다** —
120×3 + 12×2 = 384로 480 화면에서 좌우 여백 48씩 정확히 중앙 정렬된다는 기존 근거가 그대로고,
9종 이름과 가장 긴 현재→다음 줄(`550ms → 468ms`)이 안쪽 폭 104에 들어간다.

| 값 | 새 값 | 근거 |
|---|---|---|
| `H` | **90** | 위에서부터 배지 12 + 이름 10 + 효과 9 + 현재→다음 8 + 레벨 8, 행간과 상하 패딩 8을 더한 합 |
| `Y` | **90** | 카드가 90~180을 차지 → 세로 중심 135 = 화면(270) 정중앙. 상단 HUD(y≤26)·배지 줄(y 252)과 안 겹친다 |
| `TITLE_Y` | **68** | 카드 상단 90에서 22 위. 12px 헤더가 62~74를 차지해 카드와 16px 떨어진다 |
| `EFFECT_FONT_PX` | **9**(신설) | 이름 10 > 효과 9 > 보조 8. `FONT_MIN_PX`(8) 위 |

**카드 내부 오프셋도 전부 상수로 뺀다**: `PAD` 8 / `BADGE_DY` 8 / `CAT_LABEL_DX` 4(가로) /
`NAME_DY` 34 / `EFFECT_DY` 49 / `DELTA_DY` 62 / `LEVEL_DY` 78. 세로 오프셋은 **글자 중심** 기준이고
`BADGE_DY`만 배지 **상단** 기준이다(배지는 사각형이라 상단이 자연스럽다).
지금 `Hud.ts`에 박혀 있는 `+8`·`+4`·`+34`·`card.H - 14`·`16`이 §8-6 위반이라 같이 정리한다.

#### ⓔ 암막 `CHOICE_DIM_ALPHA` 0.65 → **0.80** (근거는 폐기하지 않고 유지한다)

사용자 지적: *"뒤의 적들이 다 보여 산만하다."* 타당하다. 그러나 0.65의 근거
(*"3택 중에도 적 실루엣이 남아야 한다"*)는 **폐기하지 않는다.** 지금 얼마나 포위됐는지가
화력/생존 선택의 유일한 판단 재료이기 때문이다(§9-5-A 기존 조항).

**두 요구가 충돌하지 않는 이유 — 암막은 곱연산이다.**
검정 오버레이는 적과 배경의 밝기를 **같은 비율로** 낮춘다. 즉 둘의 **밝기 비(= 실루엣 경계)는
거의 보존되고 절대 밝기만 내려간다.** 실루엣은 남고, 카드와의 시선 경쟁만 사라진다.
0.65 → 0.80이면 배경 밝기는 원본의 35% → **20%**가 되고, 카드는 불투명이라 그대로다.

- **1.0으로 올리지 않는다.** 그러면 3택이 게임 상황과 무관한 메뉴가 되고 위 근거가 진짜로 죽는다.
- **§9-8이 적어 둔 「세 화면의 밝기가 서로 다르다」는 그대로 성립한다**: 게임 **0** / 3택 **0.80** /
  타이틀 **0.75**. 규칙은 "서로 다르다"이지 "이 순서"가 아니다 — 타이틀은 `arena.jpg` 질감을
  보여 주는 것이 목적이고, 3택은 그 반대(뒤를 조용히 시키는 것)라 방향이 다른 게 정상이다.

#### ⓕ 호버 강조 — 클릭 전용 입력에서 유일한 피드백

지금은 마우스를 올려도 아무 변화가 없다. 입력이 클릭 1종(§2)이라 **호버가 유일한 상태 표시**다.

| | 기본 | 호버 |
|---|---|---|
| 테두리 | 1px, 카테고리 색 | **2px**, 카테고리 색 |
| 배경 | `COLOR.CHOICE_BG` | **`COLOR.CHOICE_BG_HOVER`** |

- **크기 확대(scale)·애니메이션을 쓰지 않는다.** 비정수 배율은 `SCREEN.SUPERSAMPLE`(§9-9)이
  성립시킨 **1 텍셀 = 1 물리 픽셀**을 깨서 카드 텍스트에 계단·흐림을 만든다. 그 근거를 호버
  연출로 무너뜨릴 이유가 없다. 색·두께 교체는 픽셀 정렬을 하나도 안 건드린다.
- 두 변화(테두리 + 배경)를 **한 상태로 묶는다.** 호버 상태는 2개가 아니라 1개다.
- `pointerout`에서 기본 상태로 정확히 되돌린다. 선택 확정 시에는 화면 자체가 사라지므로
  "선택됨" 상태는 만들지 않는다.

#### ⓖ 이 절에서 **안 만드는 것**

`UpgradeDef`의 설명 문자열 필드 / 툴팁·상세 팝업 / 아이콘 PNG(§9-4 그대로 코드 도형 배지) /
카드 재추첨(리롤)·스킵 / 키보드 선택·숫자 키 단축키 / 카드 등장 애니메이션·트윈 /
"이 업그레이드를 몇 번 골랐는지"의 히스토리 표시(좌하단 배지 §8-15가 이미 담당) /
DPS 같은 **합성 지표**(2개 이상 stat을 곱해 만든 수는 어느 카드에도 안 쓴다 — 새 계산 축이다).

### 9-6. 오디오

BGM **1곡**만. 타이틀·시련 3종·결과 화면 공통이며 시련별 곡 전환은 없다(전환 규칙이라는 축이 생긴다).
mp3(Safari 포함 전 브라우저 재생 보장), 60초 목표 루프, 기본 음량 0.5, `M` 키 음소거.
브라우저 자동재생 차단 때문에 **타이틀의 첫 입력에서 재생을 시작**한다.
**효과음(피격·발사·처치)은 만들지 않는다** — 곱셈 축이고, 발사는 초당 1.8회라 소음이 된다.

### 9-7. 이 축의 대가 (스코프는 순증가하지 않는다)

에셋·HUD를 넣는 대신 **밸런스 튜닝 왕복을 제한**한다. 상한을 넘으면 `playtest` 측정만 하고
수치를 동결하며, 남은 시간은 제출물 4종에 쓴다.

**상한 2회 → 3회로 개정 (2026-08-08, D37).** 원래 근거는 *"에셋을 스코프에 넣는 대가로 시간이
모자랄 것"*이었는데 **그 전제가 실측으로 깨졌다** — 렌더 축 구현에 4시간 30분을 배정했으나
`tech`가 **38분**에 끝냈다. 예산의 근거가 시간이었으므로 시간이 남으면 예산도 남는다.
늘린 1회를 무엇에 쓰는지도 미리 못박는다: **3회차 = `XP_CURVE.GROWTH`, 4회차 = `ENEMIES.boss.HP`**
(§6-A). ~~동결 시점은 4회차 직후이며, 5회차는 없다.~~

**상한 3회 → 4회로 재개정 (2026-08-08, D44). 「5회차 없음」 조항을 폐기한다.**
사유는 시간이 아니라 **적용 범위의 구멍**이다 — 4회차의 `ENEMIES.boss.HP`는 `bossSpawnSec 999`인
연꽃에 **수학적으로 영향이 0**인데(§6-A-1) 연꽃은 3지표 미달이다. 즉 D37의 계획대로 동결하면
**미달 3개짜리 시련을 한 번도 안 건드리고 낸다.** 5회차 = `TRIALS[lotophagoi].enemyHpMul` 하나뿐.

**이번에 지불하는 대가 (하네스 7 — 순증가 제안은 거절이다):**

1. **이타카의 레벨업 미달을 「확정 미달」로 확정한다**(§6-B). 5회차 축이 연꽃 로컬이라 이타카는
   증명 가능한 무동작이고, **이타카를 고칠 회차는 이제 영원히 없다.**
2. **후보 3개를 지금 영구 기각한다** — `PLAYER.PICKUP_RADIUS` / `ENEMIES.brute.XP` /
   `TRIALS[lotophagoi].wKill`. 5회차가 실패해도 이 축들로 도망갈 길을 미리 닫는다.
3. **6회차는 결과와 무관하게 없다.** 5회차 측정 후 연꽃이 여전히 미달이면 §6-B 표에 행을 추가하고
   **그대로 제출한다.** 동결은 조건부가 아니다.
4. 5회차 측정에서 새로 **판정하는 시련은 연꽃 1종뿐**이다. 키클롭스·이타카는 「바이트 단위 동일」
   검증용으로만 돌린다 — 다르게 나오면 그건 밸런스가 아니라 무동작 가정이 깨진 것이다.

### 9-8. 타이틀 화면 (2026-08-08 추가 — D35)

**왜 이 화면만 규격을 따로 두는가**: 심사자가 링크를 눌러 **처음 보는 화면**이고, 동시에
제출물 2번(30~60초 영상)의 **첫 프레임**이다. 지금은 검은 배경 + 흰 글씨뿐이라 첫인상이 비어 있다.
**새 에셋을 만들지 않고** 기존 `arena.jpg` + `player.png`의 파생 1장으로만 채운다.

수치 원본은 `HUD.TITLE`(레이아웃·암막)과 `ASSETS.PATHS.titleHero`(파일 경로)다.

```
 ┌───────────────────────────────────────────────────────┐
 │        (arena.jpg를 1:1로 깔고 그 위에 검정 암막)     │
 │              오디세이 서바이버즈                       │  y 16  18px
 │        ↑↓ 로 시련 선택, Enter/클릭으로 시작            │  y 31   9px
 │                    ┌────────┐                         │
 │                    │ 오디세 │  128×128                │  y 40–168
 │                    │  우스  │  (중심 240,104)         │
 │                    └────────┘                         │
 │              키클롭스의 동굴        보통               │  y 182 14px / 라벨 9px
 │        외눈의 거인이 입구를 막았다. …                  │  y 194  8px
 │           연꽃 먹는 자들의 해안     어려움             │  y 212
 │              이타카의 홀            보통               │  y 242
 └───────────────────────────────────────────────────────┘
        (이름은 중앙 240 정렬 / 난이도 라벨은 x 322 좌측 정렬 = 한 열)
```

**규칙**

1. **배경** — `ASSETS.PATHS.arena`를 `VISUAL.ARENA_IMAGE_SCALE`(1.0) 그대로, 화면 중앙 정렬로 1장 깐다.
   즉 아레나 월드 960×540 중 **가운데 480×270 월드 영역만 보인다.**
   추가 축소를 하지 않는 이유는 D25·§9-9와 같다 — 런타임 재표본은 1 텍셀 = 1 물리 픽셀일 때만 없다.
2. **틴트를 걸지 않는다.** `TrialDef.bgTint`는 **시련의 것**이고 타이틀에는 시련이 없다.
   타이틀에서 틴트를 고르려면 "어느 시련 색인가"라는 결정이 생기고, 그건 §8-2(시련 id 분기 0개)로 가는 길이다.
3. **암막** — 배경 위에 검정 `HUD.TITLE.DIM_ALPHA`(0.75)를 덮는다. 게임 화면은 암막이 **0**,
   3택 화면은 **0.80**(D53에서 0.65 → 0.80), 타이틀은 0.75다.
   → **세 화면의 밝기가 서로 다르다**는 것이 화면 구분의 전부다. **규칙은 "서로 다르다"이지
   "이 순서"가 아니다** — 타이틀은 `arena.jpg` 질감을 보여 주는 게 목적이고 3택은 그 반대(뒤를
   조용히 시키는 것)라 3택이 타이틀보다 어두운 것이 정상이다(§9-5-Aⓔ).
4. **히어로** — `ASSETS.PATHS.titleHero`를 `HUD.TITLE.HERO_PX`(128) **월드/논리 정사각**으로 중심 (240,104)에 그린다.
   파일은 `128 × SUPERSAMPLE` = **384px**이고 카메라 줌 R과 상쇄되어 **1 텍셀 = 1 물리 픽셀 = 재표본 0**이다.
   화면상 크기는 논리 128 그대로라 아래 레이아웃 좌표가 한 픽셀도 안 움직인다. 회전·애니메이션 없음.
5. **시련 3종 목록과 조작 안내는 기능이 그대로다.** ↑↓ 선택 / Enter·Space·클릭 시작, 선택 항목은
   `VISUAL.COLOR.XP_ORB`, 비선택은 `COLOR.TEXT`. 바뀌는 것은 **좌표뿐이며 그 좌표는 `HUD.TITLE`에 있다**
   (128px 히어로가 들어가면서 세로 배치가 재조정됐다. 씬에 좌표 리터럴이 남으면 §8-6 위반).
   **각 항목 오른쪽에 난이도 라벨이 붙는다 — 규격은 §9-8-A.**
6. **폴백 (§8-20)** — `title-hero.png`가 없으면 **그 자리를 비운다.** 대체 도형을 그리지 않는다
   (128px 청록 원은 없는 것보다 나쁘다). `arena.jpg`가 없으면 `VISUAL.COLOR.ARENA_FALLBACK` 단색으로
   대체하고 암막은 그대로 적용한다. **어느 경우에도 콘솔 에러 0건이고, 나머지 요소의 좌표는 절대 좌표라
   한 픽셀도 움직이지 않는다.** 레이아웃이 에셋 유무에 따라 흔들리면 그건 폴백이 아니라 두 번째 레이아웃이다.

**안 만들 것** (이 절에서 명시적으로 제외한다)
로고·타이틀 문자 이미지 / 히어로 애니메이션·부유 연출 / 파티클·비네트 / 페이드인 인트로 /
배경 스크롤·패럴럭스 / 타이틀 전용 BGM(§9-6대로 1곡 공통) / 설정 화면·크레딧 화면 /
**타이틀에 새 입력 축**(기존 ↑↓·Enter·Space·클릭이 전부다).

**완료 기준 (O/X)**

| # | O 판정 문장 |
|---|---|
| 31 | 타이틀 화면에 arena 텍스처가 보인다(단색 검정이 아니다). 그 위 암막 알파가 `HUD.TITLE.DIM_ALPHA`에서 오고, 씬에 알파 리터럴이 0개다 |
| 32 | 히어로가 **논리 128×128**을 차지하고, 텍스처 384px이 물리 픽셀과 1:1로 그려진다(계단·흐림 0). 씬에 좌표·크기 리터럴이 0개이고 전부 `HUD.TITLE`에서 온다 |
| 33 | `public/sprites/title-hero.png`만 지운 상태에서 타이틀이 콘솔 에러 없이 뜨고, 시련 3종 목록과 조작 안내의 좌표가 그대로다 |
| 34 | 타이틀에서 시련 3종을 ↑↓·클릭으로 고르고 Enter/클릭으로 시작할 수 있다(기존 동작 유지) |

**이 축의 대가**: 새 생성 에셋 0개, 새 밸런스 축 0개, 새 입력 0개다. 늘어난 것은 **파생 파일 1장 +
`HUD.TITLE` 상수 블록**뿐이고, 그 대가로 `ASSETS.MAX_DERIVED_IMAGE_FILES`를 2로 못박아
"파생은 공짜니까"라는 이유로 파일이 늘어나는 길을 미리 닫았다.

### 9-8-A. 시련 목록의 난이도 표시 (2026-08-08 추가 — D54·D55)

**사용자 지적**: *"보통 어려운 순서대로 맵을 배치하거나 하드/중간/이지 이런 식으로 표시해주잖아."*
타당하다. 목록에 난이도 정보가 **하나도 없었고**, 순서가 `키클롭스 → 연꽃 → 이타카`라
**가장 어려운 것이 가운데** 있다.

**순서는 바꾸지 않는다.** 이타카는 이 게임 전제("집으로 돌아가는 여정", §1)의 **도착지**다.
난이도순으로 재배치하면 집에 도착한 뒤 다시 연꽃 해안으로 돌아가는 목록이 된다 —
**서사가 곧 순서인 게임에서 순서는 표시 축이 아니라 설계다.** 대신 라벨을 붙인다.

수치 원본은 `TrialDef.difficultyTier`(등급) · `DIFFICULTY_DISPLAY`(문자열·색) ·
`HUD.TITLE.LIST_DIFF_X` / `LIST_DIFF_FONT_PX`(좌표) · `VISUAL.COLOR.DIFF_*`(색)다.

#### 등급표 — 감이 아니라 실측이다

| 시련 | ~~5회차~~ | **6회차 실측 클리어율(동결 시점)** | 등급 | 화면 표시 | 색 |
|---|---|---|---|---|---|
| 키클롭스의 동굴 | ~~0.490~~ | **0.455** | `normal` | 보통 | `DIFF_NORMAL` 0x9a9184 |
| 연꽃 먹는 자들의 해안 | ~~0.215~~ | **0.255** | `hard` | 어려움 | `DIFF_HARD` 0xd97b3c |
| 이타카의 홀 | 0.450 | **0.450**(불변) | `normal` | 보통 | `DIFF_NORMAL` 0x9a9184 |

측정 조건: 동결 시점(**6회차**) `npm run sim -- 200`, 시드 고정, 봇 정책 = `BOT`.

**6회차 갱신 결과: 세 등급 모두 그대로다(D58).** 경계 0.35를 다시 적용했을 뿐이고
`TrialDef.difficultyTier` 값도, 화면도 **한 글자도 안 바뀌었다** — 등급이 판단이 아니라 계산이라
갱신이 표 교체로 끝난다는 것이 규칙 1이 노린 바로 그 성질이다(D54-ⓒ).
- 규칙 3(2단계)의 근거는 **더 강해졌다**: 키클롭스 0.455와 이타카 0.450의 차이가 4.0%p → **0.5%p**로
  줄어 표준오차 3.5%p의 **0.14배**다. 두 시련을 가르는 것은 이제 더더욱 노이즈에 이름 붙이기다.
- 연꽃과의 차이는 0.450 → 0.255 = **19.5%p ≈ 5.6 표준오차**로 여전히 실재한다(5회차 6.6 표준오차).

**규칙**

1. **등급은 판단이 아니라 계산이다.** 경계는 `TARGET_METRICS.CLEAR_RATE`의 **하한 0.35 하나뿐**이며,
   `클리어율 ≥ 0.35 → 보통` / `< 0.35 → 어려움`이다. 새 상수를 만들지 않았다.
   → **다음 세션이 "이 라벨 근거가 뭐냐"를 물을 필요가 없다.** 클리어율이 바뀌면 표만 갈아 끼우고
   같은 식을 다시 적용한다.
2. **「쉬움」을 만들지 않는다.** 그 자리는 "클리어율 > 상한 0.70"인데, 그런 시련이 생기면 그건
   라벨을 붙일 일이 아니라 **`CLEAR_RATE` 위반 = 밸런스 결함**이고 수치로 고쳐야 한다.
   즉 3단계는 원리적으로 빈칸이 생기는 구성이다.
3. **그래서 2단계다 — 결정적 근거는 표본 오차다.** 키클롭스 0.490과 이타카 0.450의 차이는 **4.0%p**인데
   n=200에서 비율의 표준오차는 `sqrt(0.25/200)` ≈ **3.5%p**다. **1.1 표준오차 = 측정 노이즈와
   구분되지 않는다.** 이 둘을 「쉬움」과 「보통」으로 가르는 것은 **노이즈에 이름을 붙이는 것**이다.
   반면 0.450 → 0.215는 23.5%p ≈ **6.6 표준오차**라 실재한다. **갈라도 되는 곳이 한 군데뿐이므로
   등급도 두 개다.** (3단계로 보이지 않는 것이 정직함의 비용이다.)
4. ⚠️ **이 라벨은 「봇 측정 기준」이다(하네스 16 — 사람 실측이 아니다).** 봇은 `BOT.PICK_PRIORITY`
   고정 순서로 3택을 고르고 반응 지연 300ms로 도주한다(§7). **사람은 다르게 논다** — 3택을 상황에
   맞춰 고르고 포위를 미리 읽는다. 따라서 **사람 체감과 다를 수 있다.** 그럼에도 붙이는 이유는
   *감으로 정한 라벨보다는 낫기 때문*이고, 그 이상을 주장하지 않는다.
5. **그 한계를 화면에는 적지 않는다.** 심사자에게 "봇"이라는 개념이 없어 각주가 정보가 아니라
   잡음이 되고, 480×270에 텍스트 줄이 하나 더 늘면 §9-8의 「안 만들 것」(새 요소 금지)을 깬다.
   → 한계는 이 문서와 `balance.ts` 주석에만 남는다.
6. **배치** — 시련 **이름과 같은 줄**(`LIST_Y + index × LIST_PITCH`), `LIST_DIFF_X`(322)에
   **좌측 정렬(원점 0, 0.5)**. 3줄의 라벨이 **한 열로 정렬**되어 목록이 표로 읽힌다.
   - 이름 오른쪽에 "붙이지" 않는 이유: 이름은 중앙 정렬이고 폭이 제각각이라 붙이려면 런타임 텍스트
     폭 측정이 필요한데, 시스템 sans-serif(D24)는 **환경마다 자간이 달라** 심사자 PC에서 겹칠 수 있다.
   - tagline 줄에 두지 않는 이유: 난이도는 **선택의 판단 재료**라 선택 대상(이름)과 같은 높이여야 한다.
   - 322의 산출: 가장 긴 이름 「연꽃 먹는 자들의 해안」이 14px에서 약 136px → 중앙 정렬 오른쪽 끝 **308**,
     거기서 14px(이름 한 글자 폭) 띄운 값. **이름이 길어져 308을 넘으면 좌표가 아니라 이름을 줄인다**(D52-ⓐ).
7. **폰트 서열은 이름 14 > 난이도 9 > tagline 8.** tagline보다 큰 이유는 분위기보다 판단 재료가
   우선이기 때문이고, 이름보다 작은 이유는 목록에서 고르는 대상이 여전히 시련 이름이기 때문이다.
8. **라벨 색은 선택 여부와 무관하게 고정이다.** 선택 표시는 이름 색이 `COLOR.XP_ORB`(차가운 색)로
   바뀌는 것 하나이고, 라벨은 `DIFF_NORMAL`/`DIFF_HARD`(무채·따뜻한 색)를 유지한다.
   → **"선택됨"과 "어렵다"가 화면에서 절대 섞이지 않는다.** `DIFF_HARD`가 `COLOR.GRUNT`와 같은 값인 것은
   D13("적 = 따뜻한 색")의 적용이다 — **난이도 경고는 적의 색으로 말한다.**

**안 만들 것** (이 절에서 명시적으로 제외한다)
난이도순 재배치 / 별점·게이지·아이콘 등 라벨 외의 표현 / 난이도 선택 옵션(이지·하드 모드) /
시련별 권장 레벨·클리어율 숫자 노출 / 화면 각주("봇 기준" 문구) / 잠금·해금 순서 /
라벨에 대한 툴팁·설명 / **`src/sim/`이 이 필드를 읽는 모든 형태**.

**완료 기준 (O/X)**

| # | O 판정 문장 |
|---|---|
| 46 | **sim 무오염.** `src/sim/` 아래에서 `difficultyTier` · `DIFFICULTY_DISPLAY` · `DIFF_NORMAL` · `DIFF_HARD` · `DifficultyTier` 검색 결과가 **0건**이다. 1건이라도 있으면 표시 축이 밸런스 축이 된 것이라 불합격 |
| 47 | **표시.** 타이틀에서 3줄의 라벨이 `보통` / `어려움` / `보통` 순으로 뜨고, 세 라벨의 **왼쪽 끝 x가 서로 같다**(한 열). 어느 줄에서도 시련 이름과 **겹치지 않는다**. `TitleScene.ts`에 좌표·폰트·색·문자열 리터럴이 0개이고 전부 `HUD.TITLE`·`DIFFICULTY_DISPLAY`에서 온다(§8-6) |
| 48 | **불변.** `git diff`에서 `TRIALS`의 기존 필드(`goal`/`wTime`/`wKill`/`wBossKill`/`bossSpawnSec`/`spawnTotalCap`/~~`spawnWeights`~~/`spawnIntervalMul`/`enemyHpMul`/`enemySpeedMul`/`timeLimitSec`/`bgTint`)와 배열 **순서**가 한 글자도 안 바뀌었다. `WEAPON_BOW`·`ENEMIES`·`SPAWN`·`XP_CURVE`·`BOT`·`TARGET_METRICS`·`UPGRADE_POOL`도 동일하다(5회차 동결, D44)<br>⚠️ **`spawnWeights`는 이 항목의 대상에서 빠졌다(D56, 6회차).** 이 항목은 **D54(난이도 표시) 작업의 무변경**을 찍는 것이고, D56은 **명시적으로 회차를 열어 이 필드만 바꾼 별개 변경**이다. 판정은 §8-49로 옮긴다 |

**이 축의 대가**: 새 에셋 0개, 새 밸런스 축 0개, 새 입력 0개, 새 화면 줄 0줄(기존 목록 줄에 얹는다).
늘어난 것은 **`TrialDef` 필드 1개 + 상수 6개**뿐이고, 그 대가로 위 「안 만들 것」에 난이도 관련
확장 8종을 미리 닫았다.

~~⚠️ 위 등급표는 5회차 값이다. 6회차(D56)로 밸런스가 한 번 더 움직였으므로 실측이 도착하면 갱신한다.~~
→ **갱신 완료(D58). 등급 3개 모두 불변.** **원본은 §6-A의 회차 기록표이고 이 표는 그 인용이다**
(§6-A-2 = 5회차 / §6-A-3 = 6회차). 밸런스는 **6회차 직후 동결**이며 7회차는 없다(D56-ⓒ).

### 8-G. 적 배치의 서사 정합 (§5-A. 2026-08-08 — 사용자가 "각 스테이지에 맞는 캐릭터만" 지적)

**49·50은 `git diff`와 grep으로, 51은 `npm run sim` 출력으로 찍는다.**

| # | 항목 | O 판정 문장 |
|---|---|---|
| 49 | 전속 배치 | `TRIALS`의 `spawnWeights`가 키클롭스 `[1.0, 0, 0]` / 연꽃 `[0, 1.0, 0]` / 이타카 `[1.0, 0, 0]`이다. **세 배열 모두 0이 아닌 원소가 정확히 1개**이고, `boss` 자리(인덱스 2)는 3행 전부 0이다 |
| 50 | 정체성 일치 | ⓐ `ENEMIES.grunt` 주석의 **첫 줄이 단일 이름**이고(「달려드는 자」), 자신이 나오는 시련 **2종이 모두 이름으로** 적혀 있다. ⓑ `ENEMIES.brute` 주석이 **「연꽃 먹는 자들의 해안」 외의 시련을 자기 정체로 주장하지 않는다.** ⓒ GDD §5 표의 「잡몹」 칸과 `TRIALS[*].spawnWeights` 값이 **행마다 일치**한다.<br>※ 「구혼자/부하」·「0.7, 0.3」 같은 **옛 값의 언급은 위반이 아니다** — 「~였다 / → 」 형태의 이력 기술은 지우지 않는 것이 규칙이다(D57-ⓑ). 위반은 **현재 정체성을 주장하는 문장**일 때만이다 |
| 51 | 무동작 실증(4차) | 6회차 측정의 **이타카 8지표가 5회차(§6-A-2)와 동일**하다. 다르면 밸런스가 아니라 §6-A-1의 「서로소 시련 집합」 가정이 깨진 것이므로 **수치를 만지기 전에 원인을 먼저 보고한다**(§8-30-b와 같은 규칙) |
| 52 | 기록 완비 | §6-A-2(5회차 8지표) · §6-A-3(6회차 8지표) 표가 **둘 다 실측값으로 채워져 있고**, 추정값·계산값이 실측인 것처럼 적힌 칸이 0개다(하네스 16 — tech의 다른 시드 사전 측정은 §6-A-3의 **별도 표**에 「비교만 유효」로 격리돼 있다). §6-B에 **연꽃 행**과 **키클롭스 사망 생존시간 행**이 존재한다 |
| 53 | 동결 | 6회차 실측 이후 `git diff`에서 `TRIALS[*].spawnWeights`가 **더 이상 바뀌지 않았다**(D56-ⓒ·D58: 7회차 없음). §9-8-A 등급표와 `TrialDef.difficultyTier` 3개가 **6회차 클리어율(0.455 / 0.255 / 0.450)에 경계 0.35를 적용한 결과와 일치**한다 |

### 9-9. 논리 해상도와 렌더 해상도의 분리 — `SCREEN.SUPERSAMPLE` (2026-08-08 추가 — D40~D42)

**사용자 지적**: "캐릭터 화질이 너무 안 좋아 보인다." 타당하다.

**원인 (확정됨)**. `index.html`에 `image-rendering: pixelated`가 이미 있으므로 **확대 방식 문제가 아니다.**
캔버스 백킹스토어가 `SCREEN.WIDTH×HEIGHT` = **480×270 고정**이고 `fitCanvasToIntegerScale()`이 CSS로
정수배 확대한다. 즉 grunt는 화면에 아무리 크게 보여도 **원본 텍스처가 14px**이다.
2560px 생성물을 14px로 줄인 것을 3배로 늘린 것이라 **필터를 무엇으로 바꿔도 개선되지 않는다** —
정보가 14px 단계에서 이미 사라졌기 때문이다.

**핵심 진단: 논리 해상도(월드 좌표계)와 렌더 해상도(픽셀 밀도)가 같은 값에 묶여 있었다.**

#### 설계 — 두 축을 분리한다

`SCREEN.SUPERSAMPLE` = **R**(현재 3)을 신설한다.

| | 변경 전 | 변경 후 |
|---|---|---|
| 캔버스 백킹스토어 | 480×270 | **480R × 270R** (R=3 → 1440×810) |
| 카메라 줌 | 1 | **R** |
| **카메라가 보여 주는 월드 영역** | 480×270 | **480×270 (불변)** |
| grunt 표시 크기 | 월드 14 | **월드 14 (이 변경에서는 불변. 나중에 §9-10에서 20으로 개정)** |
| grunt 텍스처 파일 | 14px | **42px** (§9-10 이후 **60px**) |
| 화면상 grunt (CSS 3배 창) | 42 물리px — 단 **14px 텍스처를 3×3 블록으로 늘린 것** | 42 물리px — **42px 텍스처, 텍셀 1:1** |
| 텍스처 정보량 | 2560px 원본의 **0.55%**(14/2560) | 2560px 원본의 **1.64%**(42/2560) = **9배** |

#### ⭐ 왜 이것이 D14를 깨지 않는가 (이 문단이 이 절의 존재 이유다)

D14가 480×270을 동결한 **근거**는 `balance.ts` 주석의 화면 대비 비율들이었다 —
사거리 140 = 화면 폭 **29%**, 보스 반경 14 = 화면 높이 **5%**, 흡수 반경 18,
그리고 `VISUAL.SPRITE_PX`의 근거인 **화면 점유율**.
(이 절이 쓰인 시점의 값은 18/14/22/40이고 점유율 근거는 "90마리 동시" = grunt 13.6%였다.
**그 근거는 §9-10에서 폐기됐고 값도 34/20/30/56으로 개정됐다.** 아래 논증은 그 개정과 무관하게 성립한다 —
논증의 내용은 "점유율은 **월드 뷰 대비 비율**이라 R에 영향받지 않는다"이지 특정 값이 아니기 때문이다.)

**이 비율들이 참조하는 것은 「카메라가 보여 주는 월드 영역」이지 「물리 픽셀 수」가 아니다.**
백킹스토어 R배와 카메라 줌 R배는 정확히 상쇄되므로 **월드 뷰는 480×270 그대로**이고,
따라서 **위 비율이 전부 한 자리도 바뀌지 않는다.** 바뀌는 것은 그 영역을 몇 개의 물리 픽셀로
그리느냐 하나뿐이다.

→ **밸런스 재측정 대상이 0개다.** sim은 `SCREEN`·`VISUAL`을 하나도 읽지 않고(§9 머리말),
`WEAPON_BOW`/`ENEMIES`/`SPAWN`/`XP_CURVE`/`TRIALS`/`BOT`/`TARGET_METRICS`는 **한 글자도 안 바뀌었다.**
(§8-37이 이걸 화면에서 기계적으로 확인한다.)

**다음 세션에서 "해상도를 왜 올렸냐"로 재논의하지 마라. 해상도(월드 뷰)는 안 올렸다.
픽셀 밀도만 올렸고, D14가 지킨 것은 전자다.**

#### 규칙

1. **`balance.ts`의 모든 표시 크기·좌표·폰트 수치는 월드/논리 단위다.** R을 곱해 적지 않는다.
   곱하면 "화면의 N%" 같은 점유율 근거 문장이 전부 거짓이 된다.
2. **텍스처 파일의 픽셀 수 = 표시 크기 × R.** 곱셈은 빌드 스크립트(`scripts/prep-assets.mjs`)가 한다.
   런타임 표시 배율은 항상 `표시 크기 / 텍스처 폭` = 1/R이다.
3. **재표본은 오프라인에서 끝낸다. 런타임은 1 텍셀 = 1 물리 픽셀일 때만 쓴다**(D25에서 이어짐).
4. **`pixelArt: true`(텍스처 NEAREST)는 유지한다.** 텍스처가 항상 1:1로 그려지므로 필터가 결과에
   관여하지 않고, 코드 도형·폴백의 픽셀 정렬이 지금과 같게 남는다. 실물에서 문제가 보이면 그때 tech가 보고한다.
5. **CSS 축소 시에만 스무딩을 켠다**(`SCREEN.SMOOTH_ON_DOWNSCALE`). `pixelated`의 목적은
   "확대할 때 흐려지지 않는 것"이고 축소는 정반대다 — 보간이 없으면 3픽셀 중 2개를 버려
   한글 획과 90마리 실루엣이 불규칙하게 사라진다. **R 도입 전에는 축소가 원리적으로 없었다**
   (480×270은 어떤 창보다 작다). 백킹이 1440×810이 되면서 처음 생긴 경우다.
6. **HUD·3택·타이틀의 텍스트는 논리 px × R로 「래스터화」된다.** 논리 px로 그린 뒤 R배 확대하면
   개선이 정확히 0이다. 구현 방식은 tech가 고르되 **브라우저 실물로 확인**한다(§8-36).

#### R = 3인 근거

R은 취향값이 아니라 **표시 정수배와 같아야 하는 값**이다. 사용자 화면(폭 약 1544px)에서
`fitCanvasToIntegerScale()`이 고른 정수배가 **3**이었고, R을 그 값으로 두면 텍셀:물리픽셀이 1:1이 된다.

- **2가 아닌 이유**: 백킹 960×540을 같은 창에 띄우면 정수배가 1이라 **화면상 그림이 지금보다 작아진다.**
  선명해지는 대신 작아지면 개선이 아니다.
- **4가 아닌 이유**: 텍스처 픽셀 수가 **16배**가 되는데 정수배 4는 폭 1920px 창부터 가능하다.
  지금 창에서는 어차피 3으로 표시되므로 **늘어난 용량이 화면에 도달하지 않는다.** 게다가 §9-2 ③
  arena의 원본이 2560×1440이라 R=4의 3840×2160은 **1.5배 업스케일**이 된다
  (R=3의 2880×1620은 1.125배라 사실상 원본 해상도 안이다).

#### 용량 (D41)

`ASSETS.MAX_IMAGE_TOTAL_KB` **800 → 1200**. 픽셀 수가 9배가 되므로 R=1 실측 합계 68.8KB의
비관적 상한이 **619KB**다. 800은 여유가 29%뿐이고 "화질이 나쁘면 JPEG 품질↑"(D25) 경로가
arena를 배로 키울 수 있다. 1200 + 오디오 2000 = 3200 ≤ `MAX_TOTAL_KB` 4000이라 **상위 상한은 안 건드린다.**

**⚠️ 619KB는 계산이지 실측이 아니다(하네스 16).** tech가 실측치를 보고하면 그 값으로 재검토한다.

**「배경 무손실 PNG 폴백」 조항은 폐기한다** — 2880×1620 무손실 PNG는 약 6MB라 어떤 상한에도 못 들어간다.
**용량이 걸렸을 때 되돌리는 순서는 이 둘뿐이다**:

1. arena JPEG **품질↓**
2. **arena만** 1920×1080(= 월드의 2배)으로 내리고 런타임 1.5배를 허용한다(NEAREST 계단을 감수)

**스프라이트 4종과 title-hero는 어느 경우에도 R배를 유지한다.** 지적의 대상이 캐릭터이고,
스프라이트 전체는 추정 60~80KB로 예산에서 차지하는 몫이 작다.

#### 안 만들 것 (이번 스펙에서 명시적으로 제외)

- **밸런스 수치 변경 0건.** `WEAPON_BOW`/`ENEMIES`/`SPAWN`/`XP_CURVE`/`TRIALS`/`BOT`/`TARGET_METRICS`를
  한 글자도 안 건드린다. **`playtest` 4회차 진단이 진행 중이라 건드리면 측정이 무효가 된다.**
- **`SCREEN.WIDTH/HEIGHT` 변경.** 480×270 = 월드 뷰 크기로 동결(D14) 유지.
- ~~**`VISUAL.SPRITE_PX` 값 변경.** 18/14/22/40은 월드 단위이며 그대로다.~~
  → **이 제외 조항은 §9-10에서 해제됐다.** 이건 D40 세션의 스코프 선언이었고 목적은
  "이번엔 픽셀 밀도 **한 축만** 바꾼다"였다. 밀도 변경이 실물에서 확인된 뒤 **남은 문제가 크기였음이
  드러나** 다음 세션에서 별도 축으로 다뤘다 — 두 축을 같은 세션에 섞지 않았으므로 무엇이 효과였는지 구분된다.
- 해상도 자동 감지·설정 화면·품질 옵션 토글 / dpr별 다중 에셋(1x·2x·3x 셋) /
  스프라이트시트·아틀라스 도입 / 새 에셋 생성(생성 5 + 파생 1 그대로) /
  R을 창 크기에 따라 런타임에 바꾸는 것(= 텍스처를 다시 로드해야 하고 §8-37 판정이 불가능해진다).

#### 완료 기준

**§8-D의 항목 35~39**가 이 절의 완료 기준이다. 전부 브라우저 실물에서 눈으로 O/X를 찍는다.

### 9-10. 표시 크기 개정 — `VISUAL.SPRITE_PX` 18/14/22/40 → **34/20/30/56** (2026-08-08 — D50)

> **렌더 축이다. 밸런스 수치는 한 글자도 안 바뀌었다**(`WEAPON_BOW`/`ENEMIES`(`RADIUS` 포함)/`SPAWN`/
> `XP_CURVE`/`TRIALS`/`BOT`/`TARGET_METRICS`). 밸런스는 5회차로 동결됐다(D44).
> **히트박스 `RADIUS`는 밸런스다 — 바꾼 것은 표시 크기 4개뿐이다.**

#### 무엇이 틀렸었나 (이 문단이 이 절의 존재 이유다)

§9-9(렌더 해상도 분리, R=3)는 실물에서 효과가 확인됐다 — 타이틀 히어로와 HUD 한글이 선명해졌다.
**그런데 인게임 캐릭터는 여전히 작다는 지적이 두 번 나왔다. 남은 문제는 해상도가 아니라 크기였다.**

옛 근거는 이랬다: *"90마리 전부 화면에 들어와도 14²×90 = 17,640 = 화면의 13.6%. 16이면 17.8%가 되어
배경을 덮는다."* **산수는 맞고 전제가 틀렸다.**

| 옛 전제 | 왜 틀렸나 |
|---|---|
| **「`SPAWN.MAX_ALIVE` = 90이 전부 화면에」** | 90은 **아레나 960×540 전체**의 상한이고 카메라 뷰는 그 1/4이다. 무엇보다 **실측 동시 최대 적 p95의 최댓값이 54**(§6-A, 키클롭스 2회차-B)라 90은 5회차까지 **한 번도 도달한 적이 없다.** 도달하지 않은 수를 "최악의 경우"로 잡으면 과대평가다 |
| **「화면당 약 22마리」** (`SPAWN.MAX_ALIVE` 주석) | 아레나 면적비 1/4을 **균등 분포**로 가정한 값이다. `spawnPointOutsideView`(`src/sim/spawn.ts:46`)는 **카메라 뷰 경계 +24px**에 스폰하고 적은 플레이어로 수렴하므로 분포가 화면 쪽으로 치우친다 → 과소평가다 |

**같은 파일 안에서 두 주석이 서로 다른 전제를 쓰고 있었고(90 vs 22), 결과적으로 예산의 절반만 썼다.**

**정직하게 적어 둘 것**: "90마리 전부 화면 진입은 아레나 크기상 **불가능**"은 아니다 — 플레이어가
멈춰 있으면 원리적으로 가능하다. 90을 안 쓰는 이유는 불가능해서가 아니라 **관측된 적이 없어서**다.

#### 새 기준 — 화면당 실제 마릿수 **N = 54**

실측 p95 최댓값 54가 **전부 화면 안에 있다**고 가정한 보수값. 균등분포로 나누지 않는다(위 표 2행).
화면(= 카메라 월드 뷰) 면적 = 480×270 = **129,600 월드단위²**.

**상한 규칙: 적 스프라이트 사각 면적 합 ≤ 화면의 1/3(33.3%).**
근거는 배경이다 — 시련 3종의 시각 구분은 `TrialDef.bgTint` **한 필드뿐**(§9-3·D17)이라
배경이 2/3 이상 남지 않으면 시련이 구분되지 않는다.

| 시련 (`spawnWeights`) | 마리당 평균 사각 면적 | ×54 | 점유율 | 옛 값에서 |
|---|---|---|---|---|
| ~~키클롭스 (grunt .7 / brute .3)~~ | ~~550~~ | ~~29,700~~ | ~~**22.9%** (+보스 3,136 → 25.3%)~~ | ~~11.8%~~ |
| ~~연꽃 (grunt .3 / brute .7)~~ | ~~750~~ | ~~40,500~~ | ~~**31.2%** ← 상한을 정하는 판~~ | ~~16.6%~~ |
| 이타카 (grunt 1.0) | 400 | 21,600 | **16.7%** | 8.2% |

**⚠️ 6회차(D56)에 `spawnWeights`가 바뀌어 위 두 행이 갱신됐다.** `VISUAL.SPRITE_PX`는 **안 바꿨다.**

| 시련 (6회차 `spawnWeights`) | 마리당 평균 사각 면적 | ×54 | 점유율 | 판정 |
|---|---|---|---|---|
| 키클롭스 (grunt 1.0) | 400 | 21,600 | **16.7%** (+보스 3,136 → **19.1%**) | 상한 33.3% 안. 22.9%에서 **개선** |
| 연꽃 (brute 1.0) | 900 | 48,600 | **37.5%** | **상한 33.3% 초과** ← 아래 |
| 이타카 (grunt 1.0) | 400 | 21,600 | **16.7%** | 불변 |

**연꽃 37.5%를 표시 크기로 대비하지 않는다. 이유 넷.**

1. **이 절 자신의 규칙이 그렇다** — 바로 아래 문단이 *"점유율 초과는 표시 크기 문제가 아니라 밸런스
   축에서 잡는다"*고 이미 정해 뒀다. 규칙을 만든 판(연꽃)에서 규칙을 어기면 규칙이 아니다.
2. **N = 54라는 분모가 이미 낡았다.** 54는 키클롭스 2회차-B 값인데 **5회차 연꽃 실측 p95는 90**이다
   (§6-A-2). 즉 이 표의 모든 칸이 과소평가이고, **그건 6회차 이전부터 그랬다.** 표시 크기를 지금
   손대면 **낡은 분모에 맞춰 스프라이트를 줄이는 것**이 된다.
3. **방향이 사용자 지적과 정반대다.** D50은 사용자가 *"캐릭터가 작다"*고 **두 번** 지적해 키운 값이다.
   하루 만에 되돌리면 그 지적을 무효화하고, 텍스처 4종을 다시 뽑아야 해 tech 작업이 늘어난다.
4. **1/3은 지각의 절벽이 아니라 예산이다.** 근거는 *"배경이 남아 시련이 구분된다"*인데 37.5%에서도
   배경은 **62.5%**이고, 게다가 6회차의 연꽃은 적이 **전부 같은 색(흙적·자주)**이라 밝은 모래빛
   `bgTint`(0xc8c090)와의 대비는 옛 2색 혼합보다 오히려 **단순해졌다.**

→ **밸런스 축에서 잡을 수도 없다(동결 중).** 그래서 이 절이 하는 일은 **수를 정직하게 적어 두는 것**뿐이다.
이 사실이 `VISUAL.SPRITE_PX.brute` 30의 상한 근거(*"32면 34.9%로 초과"*)를 **약화시킨다는 것도 적어 둔다** —
그 근거는 이제 "옛 구성에서의 상한"이며, 새 구성에서 30은 **점유율이 아니라 히트박스 비(2.5×)와
grunt 대비 비(1.5×)로만 지지된다.** 다음 세션이 이 값을 만지려 하면 이 문단을 먼저 읽어야 한다.

**⚠️ 상한 90에 정말 도달하고 그 전부가 화면에 들어오면 연꽃은 옛 구성 52.1% / 새 구성 62.5%가 된다.**
그런 판이 나오면 그건 표시 크기 문제가 아니라 **스폰 곡선이 상한에 잘린 것**이고
`TARGET_METRICS.MAX_ALIVE_P95_MAX`(≤90)의 경계이므로 **밸런스 축에서 잡는다. 표시 크기로 대비하지 않는다.**
**5회차 실측 연꽃 p95 = 90은 이미 그 상태다**(§6-A-2).

#### 값 4개와 각각의 상한 근거

| | 옛값 | **새값** | 히트박스 지름 | 표시/히트박스 | grunt 대비 | 이 값을 더 못 올리는 이유 |
|---|---|---|---|---|---|---|
| player | 18 | **34** | 8 | 4.25× | 1.70× | **36 = `PLAYER.PICKUP_RADIUS`(18)×2.** 넘으면 스프라이트 반폭 > 흡수 반경이라 XP 오브가 **몸 안에서** 사라져 "밟는 게 아니라 근처면 먹는다"는 근거가 화면에서 거짓이 된다 |
| grunt | 14 | **20** | 8 | 2.5× | — | 22면 연꽃 여유가 1%p(32.3%)만 남고, brute를 같은 비(1.5배)로 33까지 올리면 **37.8%**로 상한을 넘는다 |
| brute | 22 | **30** | 12 | 2.5× | 1.50× | 32면 연꽃 **34.9% > 33.3%**. 30에서 31.2%로 딱 들어온다 |
| boss | 40 | **56** | 28 | 2.0× | 2.80× | 크기가 유일한 위압감 수단(D5)이라 **grunt 대비 배율(옛 2.86)을 보존**하고 절대 크기만 함께 올렸다 |

- **표시 > 히트박스 지름**은 4개 전부 유지된다(D15의 원칙. 방향이 반대면 "안 닿았는데 맞았다"가 된다).
- **크기만으로 3종이 갈린다**: 20 → 30 → 56. 비 1.50 / 2.80은 옛 비 1.57 / 2.86과 사실상 같다.
- **player < brute였던 것을 고쳤다.** 18 < 22라 플레이어가 화면에서 가장 작은 축이었다.
  `DEPTH.PLAYER`가 최상위라 가려지진 않지만, "90마리 속에서 내가 어디"는 깊이가 아니라 크기·색으로 푸는 문제다.
- **`HUD.TITLE.HERO_PX`(128)는 안 바꾼다.** ⓐ 타이틀은 이미 선명하고 128은 화면 높이의 47%라 작지 않다.
  ⓑ 이 값은 크기이자 **레이아웃 앵커**다 — 바꾸면 `HERO_Y`(104)·`LIST_Y`(182)가 함께 깨져 한 축이 세 축이 된다.

#### 에셋 규격 (§9-2 표가 원본. 여기는 요약)

배포 규격 = 표시 크기 × R(3) → **player 102 / grunt 60 / brute 90 / boss 168**.
텍스처 픽셀 총량 23,436 → 50,328 = **2.15배**.
**⚠️ 용량은 계산이지 실측이 아니다(하네스 16).** 현재 이미지 합계 671KB / 상한 1200KB이고
늘어나는 것은 스프라이트 4종뿐(arena·title-hero 불변)이라 상한 안이라고 **추정**한다.
실측은 §8-39(tech가 바이트를 숫자로 보고). 넘으면 §9-9의 되돌리기 순서(① arena 품질↓ → ② arena 1920×1080)를 먼저 쓴다.

#### 안 만들 것 (이번 개정에서 명시적으로 제외)

- **밸런스 수치 변경 0건.** 히트박스 `RADIUS` 4개 포함. 5회차 동결(D44)을 깨지 않는다.
- **`SCREEN.WIDTH/HEIGHT`(D14)·`SCREEN.SUPERSAMPLE`(D40) 변경.** 이번 축은 **표시 크기 하나**다.
- **`HUD.*` 좌표·폰트 변경.** HUD는 지적 대상이 아니었다(§9-9로 이미 선명해졌다).
- 새 에셋 생성(생성 5 + 파생 1 그대로. **같은 원본에서 다시 뽑기만 한다**) / 크기 옵션·줌 토글 /
  적 종류별 스프라이트 추가 / 애니메이션.

#### 완료 기준

**§8-E의 항목 40~42.** 40·41은 코드/문서를 읽어 기계적으로 판정하고, 42는 브라우저 실물로 찍는다.
