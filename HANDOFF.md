# HANDOFF — 새 세션은 이 문서부터 읽는다

작성: 2026-08-08 (`capybara-escape` 세션에서 인계)

## 🚀 첫 행동

**`director`를 호출한다.** 아래 「정한 것」을 브리핑으로 넘기고, 「안 정한 것」 4건을 승인 항목으로 올린다.

시간이 없다. **오늘(8/8)이 게임, 내일(8/9)이 영상·PDF 2종·배포다.**

> `.claude/agents/*.md` 5종은 **이 게임에 맞게 개정 완료**(2026-08-08). 카피바라 잔재는 없다.
> 다만 `playtest.md`의 **목표 지표 합격 구간이 의도적으로 비어 있다** — 그 숫자는 `gd` 소유물이라
> GDD 확정과 동시에 채워야 한다. 그 전의 "통과" 판정은 무효다. (→ `CLAUDE.md`)

## 지금 이 폴더에 있는 것

```
odyssey-survivors/
  CLAUDE.md              ← 프로젝트 하네스 (새로 씀)
  HANDOFF.md             ← 이 문서
  .claude/agents/*.md    ← 5종. capybara-escape에서 바이트 그대로 복사
```

**그 외에는 아무것도 없다.** git 저장소 아님, npm 프로젝트 아님, 코드 0줄, GDD 없음.

## 정한 것 (재논의 금지)

| # | 결정 | 근거 |
|---|---|---|
| 1 | **탕탕특공대(Survivor.io)식 뱀서라이크** | 사용자 발주 |
| 2 | **세계관은 오디세이아** — 집(이타카)으로 돌아가는 여정. **시련 하나 = 스테이지 하나** | 사용자 발주 |
| 3 | **시련 3종**으로 스코프 상한 | 가용 이틀 |
| 4 | **NAN 2026 사전 과제에 제출한다.** 마감 8/10, 배포 8/9 | 사용자 확인 |
| 5 | **카피바라(`capybara-escape`)는 폴백.** 별개 저장소로 살려 두고 8/9에 둘 중 나은 것을 낸다 | 사용자 선택 |
| 6 | **새 저장소 `odyssey-survivors`.** 카피바라 저장소를 피벗하지 않는다 | 사용자 선택 |
| 7 | **에이전트 5종은 그대로 가져다 쓴다** (내용 갱신은 별건) | 사용자 지시 |

## 안 정한 것 — 승인 대기 4건

1. **시련 3종이 무엇인가** (`gd`가 제안 → 사용자 승인)
   - 후보(사용자 미지정): 로토파고이 / 폴리페모스 / 아이올로스 / 키르케 / 세이렌 / 스킬라·카리브디스 / 이타카 구혼자 108명
   - 판단 근거: **같은 코어 루프에 파라미터·적 조합만 다른 것**이어야 한다. 시련마다 새 메카닉이 필요한 조합(예: 스킬라·카리브디스의 좁은 통로)은 구현 축이 늘어난다
   - 보스는 "체력 많은 적 1기"로 표현 가능한 것만. 고유 패턴 3개짜리 보스는 하루에 못 튜닝한다
   - 적 스프라이트는 **코드 생성**으로 가능해야 한다 (외부 에셋은 하네스 11 대상)
2. **8/9 정오 게이트의 판정 기준** — "플레이 가능"이 무엇인지 **플레이 전에** 고정한다. 사후에 정하면 자기기만이 된다
3. **스택 확정** — Phaser 3 + TS + Vite를 그대로 갈지 (권고: 그대로. 배포 경로가 검증돼 있다)
4. **GitHub 저장소 생성 시점** — 커밋 기록 유지가 제출 요건이라 **빠를수록 유리하다**

## 카피바라에서 배운 것 — 다시 겪지 않도록

전 프로젝트 6세션에서 실제로 시간을 잡아먹은 것들이다. 코드를 옮기라는 게 아니라 **같은 함정을 피하라**는 뜻이다.

- **GitHub Pages 배포**: `actions/deploy-pages` 경로가 5회 연속 `deployment_queued`로 정체했다. **`gh-pages` 브랜치 배포로 전환하니 한 번에 성공.** 또 **Pages 배포 ID는 커밋 SHA라서, 실패한 커밋을 그대로 재실행하면 영원히 안 된다** — 새 커밋이 필요하다
- **비정수 devicePixelRatio**: `pixelArt: true`는 NEAREST 샘플링이라 dpr이 1.1 같은 비정수면 **한글 획(1px)이 통째로 소실된다.** 백킹스토어는 두고 **CSS 크기를 `논리크기/dpr`의 정수배로** 맞춘다. dpr≥2에서 N=1로 못 박으면 화면이 우표만 해지니 **N은 정수배 중에서 고른다**
- **Vite CJS 경고**: `package.json`에 `"type": "module"`을 넣으면 CommonJS로 출력하는 헤드리스 시뮬이 깨진다. 둘을 같이 정해야 한다
- **sim 경계 검사**: `tsconfig` 분리만으로는 `src/sim/`의 Phaser import가 **안 잡힌다**(`tsc`가 exit 0). 별도 정적 스캐너(`scripts/check-boundary.mjs` 같은)를 `build`에도 걸어야 실효가 있다
- **안 보고 만들면 안 된다**: 스프라이트·UI를 브라우저로 직접 열어보지 않은 채 쌓아 올렸다가 매번 문제가 났다

## ⚠️ 카피바라 폴백의 실제 상태 (8/8 확인)

**현재 배포된 URL(`https://leehaejin02.github.io/capybara-escape/`)은 구버전이다.**
맵 3종 + DPR 렌더 수리(세션 5·6 변경분)가 **gh-pages에 아직 배포되지 않았다.**

→ 폴백을 진짜 폴백으로 쓰려면 **카피바라 저장소에서 한 번 배포하고 태그로 고정**해야 한다(약 15분).
이건 이 프로젝트가 아니라 **카피바라 세션에서** 할 일이다. 잊으면 8/9에 돌아갈 곳이 낡은 빌드다.

## 제출물 4종 (하나라도 누락 = 심사 제외)

| # | 제출물 | 형태 | 상태 |
|---|---|---|---|
| 1 | 플레이 가능한 빌드 + 전체 소스 | 웹 배포 링크 + 공개 저장소, **커밋 기록 유지** | ☐ |
| 2 | 플레이 동영상 | **30~60초**, 실제 플레이 화면. AI 합성·조작 금지 | ☐ |
| 3 | 게임 소개 문서 | PDF. 제목·한 줄 소개·목표·조작·**종료 조건**·실행 방법·링크 | ☐ |
| 4 | **AI 활용 기술 문서** | PDF. **구조 설명 + AI 대상 주요 프롬프트 및 지시 사항** + 외부 에셋 출처 | ☐ |

> 4번이 승부처다. `CLAUDE.md` 하네스 / `.claude/agents/*.md` 5종 / 에이전트 간 반려 왕복 로그 /
> 커밋 히스토리 / 에이전트별 호출 횟수가 그대로 본문이 된다.
> **마지막에 쓰는 게 아니라 개발하면서 쌓이는 것**이다. 그래서 WORKLOG와 DECISIONS를 처음부터 쓴다.

원문 명세는 `capybara-escape/docs/SUBMISSION.md`에 있다. **이 프로젝트로 복사해 오는 게 좋다.**
